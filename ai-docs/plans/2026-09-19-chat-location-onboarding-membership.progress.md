# Progress: Chat location, deep-link onboarding, and player-to-chat membership — ACTIVE
_Updated: 2026-09-19 17:20_

> Read THIS FIRST → ready to continue. No need to re-read the codebase.

**Branch:** feat/2026-09-19-chat-location-onboarding-membership
**base_commit:** b48ce14778576085f52db79fcbeb6d7333decb18
**Last build:** PASS

**Issue:** #30
**Spec:** ai-docs/plans/2026-09-19-chat-location-onboarding-membership.spec.md

**current_step:** Step 10 — self-review APPROVE (Round 3)
**last_passed_gate:** make verify | 2026-09-19T15:15:34Z | 2341290
**entry_args:** 30

## Next action

**Do this immediately:** Step 12 — finalise INDEX.md, move spec and design to `done/`, propagate the inbox, append the run record, open the PR.

## Subtasks

- [x] 1. Forward migration: `home` scope definition + backfill, `chat_presence`, `chat_membership`, the `chat_knowledge` view, and every hard-coded manifest it moves  (c75b2b2)
- [x] 2. Behaviour tests for `chat_knowledge`
- [x] 3. The peaceful-home instrument
- [x] 4. `store.EnsureOwner`
- [x] 5. `internal/chat` (presence, membership, `DestinationLookup`) + `testdb.Binaries`
- [x] 6. Outbound gate: rename and rewire in one subtask
- [x] 7. `internal/onboard`: the `start` payload codec and the link builder
- [x] 8. The `my_chat_member` handler + `testdb.Binaries`
- [x] 9. The `/start` handler
- [x] 10. Composition root: both routes, the outbound test, the funnel test
- [x] 11. Documentation and the propagation sweep  (c5b1546)

## Decisions log

Append-only, one line per non-trivial decision. Each line is prefixed with the step or phase that made it. Never edit or remove prior entries.

- **Step 7**: design-review ran five rounds; the owner raised the cap twice by explicit decision (3 → 4 after round 3's ITERATE, 4 → 5 after round 4's), each time choosing another round with review over accepting the design as it stood.
- **Step 7**: round 5 returned GO with five `minor` notes and four recommendation bullets; every one is `design-internal`, so `design-writer` folded them in and design-review did not run again.
- **Step 8, subtask 1**: also fixed a second hard-coded `goose_db_version` count in `internal/store/migrate_process_test.go` (`TestMigrate_ConcurrentApplyUnderSameLockIDAppliesOnce`) that the design's file list for subtask 1 did not name — caught by the full `go test ./internal/store/...` gate.
- **Step 8, subtask 11**: the propagation sweep reached two files the design's file list did not name — `.claude/agents/self-review.md` and `.claude/agents/review-findings.md`, whose Chat-safety rows restated the allowlist as the whole gate. `ai-docs/propagation-groups.md` makes that mandatory rather than optional: its domain-invariant row names chat safety and both reviewers as siblings of `ai-docs/domain-invariants.md`.
- **Step 8, subtask 11**: `~/lab-private/DESIGN.md` §16.7 (player↔chat membership) was **not** edited. The owner's answers in this task's spec settle its MVP half — what makes a member, and which chat a raid starts from — but the corpus is outside subtask 11's file list, and editing it is the owner's call to make explicitly. Surfaced rather than absorbed.
- **Step 9**: `make verify` exit 0 — the target is `fmt-check build vet lint file-limits test test-race tidy-check actionlint shellcheck comment-refs import-guard`, so the whole Step-9 gate list ran in it; the whole-module race gate was additionally run alone and reported 29 ok over 29 packages.
- **Step 9**: panic-index needs no row — no `panic(`, `log.Fatal` or `Must…` helper in the changed production files, with the pattern seen to match a constructed control line.
- **Step 9**: domain-invariant sweep found one `time.Now()` (`internal/ingest/loop.go:181`); it is not in this diff and measures poll duration, not a generation/combat/replay path. No postings or item movements, so no posting signature is owed.
- **Step 9**: per-AC sweep run with `-count=1` against a shared server, so no profile was replayed: 78 subtests PASS, 0 FAIL across store, onboard, ingest and chat.
- **Step 9**: the owner chose to record the settled halves of `~/lab-private/DESIGN.md` §16.7 in the corpus; committed there in place as 5b3bc41, outside this PR.
- **Step 9.5**: docs landed in subtask 11; the orchestrator ran the removal sweep itself — every live mention of `NewPoolGate` / `PlayerLookup` is a past-tense record of the rename, not a claim they exist, and KD-30 carries an *Amended by #30* clause.
- **Step 9.5**: `context-status.md` carries the entry with the literal `#TBD-at-Step-12` locator (exactly one occurrence in the file) and no tallies.
- **Step 11 (Round 1)**: all four open findings fixed, none objected; the `.go` batch was authored by `code-writer` in Mode B and committed by the orchestrator after reading the diff.
- **Step 11 (Round 1)**: the orchestrator re-ran the `date` mutation itself — blanking it reds both presence tests and the failure lines name the new assertion, so AC16's "when" is pinned rather than merely present. Source restored from a cp backup and confirmed absent from `git diff --name-only`.
- **Step 11 (Round 1)**: every register row's own verifying command re-run — chat_title occurrences 0 → 4, SELECT in start_handler.go 1 → 0, no `r.Body.Read` left, `go doc` states the Accounts contract. `make verify` 0, five AC packages ok, ratchet 91.17% holds against 91.34%.
- **Step 11 (Round 2)**: all four findings fixed, none objected; `.go` batch authored by `code-writer` in Mode B, committed by the orchestrator after reading the diff.
- **Step 11 (Round 2)**: the orchestrator re-ran the gate mutation itself — removing the presence-error branch builds and reds exactly `TestGate_presenceLookupErrorRefusesEvenForAKnownPlayer` by name, so the fail-closed clause is pinned. `internal/ingest/gate.go` restored from a cp backup and absent from `git diff --name-only`.
- **Step 11 (Round 2)**: the reviewer's parked Half-A item was checked rather than accepted — the only `scope_id` column in the schema (`account.scope_id`) does carry its foreign key, so the FK-only scope arm has no reachable counterexample, unlike the maze arm which `event.maze_id` forced wide.
- **Step 11 (Round 2)**: register commands re-run — ChatOwnerID named in 20 test lines (was 0), two independent stub error fields, ErrChatRefused asserted in the outbound test, no "in this file" left. `make verify` 0, ratchet 91.29% against 91.34%.
- **Step 10**: APPROVE at round 3 of 3. Rounds raised 9, 7 and 3 rows; no row was ever re-opened, so the re-litigation share stayed at zero and the cap was never contested.
- **Step 10**: both rounds' majors were the same class — a test that existed but did not discriminate. The orchestrator confirmed each by its own mutation rather than by reading, and each mutation built before it was read, so behaviour was under test and not the compiler.

## GO notes

| # | round | note | kind | route | resolution |
|---|-------|------|------|-------|------------|
| G1 | 5 | The peaceful-home guard's subject is "any **relation** that … declares a column named `maze_id`, **whatever its primary key**" | design-internal | folded | design § Approach D1 + § Test Design subtask 3 @ b48ce14 — scan narrowed to `table_type = 'BASE TABLE'` / `relkind = 'r'`, with `chat_knowledge` named as what an unfiltered scan would pick up |
| G2 | 5 | AC12 has two clauses: a joiner brings prior discoveries in, **and** "a player who is a member of two chats has each discovery counted in both" | design-internal | folded | design § Test Design subtask 2 @ b48ce14 — scenario (c′), one player in chats A and B, one cell, visible in both |
| G3 | 5 | D2 argues the minimal projection is safe because "a later `first_discovered_at` is an **append**" | design-internal | folded | design § Approach D2 @ b48ce14 — `player_id` stated as deliberately outside the projection, reachable by joining `node_discovery` |
| G4 | 5 | The § Risks row on pre-existing chats covers the case where **no** `my_chat_member` ever arrives | design-internal | folded | design § Approach D6 + § Risks + § Test Design subtask 8 @ b48ce14 — promotion-first path named, scenario (g′) added, presence stated as written on every handled update |
| G5 | 5 | Two incidental factual claims carry no tag | design-internal | folded | design § Risks + § Approach D1 @ b48ce14 — the host clause dropped; the combat sentence narrowed to "no combat or looting **function** exists" with its measurement tag |
| G6 | 5 | Notes 1 and 4 are the two worth folding in carefully | design-internal | folded | discharged by G1 and G4 |
| G7 | 5 | Note 2 is a single scenario line; without it AC12's multi-chat clause ships unpinned | design-internal | folded | discharged by G2 |
| G8 | 5 | The design's § Open questions (the supergroup-upgrade chat-id change) does reach `ai-docs/deferred/_inbox.jsonl` | design-internal | folded | no design edit owed — Step 12 sub-step 5 parses the design; orchestrator confirmed none of the five notes matches a trigger of the *orchestrator originates no spec row* AXIOM |
| G9 | 5 | **Round-trip required:** before Step 8, update the design doc to incorporate each note/recommendation above | design-internal | folded | design @ b48ce14 — orchestrator confirmed all five landed by reading the file, not the delegate's summary |

## Key discoveries (don't re-investigate)

- **The peaceful home is the address space, not a flag.** A home is a `scope` row and carries no `(maze, q, r)`; no relation carries both a `scope` reference and a maze-cell key, and that half has no exemptions, which is what lets AC2 rest on it.
- **`event.maze_id` carries no foreign key** (`-- no FK: an open question, not a missing table`), so a guard keyed on FK-declared maze references alone would miss the one shape the tree already ships. The guard's subject is FK **or** a column named `maze_id`, over ordinary tables only.
- **`chat_knowledge` ships with no Go reader**, following the `metric_*` precedent; its column set, order and types are a permanent contract from merge.
- **Subtask 1 turns `TestCreateOwner_chat_has_no_scope_or_accounts` red** — the test's name encodes the invariant this task reverses. It is renamed and re-pointed in the same subtask, along with the now-false comment in `internal/store/move_test.go`.
- **`go build ./...` runs after every subtask**, so a deleted constructor and its out-of-package caller must move in one subtask — this is why the gate rename and the `cmd/bot` rewire are both subtask 6.
- **`player_started` is emitted when the transaction creates the player row *or* inserts a membership row.** Attribution in `metric_activation_funnel` is by the earliest *chat-bearing* event, so a bare Start followed by a link Start would otherwise be attributable to no chat forever.
- **`bot_kicked` carries `chat_id` as a column dimension**, not only in the payload: `event` is append-only and JSONB cannot express referential integrity, so a null there is permanently unrepairable.

## AC Status

| AC | Status |
|----|--------|
| AC1 | PASS — TestPresenceHandler_add, TestPresenceHandler_secondChatIsIndependent, TestEnsureOwner_chatGetsHomeScope |
| AC2 | PASS — TestPeacefulHome_shippedSchema; its halfA/halfB/surrogate controls plant a violating relation and require the scan to name it |
| AC3 | PASS — TestPresenceHandler_noChunkOrGateCreated |
| AC4 | PASS — TestOutbound_deepLinkThroughTheRealGateAndClient, TestChatStartLink_hostPathAndQuery |
| AC5 | PASS — TestStartHandler_withChatPayload |
| AC6 | PASS — TestStartHandler_secondChatMembershipKeepsFirst |
| AC7 | PASS — TestStartHandler_repeatSameChatLinkAppendsNothing, TestAddMembership_idempotentOnRepeat |
| AC8 | PASS — TestStartHandler_groupMessageCreatesNothing |
| AC9 | PASS — TestStartHandler_bareStartNoPayload |
| AC10 | PASS — TestChatKnowledge (a): exact-set equality, so "and nothing else" is asserted, not implied |
| AC11 | PASS — TestChatKnowledge (b): chat A unchanged by a non-member's discovery, and chat B holds none of A's cells |
| AC12 | PASS — TestChatKnowledge (c) asserts empty BEFORE the membership row, then the cell after; (c′) gives one cell to both chats |
| AC13 | PASS — TestGate_removedThenReAddedChat (refused after removal, nothing positive cached) + TestPresenceHandler_removalAndReAdd (chat, home and membership intact) |
| AC14 | PASS — TestPresenceHandler_removalAndReAdd, TestGate_removedThenReAddedChat (fourth transition) |
| AC15 | PASS — TestPresenceHandler_add, TestStartHandler_withChatPayload, TestPresenceHandler_removalAndReAdd |
| AC16 | PASS — TestPresenceHandler_removalAndReAdd: chat_id read as a column joined back to owner, not from the payload, plus actor, both statuses and update_id |
| AC17 | PASS — TestFunnel_addThenStartAttributesPlayer, TestFunnel_bareStartThenLinkAttributesFirstChat |

## Review register

| id | raised | severity | status | verifying command |
|----|--------|----------|--------|-------------------|
| R1-1 | round 1 | major | fixed@2b664ef | `grep -c chat_title internal/onboard/presence_handler_test.go` (0 = defect); mutation: blank `ChatType`/`ChatTitle`/`Date` in `presenceEventPayload`, then `go test -count=1 ./internal/onboard/` — currently exit 0, 0 FAIL |
| R1-2 | round 1 | major | fixed@2b664ef | `grep -c SELECT internal/onboard/start_handler.go` (1 = defect, 0 = fixed) |
| R1-3 | round 1 | minor | fixed@2b664ef | `grep -n 'r.Body.Read' internal/onboard/outbound_test.go` |
| R1-4 | round 1 | minor | fixed@2b664ef | `go doc ./internal/store EnsureOwner` |
| R1-5 | round 1 | minor | accepted@1 — the design's subtask-3 controls (e)/(f) say "both halves must go red on it", but the control relations carry a `scope (id)` reference and no `owner (id)` one, so Half B cannot be red on them and `TestPeacefulHome_surrogateKeyControls` exercises Half A only. Both halves derive their subject set from the single `mazeTouchingRelations` extractor, and the test asserts the control relation IS in that set — which is the property "would escape both halves" turns on. No code or design change owed. | `go test ./internal/store/ -run TestPeacefulHome` |
| R1-6 | round 1 | nit | accepted@1 — `TestStartHandler_groupMessageCreatesNothing` merges design scenarios (d) and (i) into one case sending `/start` in a group. The private-chat filter at `start_handler.go:59` returns before the text check, so an ordinary group message and a group `/start` take the identical branch; AC8 is covered in the stronger form. | `go test ./internal/onboard/ -run TestStartHandler_groupMessageCreatesNothing` |
| R1-7 | round 1 | nit | accepted@1 — `start_handler.go:67-69` (`msg.From == nil`) has no test. Below severity floor; the ratchet holds at 91.26% against 91.34%. | `make cover-ratchet` |
| R1-8 | round 1 | nit | accepted@1 — `/start@botusername` in a private chat is a no-op (`start_handler.go:64`). Telegram's deep link delivers `/start <payload>`; the `@bot` suffix is a group-chat spelling and no AC covers it. | `grep -n 'startCommand+\" \"' internal/onboard/start_handler.go` |
| R1-9 | round 1 | nit | accepted@1 — `internal/chat/lookup.go:20` names "the ingest package's DestinationLookup interface". Contract-stating rather than a pointer, and the same shape as the tree's shipped precedent (`internal/ingest/gate.go`: "the Telegram client's outbound Gate interface"); `make comment-refs` green. | `make comment-refs` |
| R2-1 | round 2 | major | fixed@2341290 | mutation: rewrite `store.ChatOwnerID`'s literal to `SELECT id FROM owner WHERE telegram_id = $1` (drop the kind predicate and its argument), then `go run ./cmd/testpg -- go test -count=1 ./...` — **exit 0 with 29 `ok` is the defect**; a FAIL naming a `ChatOwnerID` test is the fix |
| R2-2 | round 2 | major | fixed@2341290 | mutation: in `internal/ingest/gate.go` replace `present, presenceErr := g.lookup.BotPresentInChat(ctx, id)` plus its `if presenceErr != nil { return … }` clause with `present, _ := g.lookup.BotPresentInChat(ctx, id)`, then `go test -count=1 ./internal/ingest/` — **exit 0 is the defect**; `--- FAIL: TestGate_presenceLookupErrorRefuses` is the fix |
| R2-3 | round 2 | minor | fixed@2341290 | `grep -c ErrChatRefused internal/onboard/outbound_test.go` (0 = defect) |
| R2-4 | round 2 | nit | fixed@2341290 | `grep -n 'in this file' internal/onboard/doc.go` (a match = defect) |
| R2-5 | round 2 | minor | accepted@2 — Half A's **scope** arm is FK-only (`fkPairsOnRelations(…, "scope", …)`) while the maze arm is FK **or** a column named `maze_id`, so a relation carrying a bare `scope_id bigint` with no foreign key beside a bare `maze_id` would escape it. Examined and ruled not-a-defect *against this diff*: the design states Half A's rule as "a **reference** to `scope (id)`" and puts its two width controls (e)/(f) on the maze arm alone, so the implementation conforms; no shipped relation carries an FK-less scope-ish column, so no reachable shape was demonstrated. Widening it would edit the design — a Design Amendment trigger, not a code fix — and is recorded here rather than spent as a round. | `go test ./internal/store/ -run TestPeacefulHome` |
| R2-6 | round 2 | nit | accepted@2 — `chat_knowledge_test.go` scenario (b) checks chat B's rows with a loose filter (`r.q == 1 || r.q == 2`) rather than an exact set, so a leak of chat A's third cell `(3,0)` would pass. The load-bearing direction of AC11 — chat A **unchanged** by a non-member's discovery — is asserted exactly against `wantA`, and (a), (c), (c′) and (d) are exact-set or exact-length assertions. Below the severity floor. | `go test ./internal/store/ -run TestChatKnowledge` |
| R2-7 | round 2 | nit | accepted@2 — AC17's "both steps attributed to it" is asserted asymmetrically: `funnelPlayerStarted` scans one column, and the **chat** step is proved only by the row existing at all. Examined and ruled sufficient: `metric_activation_funnel`'s row set comes from the `chats` CTE over `bot_added_to_chat`, so a missing add event makes the `QueryRow` return `pgx.ErrNoRows` and the helper `t.Fatalf`s. The chat step is therefore pinned, if implicitly. | `go test ./internal/onboard/ -run TestFunnel` |
| R3-1 | round 3 | nit | accepted@3 — `TestStartHandler_repeatSameChatLinkAppendsNothing` asserts one player owner and one `player_started`, but never counts `chat_membership` rows, so AC7's "one membership of that chat" is not asserted in the handler scenario itself. Examined and ruled sufficient: `chat_membership`'s `PRIMARY KEY (chat_id, player_id)` makes a duplicate unrepresentable, and `TestAddMembership_idempotentOnRepeat` asserts the count directly one layer down. Below the severity floor. | `go test ./internal/onboard/ -run TestStartHandler_repeatSameChatLinkAppendsNothing` |
| R3-2 | round 3 | nit | accepted@3 — `internal/onboard/outbound_test.go:53` discards `io.ReadAll`'s error (`body, _ := io.ReadAll(r.Body)`) inside the fake Bot API server's handler. It is the exact form round 1's own finding named as the tree's precedent (`internal/ingest/loop_test.go:130`), it is test-server code rather than a production path, and a read failure there surfaces at the `json.Unmarshal` assertion three lines later. Below the severity floor. | `grep -n 'io.ReadAll' internal/onboard/outbound_test.go` |
| R3-3 | round 3 | nit | accepted@3 — AC11's trailing clause, "however many other chats the player is a member of", is pinned at cardinality one: scenario (b)'s non-member `playerZ` belongs to exactly one other chat, and no chat's exclusion of scenario (c′)'s cell `(7,7)` — discovered by `playerV`, a member of two chats — is asserted afterwards. Examined and ruled not-a-defect: `chat_knowledge` is `SELECT DISTINCT … JOIN node_discovery nd ON nd.player_id = cm.player_id`, so a chat's row set is a function of its own membership rows alone and the count of a discoverer's *other* memberships is not an input the join could read. Distinct from R2-6, which is about the chat-B filter's looseness in the same scenario. Below the severity floor. | `go test ./internal/store/ -run TestChatKnowledge` |


## Files touched

- `internal/store/migrations/00010_chat_home_presence_membership.sql` — the `home` scope definition and its backfill, `chat_presence`, `chat_membership`, the `chat_knowledge` view
- `internal/store/catalog.go`, `views_test.go`, `migrate_test.go`, `migrate_process_test.go` — the hard-coded schema manifests the migration moves
- `internal/store/owner.go`, `owner_test.go` — `EnsureOwner`; the chat-scope test renamed and re-pointed
- `internal/store/move_test.go` — the comment the seeded row falsified
- `internal/store/chat_knowledge_test.go`, `peaceful_home_test.go` — the view's behaviour and the AC2 instrument
- `internal/chat/**` — presence, membership and the pool-backed `DestinationLookup`
- `internal/ingest/gate.go`, `errors.go`, `doc.go`, `loop.go`, `gate_test.go`, `guards_test.go` — the rename, the new branch order, and the prose that still called the allowlist sufficient
- `internal/onboard/**` — the payload codec, the link builder, both handlers, the outbound and funnel tests
- `internal/testdb/server.go`, `server_test.go` — `Binaries` raised once per new database-backed binary
- `cmd/bot/assemble.go`, `assemble_test.go`, `smoke_test.go` — both routes registered
- `ai-docs/coverage-ratchet.txt` — lowered 91.95 → 91.34 in the subtask-8 commit, justified there
- `ai-docs/key-decisions.md` — KD-50 to KD-55 under a new *Chat front door* section, plus KD-30's amendment clause
- `ai-docs/domain-invariants.md` — new § 10; § 5's no-balance-mechanic paragraph, the shipped-view obligations and the funnel-attribution paragraph; § 6's two gate bullets
- `ai-docs/process-lifecycle.md` — start-up steps 11 and 12
- `ai-docs/context.md` — the layout paragraph (both new packages, the gate, the store clause), the status date and the Code bullet
- `ai-docs/context-status.md` — the per-task entry, its PR locator still `#TBD-at-Step-12`
- `AGENTS.md` — § Domain Rules' chat-safety clause
- `.claude/agents/self-review.md`, `.claude/agents/review-findings.md` — the Chat-safety row (Review group + the domain-invariant propagation row)

## Self-Review (Round 1)

**Verdict:** REJECT

| # | File:line | Severity | Finding | Status |
|---|-----------|----------|---------|--------|
| 1 | internal/onboard/presence_handler_test.go:216 (subject: internal/onboard/presence_handler.go:99-107) | major | **AC16 is recorded PASS on a partial execution of its own scenario.** Design D7 enumerates seven payload fields as what makes `bot_kicked` answer AC16 "on its own and depends on nothing the removal ended": chat telegram id, **type**, **title**, actor telegram id, **the update's own date**, old/new statuses, update id. `TestPresenceHandler_removalAndReAdd` asserts four of them; `chat_type`, `chat_title` and `date` are asserted **nowhere in the tree** (`grep -rn 'chat_title\|\"date\"\|chat_type' internal/onboard/` matches only the struct tags at `presence_handler.go:22-25` and an unrelated fixture, with the pattern seen to match a constructed control line). Mutation proof: blanking all three in `presenceEventPayload` builds and leaves `go test -count=1 ./internal/onboard/` at **exit 0, 0 FAIL**. `date` is AC16's "when"; `chat_title` is the field D7 singles out as unrecoverable after the fact ("no `getChat` is possible afterwards"). Fix: assert `chat_type`, `chat_title` and `date` on the `bot_kicked` row in `TestPresenceHandler_removalAndReAdd`, and on `bot_added_to_chat` in `TestPresenceHandler_add`. | ✅ Fixed |
| 2 | internal/onboard/start_handler.go:92-94 | major | **Raw SQL inside the update-parsing file contradicts D3, and the contradiction has shipped as a false sentence in a durable document.** The handler issues `tx.QueryRow` against a `SELECT id FROM owner WHERE kind = 'chat' AND telegram_id = $1` literal, which is the only SQL in `internal/onboard`'s non-test code. Design D3 — and `ai-docs/key-decisions.md` KD-52, which this PR adds — state the two-package split "keeps SQL and update parsing out of each other's files". It also hand-rolls the `(kind, telegram_id)` predicate `store.EnsureOwner` already holds at `internal/store/owner.go:200-203`, which is the duplication KD-53 forbids one decision later: "a second hand-rolled query would be a silent duplicate of a security-relevant predicate, free to drift from the one the test guards". Fix: lift the read into `internal/chat` (or `internal/store`) as a named function with its own test and call it from the handler. Choosing instead to keep the inline read makes this a **Design Amendment trigger** — spawn the `design-writer` Subagent to amend `ai-docs/plans/2026-09-19-chat-location-onboarding-membership.design.md` D3 and the KD-52 sentence with it; recipe at `.claude/skills/task/SKILL.md` Step 11 fail-loud table. | ✅ Fixed |
| 3 | internal/onboard/outbound_test.go:51-52 | minor | `body := make([]byte, r.ContentLength); _, _ = r.Body.Read(body)` — one `Read` is not guaranteed to fill the buffer, and both the byte count and the error are discarded (AGENTS.md § Code Style, "Never `_ = err`"). A short read truncates `lastBody`, and AC4's assertion then fails at `json.Unmarshal`, so the test is latently flaky. The tree's own precedent is `body, _ := io.ReadAll(r.Body)` (`internal/ingest/loop_test.go:130`); this is the only `ContentLength`-sized read in the module. | ✅ Fixed |
| 4 | internal/store/owner.go:178-190 | minor | `EnsureOwner`'s doc comment does not state its return contract for `Owner.Accounts`: on a **hit** it is always nil (`owner.go:207` returns `Owner{ID, Kind, TelegramID}`), on a **miss** `CreateOwner` fills it. `TestEnsureOwner_playerScopeUnchanged` asserts six accounts on the miss path and nothing asserts the hit shape, so the asymmetry is neither documented nor pinned — a trap for the third call site D3 anticipates (#36). DOC-3 requires the return contract. | ✅ Fixed |

**Minor/nit items recorded, not raised:** 5 — entered in the register as `accepted@1` with their reasons (R1-5 peaceful-home control scope, R1-6 merged group-message scenarios, R1-7 untested `From == nil` branch, R1-8 `/start@bot` spelling, R1-9 cross-package interface name in a comment). Files: `internal/store/peaceful_home_test.go`, `internal/onboard/start_handler.go`, `internal/onboard/start_handler_test.go`, `internal/chat/lookup.go`.

### What was checked

**Gates re-run on the shipped tree (HEAD = 16a49c2), not taken from the progress file:** `go vet ./...` exit 0 · `golangci-lint run` → `0 issues.` · `make test` → GATE-GREEN, 0 `FAIL` lines · `make cover-ratchet` → `91.26% holds against 91.34% (tolerance 0.60 pp)` · `make comment-refs` green · `make import-guard` green · `make file-limits` green. The ratchet drop 91.95 → 91.34 is justified in its own commit (fd81a8a) as AGENTS.md requires.

**Design conformance.** All eleven decomposition rows' files are present; the two file-list departures (`migrate_process_test.go` in subtask 1, the two `.claude/agents/*.md` in subtask 11) are recorded in the Decisions log and the second is mandated by `ai-docs/propagation-groups.md`. GO-notes round trip: the design doc is **unchanged** across `b48ce14..HEAD`, so every `folded` row landed before the implementation diff started; G1 (the `relkind = 'r'` filter), G2 (scenario c′), G3 (`player_id` out of the projection), G4 (D6 promotion-first + scenario g′) and G5 (the narrowed combat sentence with its measurement tag) were each read in the design at HEAD. The design carries no `AC<N> verified by:` command lines (grepped), so §2's AC-verification-grep re-run has no commands to execute; the per-AC proof is the Test Design's named tests, and all 18 tests the `## AC Status` table names exist and run.

**Mutation checks (Pattern 2 — a green test is a claim about the test).** Three run, each rebuilt before the test result was read: (1) reverting `allowKnown`'s allowlist branch to allow outright → `TestGate_presenceIsNeverCached`, `TestGate_presenceLookupErrorRefuses`, `TestGate_removedThenReAddedChat` all FAIL by name; (2) narrowing `player_started`'s emission to `if !playerCreated` → `TestStartHandler_laterLinkAfterBareStart`, `TestStartHandler_secondChatMembershipKeepsFirst`, `TestFunnel_bareStartThenLinkAttributesFirstChat` all FAIL by name; (3) blanking `chat_type`/`chat_title`/`date` → suite stays GREEN, which is finding 1. All three files restored; `git status --porcelain` empty afterwards.

**Domain invariants (§ 4a).** No balance column is written outside `store.Post`/`store.Move` and no `item_movement` insert is added; no posting group, so no basis-document or zero-sum question arises and no posting signature is owed — the three event types are already in `event_type_definition` (migration 00003), which is what the telemetry row demands. No balance/tuning constant enters Go (`chatStartPrefix = "c"` is a namespace token; `startPayloadMaxLen = 64` is the Bot API's documented budget, asserted in a test). Migration 00010 renames and re-purposes nothing and re-numbers no enum — it adds `scope_definition` id 4 and two tables, forward-only. Chat safety is strengthened, not bypassed: the gate now requires allowlisted **and** present, the presence answer is read on every chat call and never cached (`TestGate_presenceIsNeverCached`), and no outbound sender is added. No `time.Now()`, unseeded `math/rand` or map-iteration order on a generation/combat/replay path — `time.Unix(cm.Date, 0)` derives from the update itself. No token, DSN or `api_id`/`api_hash` in any added file.

**Safety and correctness.** Panicking-call audit over every changed non-test `.go` file: no `panic(` / `log.Fatal*` / `log.Panic*` / `Must…` site, with the pattern seen to match a constructed control line — `ai-docs/panic-index.md` needs no row. Every error is wrapped with `%w` and an operation-naming message; `errors.Is` is used for every `pgx.ErrNoRows` and sentinel comparison; no `_ = err` outside the deferred-rollback idiom the suite already uses (finding 3 is the one exception). `ctx` is first on every new function, none stores a context, and no `context.Background()` appears on a request path. No `go` statement is added in production code, and both handlers are stateless empty structs; the race gate is recorded in the Decisions log and was green in `make verify` at fdd5a28, with no code-side change after it. No `…Unchecked` function is added.

**Doc convention.** Every exported item added carries a name-first, third-person doc comment; both new packages carry a package comment (`internal/chat/doc.go`, `internal/onboard/doc.go`). Each new sentinel (`ErrNotAChat`, `ErrNotAPlayer`, `ErrEmptyPayload`, `ErrUnknownPayloadPrefix`, `ErrMalformedPayload`, `ErrEmptyUsername`) is named in the doc of the function that returns it. No `TODO` without an issue reference, no commented-out code. `make comment-refs` green over the whole gated set; the review-judged halves (narration, a bare unqualified name used as a pointer) were read by hand across the new files — R1-9 is the one borderline case, accepted with its reason.

**Prose diff (Pattern 1 — verify every factual claim).** Re-derived rather than read: § 6's gate predicate, branch order, fall-through and never-cached presence against `gate.go:82-118`; § 10's home/membership/knowledge claims against migration 00010 and `chat_knowledge`; § 5's "three of the rows below are now discharged" against the two handlers' `store.Event` values; KD-30's `Amended by #30` clause and KD-50 to KD-55 against the shipped code; `process-lifecycle.md` steps 11 and 12 against `cmd/bot/assemble.go:328,342-346`. The propagation sweep was re-run independently (`grep -rni` over `*.go`/`*.md`/`*.yml`, with a control line): every surviving `NewPoolGate` / `PlayerLookup` mention is history (`ai-docs/plans/done/**`, this task's own design), a past-tense record (KD-30, `context-status.md`), or `TestGate_playerLookupErrorRefuses`, whose subject still exists. KD-52's "keeps SQL and update parsing out of each other's files" is the one claim that does **not** re-derive — that is finding 2.

**Progress-file fields.** `current_step`, `last_passed_gate` and `entry_args` present; `parent_skill` is correctly **omitted** per `ai-docs/templates/progress-format.md` ("omit when the writing skill IS the parent flow"), so its absence is conformance, not a gap.

## Self-Review (Round 2)

**Verdict:** REJECT

| # | File:line | Severity | Finding | Status |
|---|-----------|----------|---------|--------|
| 1 | internal/store/owner.go:38-56 (predicate at :45) | major | **The round-1 fix moved a security-relevant predicate into `store` and left its guard behind.** `store.ChatOwnerID` is a new exported function introduced by 2b664ef; nothing in the tree calls it but `start_handler.go:91`, and it has **no test of its own** (`grep -rn ChatOwnerID --include='*.go' .` → three hits: its doc, its definition, its one call site, with the pattern seen to match a constructed control line). Its `kind = $1` filter is the whole point of the function — the doc comment at :38 says so — and it is unpinned: rewriting the literal to `SELECT id FROM owner WHERE telegram_id = $1` builds, and `go run ./cmd/testpg -- go test -count=1 ./...` then returns **exit 0, 29 `ok`, 0 FAIL**. The standard this falls below is the PR's own: KD-53 (added by this diff) argues the lookup must call `store.PlayerExists` rather than hand-roll a query because "that predicate checks the `(kind, telegram_id)` **pair** and **has a test that says so**, and a second hand-rolled query would be a silent duplicate of a security-relevant predicate free to drift from the one the test guards" — and `PlayerExists`, four functions up the same file, carries `TestPlayerExists_kindPairIsThePredicate`, `_noOwnerReportsFalse`, `_closedPoolSurfacesError` and `_txAndPoolAgree`. `ChatOwnerID` carries none, and its error branch (`owner.go:54`) is unreached by any test. KD-54's "The Start handler's chat side is a read keyed on `(kind = 'chat', telegram_id)`, never `EnsureOwner`" rests on the same predicate. Blast radius today is bounded rather than nil: a drifted kind filter resolving a *player* row makes `chat.AddMembership` return `ErrNotAChat` and the update error out, so it denies rather than escalates — the defence-in-depth is real, and it is also why nothing goes red. Fix: add `TestChatOwnerID_*` beside the `PlayerExists` block in `internal/store/owner_test.go` — the kind pair (a player owner sharing the telegram id reports not-found, a chat owner with it reports found), the miss, and the error path. Code fix; no design or spec edit is owed. | ✅ Fixed |
| 2 | internal/ingest/gate_test.go:204-215 (subject: internal/ingest/gate.go:101-104) | major | **The gate's fail-closed presence clause is cosmetically tested, and this PR ships a durable document claiming otherwise.** `TestGate_presenceLookupErrorRefuses`'s own doc comment says it "pins the moved failure mode: an allowlisted destination whose presence lookup errors is refused — fail-closed". It does not. `stubLookup` carries **one** `err` field returned by *both* `PlayerExists` and `BotPresentInChat` (`gate_test.go:45-63`), so with the clause deleted the call falls through to the player lookup, which errors too, and the `errors.Is(err, ErrChatRefused)` assertion holds either way. Mutation proof: replacing `present, presenceErr := …` plus `if presenceErr != nil { return … }` with `present, _ := …` builds, and `go test -count=1 ./internal/ingest/` returns **exit 0, `ok … 7.737s`**. The instrument is live, not dead — the control mutation (drop the whole presence branch, allowlist allows outright) reds three tests by name in the same command: `TestGate_presenceIsNeverCached`, `TestGate_removedThenReAddedChat`, `TestGate_presenceLookupErrorRefuses`. The design's § Test Design subtask 6 lists "a presence lookup that errors refuses rather than allows, so the gate still fails closed" as a scenario it owes, and KD-53 states the behaviour change and calls it "**pinned by a scenario rather than left to be discovered**" — so `ai-docs/key-decisions.md` now carries a sentence the tree does not support. The clause is load-bearing for exactly one destination class KD-53 names: an id that is **both** allowlisted **and** a known player, during a presence-read failure — refused today, allowed without the clause. Fix: give `stubLookup` independent `playerErr` / `presenceErr` fields and add a scenario with the presence lookup erroring while `players[id]` is true, asserting `ErrChatRefused`. Code fix — KD-53's sentence becomes true once the scenario exists, so **no Design Amendment is owed**. | ✅ Fixed |
| 3 | internal/onboard/outbound_test.go:124-126 | minor | AC13's outbound half is asserted as `if err := sendWithLink(); err == nil { t.Fatal(…) }` — any error passes, including a transport or marshalling failure that would mean the gate never ran. `grep -c ErrChatRefused internal/onboard/outbound_test.go` → **0**; the module's own precedent for this exact assertion is three files away at `cmd/bot/assemble_test.go:116`, `if !errors.Is(sendErr, ingest.ErrChatRefused)`. Chat safety is the one class where "refused, but by what?" is worth a sentinel. Fix: compare with `ingest.ErrChatRefused` via `errors.Is`. | ✅ Fixed |
| 4 | internal/onboard/doc.go:7 | nit | "The deep-link codec and builder **in this file** are pure: no database, no Telegram call." `doc.go` holds the package comment and nothing else — `EncodeChatStart`, `ParseChatStart` and `ChatStartLink` are all in `link.go`. The claim about purity is true; the locator is false. Fix: "in this package", or name neither. | ✅ Fixed |

**Minor/nit items recorded, not raised:** 3 — entered in the register as `accepted@2` with their reasons (R2-5 the peaceful-home Half A scope arm being FK-only, R2-6 the loose chat-B filter in the `chat_knowledge` scenario (b), R2-7 AC17's chat step proved by the funnel row's existence rather than by a column). Files: `internal/store/peaceful_home_test.go`, `internal/store/chat_knowledge_test.go`, `internal/onboard/funnel_test.go`.

### What was checked

**Round-1 rows re-examined, each by its own registered verifying command.** All four confirmed `fixed@2b664ef`, and the only commit after it (70e662b) touches the progress file alone, so nothing regressed under them. R1-1: `grep -c chat_title internal/onboard/presence_handler_test.go` → 4, and the registered mutation re-run — blanking `ChatType`/`ChatTitle`/`Date` in `presenceEventPayload` builds and now reds **both** presence tests with the failure lines naming the new assertions (`presence_handler_test.go:298,301,304` and `:161,164,167`), where round 1 measured exit 0. `bot_kicked`'s payload now carries all eight D7 fields under assertion (chat telegram id, type, title, actor, date, old/new status, update id) plus the `chat_id` column joined back to `owner`. R1-2: `grep -c SELECT internal/onboard/start_handler.go` → **0**. R1-3: `grep -n 'r.Body.Read' internal/onboard/outbound_test.go` → no match; `io.ReadAll` in its place. R1-4: `go doc ./internal/store EnsureOwner` now states the `Accounts` contract on both paths, and `TestEnsureOwner_returnsExistingOnHit` asserts the nil. Every mutated source was restored from a `tmp/*.bak` copy and `git status --porcelain` was empty afterwards, three times.

**Gates re-run on the shipped tree (HEAD = 70e662b).** `go vet ./...` exit 0 · `golangci-lint run` → `0 issues.` · `make test` → GATE-GREEN, 29 `ok`, 0 FAIL · `make test-race` → exit 0, 29 `ok`, no `WARNING: DATA RACE` · `make cover-ratchet` → `91.17% holds against 91.34% (tolerance 0.60 pp)` · `make comment-refs`, `make import-guard`, `make file-limits` all exit 0. **Instrument note, not a finding against the diff:** two intermediate whole-suite runs reported a wall of `cmd/bot` / `internal/scheduler` reds whose text described the apparatus, not the subject — `failed to execute pg_try_advisory_lock: unexpected EOF`, then `the database system is not yet accepting connections`. `podman logs lab-game-test-postgres` named the cause: `FATAL: could not write to file "pg_wal/xlogtemp.6880": No space left on device` — the long-lived shared server's own filesystem, with 543G free on the host. `make test-db-down` removed it; every gate above is from a run on a fresh server afterwards.

**Design conformance.** The design doc is unchanged across `b48ce14..HEAD`, so every `folded` GO-note row landed before the implementation diff started. All eleven decomposition rows' files are present. The design carries **no** `AC<N> verified by:` command lines (`grep -c 'verified by'` → 0), so § 2's AC-verification-grep re-run has no commands to execute; the per-AC proof is § Test Design's named scenarios, and each handler file's scenario tags were read against that list — subtask 8's (a)–(j) plus (g′) and subtask 9's (a)–(i) plus (e′)/(f′) all have a named test, with (d)/(i) merged as R1-6 accepted. `cmd/bot/assemble.go:342-346` registers exactly the two routes the design names, and `smoke_test.go` pins the transmitted `allowed_updates` to match.

**Mutation checks (Pattern 2).** Four run this round, each rebuilt and the build confirmed before the test result was read: (1) drop `ChatOwnerID`'s kind predicate → whole suite green, which is finding 1; (2) delete the gate's `presenceErr != nil` clause → `internal/ingest` green, which is finding 2; (3) the **control** for (2) — delete the whole presence branch → three tests red by name, which is what makes (2)'s green a statement about the suite rather than about a dead binary; (4) R1-1's registered blanking mutation, now red.

**Domain invariants (§ 4a).** Re-derived, not carried over. No balance column is written outside `store.Post`/`store.Move`; no `item_movement` insert is added; no posting group, so no basis document and no posting signature is owed — the three event types are already in `event_type_definition` from migration 00003. No balance or tuning constant enters Go. Migration 00010 renames, re-purposes and re-numbers nothing: it seeds `scope_definition` id 4, adds two tables and one view, and its backfill is `ON CONFLICT DO NOTHING`. Chat safety is strengthened rather than bypassed — but see finding 2 for the one clause of it that is untested. Panicking-call audit over all 18 changed non-test `.go` files: no `panic(` / `log.Fatal*` / `log.Panic*` / `Must…` site, with the pattern seen to match a constructed control line, so `ai-docs/panic-index.md` needs no row. No `_ = err` in any changed production file; no new `go` statement (`internal/ingest/loop.go:227` is pre-existing and outside this diff); no `context.Background()` on a request path; `ctx` first everywhere; no `…Unchecked` function added. No token, DSN or `api_id`/`api_hash` in any added file.

**Prose diff (Pattern 1 — every factual claim re-derived).** `domain-invariants.md` § 6's new predicate, branch order, fall-through and never-cached presence against `gate.go:88-121`; § 6's "no code path in the shipped tree issues an outbound call" against a grep of every `.API()` call site in non-test code — `internal/health/probe.go:116` (`GetMe`) and `internal/ingest/loop.go:236` (`GetUpdates`), both `ChatNone`; § 10's "every outbound call to it is refused" for a chat with no presence row, traced through `BotPresentInChat`'s `ErrNoRows` → false → fall-through → `PlayerExists(negative id)` → refused; the funnel paragraph's "the predicate filters `chat_id IS NOT NULL` before `DISTINCT ON` picks a row, ties broken by the lower chat id" against `metric_activation_funnel`'s own `WHERE`/`ORDER BY` in migration 00003; "three of the rows below are now discharged" against both handlers' `store.Event` values; `process-lifecycle.md` steps 11 and 12 against `assemble.go:328,342-346`. The propagation sweep was re-run independently (`grep -rni ALLOWED_CHAT_IDS` over `*.md`/`*.go`/`*.yml`, with a control line): every live surface now states allowlist-**and**-presence, and the only untouched matches are history (`ai-docs/plans/done/**`, `context-status.md`'s #22 entry, this task's own design) or `internal/tg/gate.go:66`, which names the seam's occupant rather than claiming sufficiency. **KD-53's "pinned by a scenario rather than left to be discovered" is the one claim that does not re-derive — that is finding 2.**

**Doc convention.** Every exported item added carries a name-first, third-person doc comment, `ChatOwnerID` included; both new packages carry a package comment. Each new sentinel is named in the doc of the function that returns it. `make comment-refs` green over the whole gated set; the review-judged halves were read by hand — R1-9 stands as accepted, and finding 4 is the one false locator.

**Progress-file fields.** `current_step`, `last_passed_gate` and `entry_args` present; `parent_skill` correctly omitted per `ai-docs/templates/progress-format.md`. Contents not reviewed, per this agent's own scope.

## Self-Review (Round 3)

**Verdict:** APPROVE

| # | File:line | Severity | Finding | Status |
|---|-----------|----------|---------|--------|

_No `blocker` or `major` finding is open._

**Minor/nit items recorded, not raised:** 3 — entered in the register as `accepted@3` with their reasons (R3-1 the uncounted membership row in the Start-handler repeat scenario, R3-2 the discarded `io.ReadAll` error in the fake server's handler, R3-3 AC11's "however many other chats" clause pinned at cardinality one). Files: `internal/onboard/start_handler_test.go`, `internal/onboard/outbound_test.go`, `internal/store/chat_knowledge_test.go`.

### What was checked

**Round-2 rows re-examined by their own registered verifying commands, and the two `major` ones by their registered MUTATIONS rather than by inspection.** R2-1: the registered mutation applied — `store.ChatOwnerID`'s literal rewritten to `SELECT id FROM owner WHERE telegram_id = $1` with the kind predicate and its argument dropped — `go build ./...` confirmed green as a separate step, then `go test -count=1 ./internal/store/ -run TestChatOwnerID` → `--- FAIL: TestChatOwnerID_kindPairIsThePredicate (0.07s)`, exit 1. Round 2 measured exit 0 over the whole suite here, so the predicate is now pinned by name. R2-2: the registered mutation applied — the presence branch's `present, presenceErr := …` plus its `if presenceErr != nil { return … }` clause replaced by `present, _ := …` — build green, then `go test -count=1 ./internal/ingest/` → `--- FAIL: TestGate_presenceLookupErrorRefusesEvenForAKnownPlayer (0.00s)`, exit 1, where round 2 measured `ok … 7.737s`. The fail-closed clause is load-bearing and now observable. R2-3: `grep -c ErrChatRefused internal/onboard/outbound_test.go` → 2 (was 0). R2-4: `grep -n 'in this file' internal/onboard/doc.go` → no match. Every mutated source restored from a `tmp/*.bak` copy; `git status --porcelain` empty after each of the three restores and at the end of the round. The only commit after `2341290` is `0b34cf0`, which touches the progress file alone, so nothing regressed under the fixed rows. Round-1 rows stand `fixed@2b664ef`; round-1 and round-2 `accepted@` rows were re-read and none has changed evidence, so none is re-raised — R2-7's accepting reason was re-derived at the source rather than taken on trust: `metric_activation_funnel`'s `chats` CTE is `WHERE e.type = 'bot_added_to_chat' AND e.chat_id IS NOT NULL` (migration 00003), so a missing add event does make the funnel helper's `QueryRow` return no rows and `t.Fatalf`, and the chat step is pinned implicitly as claimed.

**Gates re-run on the shipped tree (HEAD = 0b34cf0).** `go vet ./...` exit 0 · `golangci-lint run` → `0 issues.` · `make verify` exit 0 — the target is `fmt-check build vet lint file-limits test test-race tidy-check actionlint shellcheck comment-refs import-guard`, so the whole list ran, including the race gate over every package · `make cover-ratchet` → `91.36% >= 91.34% (a rise the pre-commit hook would have recorded)`, exit 0 · `make comment-refs`, `make import-guard`, `make file-limits` each exit 0 on their own invocation as well.

**Instrument checks (Pattern 2 — a green instrument is a claim about the instrument).** Three, beyond the two register mutations above. (1) `make comment-refs` was probed for reach rather than trusted: a constructed markdown-path comment appended to `internal/chat/lookup.go` produced `internal/chat/lookup.go:59: markdown-path: ai-docs/domain-invariants.md` and exit 2, so the gate's green covers the two packages this diff adds rather than skipping them. (2) The AC2 guard's base-table filter — the subject of GO note G1 — was probed by widening `relkind = 'r'` to `relkind IN ('r','v')`: `TestPeacefulHome_shippedSchema` goes red with `maze-touching relations = [chat_knowledge chunk event node_discovery], want [chunk event node_discovery]`, which proves both that the scan genuinely reaches views and that the filter is load-bearing rather than decorative. (3) The panicking-call grep over all changed non-test `.go` files returns nothing, with the pattern seen to match a constructed control line — `ai-docs/panic-index.md` needs no row.

**Task and spec conformance.** All 17 ACs traced to named tests that exist and run; the `## AC Status` table's 18 named tests were each resolved in the tree. AC7 and AC11 carry the two nit-level assertion gaps recorded above, both discharged one layer down. No outcome outside `## Scope`: the diff adds no gate row, no raid binding, no membership-ending path, no radar surface — the four things `## Out of scope` names.

**Design conformance.** The design doc is unchanged across `b48ce14..HEAD`, so every `folded` GO-note row landed before the implementation diff started; G1 was additionally re-verified against the shipped guard by probe (2) above. All eleven decomposition rows' files are present. The design carries no `AC<N> verified by:` command lines, so § 2's AC-verification-grep re-run has no commands to execute. Two design claims were re-derived rather than read: the § Risks connection-ceiling arithmetic — `clients × Binaries(10) × parallel × (schemaMaxConns(4)+1) + 32 > ceilingMax(2000)` gives a refusal at `clients × parallel ≥ 40`, matching "from above 49 to above 39" and `internal/testdb/server_test.go`'s four recomputed cases (82, 332, 1632, 5032); and D4's branch order against `internal/ingest/gate.go:93-121` — cache, then allowlisted-and-present, then the fall-through to the player lookup, with `TestGate_allowlistedPlayerDMStillAllowed` and `TestGate_presentNonAllowlistedChatRefusedWithoutPresenceAsked` holding both directions open.

**Domain invariants (§ 4a).** Re-derived. No balance column written outside `store.Post`/`store.Move`; no `item_movement` insert; no posting group, so no basis document and no posting signature is owed — the three event types are already in `event_type_definition` from migration 00003, which is what the telemetry row demands. No balance or tuning constant enters Go. Migration 00010 renames, re-purposes and re-numbers nothing: it seeds `scope_definition` id 4, adds two tables and one view, and its backfill is `ON CONFLICT DO NOTHING` — forward-only, with no deploy window in which two shapes are read. Chat safety is strengthened: the gate now requires allowlisted **and** present for a chat destination, the presence answer is read on every chat call and never cached (`TestGate_presenceIsNeverCached`), and no outbound sender is added. No `time.Now()`, unseeded `math/rand` or map-iteration order on a generation/combat/replay path; `Router.Kinds()` returns a copy of an ordered slice, so the two order-sensitive `slices.Equal` assertions in `cmd/bot` are deterministic rather than map-ordered. No token, DSN or `api_id`/`api_hash` in any added file.

**Safety, concurrency and style.** No new `go` statement in any production file (checked over the whole `*.go` diff, control line seen to match), no `leaktest.Ignore` entry added or changed, and both new packages' `TestMain` is the single statement `os.Exit(leaktest.Main(m, testdb.Main))`. `telego.ChatMemberUpdated.From` is a value type, so `presence_handler.go:103`'s `cm.From.ID` cannot nil-dereference; `OldChatMember`/`NewChatMember` are interfaces and are nil-checked at `:57` before use. Every error is wrapped with `%w` and an operation-naming message; `errors.Is` is used for every `pgx.ErrNoRows` and sentinel comparison. `ctx` is first on every new function, none stores a context, and no `context.Background()` appears on a request path. No `…Unchecked` function is added. `make file-limits` green.

**Doc convention.** Every exported item added carries a name-first, third-person doc comment — `SetPresence`, `Present`, `AddMembership`, `PoolLookup`, `NewPoolLookup`, `EncodeChatStart`, `ParseChatStart`, `ChatStartLink`, `PresenceHandler`, `NewPresenceHandler`, `StartHandler`, `NewStartHandler`, `ChatOwnerID`, `EnsureOwner`, `DestinationLookup`, and the six sentinels — read by hand, since a last-comment-line grep is the wrong instrument for a multi-line block. Both new packages carry a package comment. Each sentinel is named in the doc of the function that returns it. No `TODO`, no `FIXME`, no commented-out code in the `.go`/`.sql` diff. `make comment-refs` green, with its reach into both new packages proved rather than assumed.

**Prose diff (Pattern 1).** The claims added by this diff that a gate cannot see were re-derived against the code: `domain-invariants.md` § 5's "three of the rows below are now discharged" against both handlers' `store.Event` values (`bot_added_to_chat` and `bot_kicked` set `ChatID: &owner.ID`; `player_started` sets `PlayerID` always and `ChatID` only when the lookup resolved); § 6's branch order, fall-through and never-cached presence against `gate.go:93-121`; § 10's "every outbound call to it is refused" for a chat with no presence row, traced `BotPresentInChat` → `pgx.ErrNoRows` → false → fall-through → `PlayerExists(negative id)` → refused; the funnel-attribution paragraph against `metric_activation_funnel`'s own `WHERE`/`ORDER BY`; KD-52's import-edge claim against `internal/ingest`'s actual import set (`internal/tg` only, in `gate.go`); KD-53's "a present chat that is not allowlisted never reaches the presence question" against the allowlist guard at `gate.go:100`; KD-55's event-conditioning rule against both handlers' emission branches. `AGENTS.md` § Domain Rules' new clause is scoped to a **chat** destination, for which the allowlist is genuinely necessary-but-not-sufficient, so it does not over-claim against the player carve-out. The Propagation Rule's siblings landed: `.claude/agents/self-review.md` and `.claude/agents/review-findings.md` both carry the amended Chat-safety row, as `ai-docs/propagation-groups.md`'s domain-invariant row requires.

**Progress-file fields.** `current_step`, `last_passed_gate` and `entry_args` present; `parent_skill` correctly omitted per `ai-docs/templates/progress-format.md`. Contents not reviewed, per this agent's own scope.

**Spawn-prompt contract.** The prompt carried exactly the invocation line, `Spec:`, `Design:`, `Progress:` and the commit range. No `PROMPT-CONTAMINATION` finding.
