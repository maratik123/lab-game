# Progress: Maze persistence — ACTIVE
_Updated: 2026-09-19T04:48:11Z_

> Read THIS FIRST → ready to continue. No need to re-read the codebase.

**Branch:** feat/2026-09-17-maze-persistence
**base_commit:** bbf761705064b277625c3e9efae4b03d31816872
**Last build:** PASS
**Issue:** #29
**Spec:** ai-docs/plans/2026-09-17-maze-persistence.spec.md
**current_step:** Step 11 — review fixes complete (Round 4)
**last_passed_gate:** golangci-lint run | 2026-09-19T04:48:11Z | 122d7bd
**entry_args:** 29

## Next action

**Do this immediately:** every subtask is done and committed; nothing is pushed and no self-review has run — both are the orchestrator's. Step 8 is complete, so the orchestrator resumes at Step 9 (per-AC verification) with `## AC Status` still all `NOT_TESTED`, then Step 10's self-review and Step 12.

## Subtasks

- [x] 1. The forward migration and the event-type registration
- [x] 2. Package skeleton and the stored-map codec
- [x] 3. The maze handle — `Spec`, `Maze`, `Open`, `Lattice()`
- [x] 4. Chunk creation — `EnsureChunkAt`
- [x] 5. Gate allocation — `ActivateChat`
- [x] 6. Depth and discovery
- [x] 7. The concurrency suite
- [x] 8. Key decisions, including the KD-41 amendment
- [x] 9. The invariant, the architecture prose and the Propagation Rule sweep

Group A = 1–7 (code, `code-writer`). Group B = 8–9 (instructions/harness, `general-purpose`).

## Decisions log

- **Step 8**: created at the Step 7 GO (design-review round 2); Group A opens with subtask 1 per the design's `## Handoff plan`, which the orchestrator does not re-derive.
- **Step 8 subtask 1:** the migration filename is `00009_world_persistence.sql` (next free number in `internal/store/migrations`). `event_type_definition` row for `chunk_created` uses id 17 (next after `bot_kicked`'s 16). The first draft of the migration's own doc comment on the two new enums used the phrase "ALTER TYPE … ADD VALUE", which tripped `TestMigrate_hygiene`'s `ADD\s+VALUE` regex against the whole file text (comments included, not just executable DDL) alongside this migration's own `CREATE TABLE`/`INSERT` — reworded to avoid the literal adjacency rather than weakening the migration or the test. The gate coverage the design listed as owed for `go mod tidy`/`git diff go.mod go.sum` does not apply — subtask 1 added no dependency.
- **Step 8 subtask 2:** the golden (`internal/world/testdata/chunkmap.golden`) was generated by a throwaway `TestGenGolden` in the package (writing the file via the real `encode` function over a `maze.NewMap`-built fixture, per D3's parity rule), then deleted before commit — never hand-typed. Format is text (`cell(q,r)=hh` per line, in `LocalCells` order), matching `internal/maze`'s own golden style, so a diff is readable. Added an unlisted `internal/world/helpers_test.go` beyond the decomposition's named subtask-2 files (`testParams`, `testGenerator`, `mapsEqual`) — the design's own `## Test Design` "Fixtures" paragraph anticipates these helpers for later subtasks; kept inside subtask 2's boundary (test-only, no API surface) rather than opening an unplanned file later. A `borderAgrees` helper drafted for subtask 4/5 was removed after `golangci-lint run` flagged it `unused` — subtask 2 must gate green on its own, so it will be (re)added when a subtask actually calls it, not carried unused. Adding a second database-backed (`testdb.Main`-calling) package tripped `internal/testdb`'s own `TestBinaries_matchesTree` manifest guard and its dependent `TestCeiling_formula` literals, both outside the design's own file list — this is the documented update path on the `Binaries` constant's own doc comment ("a new database-backed package must update this constant"), not a design change: `Binaries` 7→8 in `internal/testdb/server.go`, and the four affected arithmetic-comment/`want` pairs in `internal/testdb/server_test.go`'s `TestCeiling_formula` recomputed for the new constant.
- **Step 8 subtask 3:** `Open`'s non-positive-`CreateBudget` and invalid-`Generation` refusals both happen before any statement is issued (validated in that order), matching both refusal cases in `## Test Design`. **Deviation, reported rather than silently absorbed:** § Test Design's subtask-3 block's last bullet — "the budget is a bound, not decoration" — drives `EnsureChunkAt`, which subtask 3 does not implement (`## Decomposition`'s own Depends column has subtask 4 depend on subtask 3, not the reverse, and `EnsureChunkAt` is subtask 4's own entry point per D6). That case is deferred to subtask 4's own commit (in `chunk_test.go`, once `EnsureChunkAt` exists) rather than invented against a not-yet-existing function or skipped; every other subtask-3 bullet is covered here in `maze_test.go`.
- **Step 8 subtask 4:** `Spec.CreateBudget`'s `context.WithTimeout` was moved to wrap the *whole* `EnsureChunkAt` call (unlocked read included), not only `createLocked`'s transaction body as the design's own prose reads literally ("applies... to the creation transaction's own context") — measured, not assumed: with the budget scoped to only the locked path, the deferred budget test (a pool of `MaxConns=1` with a caller transaction already holding the only connection) failed with a plain wrapped `context deadline exceeded` from the *unlocked* read at the outer 5s test ceiling, not `ErrCreateBudget` — because that first read also needs a pool connection and was contending for the same single slot. Widening the budget's scope is strictly a superset of the stated guarantee (nothing that was bounded stops being bounded) and changes no AC; `ActivateChat` (subtask 5) follows the same shape for the same reason. Contract-silence (`TestEnsureChunkAt_MovesNoBalance`) is implemented as a direct `journal_entry` max-id comparison rather than literally via `contract.NewMark`, since `contract.Mark`'s own id field is unexported and the property under test (no journal entry past the pre-creation state) is identical either way — noted since the design's prose names the package call specifically.
- **Step 8 subtask 5:** refactored `EnsureChunkAt`'s single `createLocked` transaction body into a shared generic `lockedTxBody[T any]` (begin READ COMMITTED, lock the maze row, run the caller's body, commit/rollback) reused by `ActivateChat` — `ActivateChat` returns `hexgrid.Chunk`, not `maze.Map`, so the shared skeleton needed a type parameter; a free function, not a method, since Go methods cannot carry their own type parameters. `ring` (the payload field, and the gap check's own metric) is computed as `hexgrid.ChunkDistance(hexgrid.Chunk{}, chosen)` directly from the chosen chunk `gate.Next` already returned — no square root and no index-to-chunk inverse needed, because the "no inverse" cost KD-41 names is specifically about recovering a ring from a **spiral index alone**; here the chunk itself is already in hand. The chat-kind check (`ErrNotAChat`) runs as a plain unlocked read before the existing-gate fast path and before the lock, mirroring D6's fast-path philosophy — a bad `chatID` fails without ever taking the maze lock. `TestActivateChat_BorderAgreementOnGatePath`'s border walk is conditional on the fixture's spiral placement landing the gate adjacent to the pre-created fabric chunk (skips otherwise, with a named reason) since the spiral's actual placement is not this test's own input to control; measured at seed 104 it did land adjacent, so the case ran for real rather than skipping. **Border-walk bug found and fixed in `helpers_test.go`'s `borderAgrees` (introduced in subtask 4, not yet exercised on a non-canonical direction there):** `hexgrid.Lattice.Border(d)` for a non-canonical direction (W/SW/SE) returns `Face.Cell` values in the **neighbour's** own local frame, not `a`'s — `FaceOf`'s own canonicalisation rule (lower-ordinal direction owns the `Cell`) — so treating every returned cell as `a`-local (the original implementation) produced an out-of-range `a.Faces` lookup the moment a border test used such a direction (`TestActivateChat_BorderAgreementOnGatePath` was the first to hit one; subtask 4's own two border cases both happened to use `DirNE`, a canonical direction, so they never exercised the bug). Rewritten to walk `a`'s own `LocalCells()` directly and cross via `Lattice.At`/`Lattice.Locate` — the same crossing test the generator's own border pass in `internal/maze` uses — which needs no assumption about `Border`'s frame convention at all.
- **Step 8 subtask 6:** `RecordDiscovery`'s single `INSERT … SELECT … WHERE kind = 'player' … ON CONFLICT DO NOTHING` cannot by itself distinguish "not a player" from "already discovered" — both leave `RowsAffected() == 0`. Resolved with a second read (owner kind) only on that zero-rows branch, so the common paths (first discovery, a genuine duplicate never disambiguated) stay one statement and only the ambiguous case pays a second round trip; `ErrNotAPlayer` is returned from that second read's own result, never guessed from rows-affected alone. `TestDepth_LaterGateLowersEarlierDepth` avoids the AC13 case being a coin flip on the spiral's actual geometry: since the first activation of a fresh maze always takes the centre chunk (subtask 5's own first case), the second chat's gate is a deterministic function of that alone, computed here via the placement package's own scan directly (not hard-coded), and the probe cell is that predicted chunk's own centre — so "before" is provably positive and "after" is provably zero, with no `t.Skip` needed (unlike the draft in subtask 5's border case, which does skip on ungoverned geometry).
- **Step 8 subtask 7 (Group A complete):** the concurrency suite (`race_test.go`) fans out through `errgroup.Group` per the design's own instruction; loop-variable capture in each `g.Go` closure needs no `i := i` shadow — the module is `go 1.26.0`, past Go 1.22's per-iteration loop-variable scoping. AC7's case reuses `RecordDiscovery` as the caller's own "write something" step (rolled back), which also gives it a second, free assertion (the discovery row is gone) beyond the chunk/event survival AC7 itself asks for. `go run ./cmd/testpg -- go test -race ./internal/world/...` is green with no races reported, run in addition to (not instead of) the plain suite, per the design's own per-subtask gate list for subtask 7. Group A (subtasks 1–7, all code/`*.sql`) is now complete; Group B (subtasks 8–9, instructions/harness) is the orchestrator's per the `## Handoff plan`, and this delegate touches nothing under `ai-docs/**` beyond this progress file.

- **Step 8**: design amended twice after Group A shipped — the first pass corrected the budget's scope, the test-case placement, the forced `internal/testdb` pair and the contract-silence case; the second corrected D8's and D10's account of the shipped mechanisms. The owner raised the design-review cap to 4 (was 3).
- **Step 8**: three code fixes outside the subtask list, each a defect review found rather than a design change — the `CreateBudget` comment pair and the `ErrCreateBudget` message (5b2cda7), and the invented rationale in the event table comment (6c2c2be).
- **Step 8 subtask 8:** the decomposition names four new entries, and four landed — KD-46 (schema, codec, generation version, enum vocabulary), KD-47 (the maze row lock, its ordering rule, the isolation pin, the unlocked fast path and the call budget), KD-48 (depth on read), KD-49 (the chunk-created event) — under a new `## Maze persistence (2026-09-17)` section. **Two placement calls worth recording.** The discovery decision (design D10) is named in no subtask-8 bullet, and rather than mint an unplanned fifth entry it rides KD-46, whose subject is the schema `node_discovery` belongs to and whose rejected-alternatives list is where the rejected composite kind-pinned foreign key has to sit beside KD-17's scoping; the season and seed rulings (D11) ride KD-46 for the same reason — they are refusals about columns that entry describes. **One claim measured in this invocation rather than carried over from the design:** `grep -rn "AppendEvent(\|store.Post(\|store.Move(" --include=*.go internal/ cmd/` returns 52 hits, of which exactly two are outside `_test.go` — the declaration in `internal/store/event.go` and the call in `internal/world/event.go` — and no non-test `store.Post`/`store.Move` caller exists, which is what KD-49's "first event type any production code in this module emits" rests on; the pattern's own `store.Post(`/`store.Move(` hits in test source are the control that it matches. The explicit-row-lock inventory KD-47 states was likewise re-measured at HEAD, not copied: eleven non-test hits, all `internal/scheduler`'s or `internal/world`'s `lockMazeSQL`, none naming `owner`. KD-41 was amended in two places — its data-contract paragraph, whose conditional antecedent now holds, and its closing sentence, which handed exactly the two questions KD-48 answers.
- **Step 8 subtask 9 (Group B complete):** the lock-ordering rule went into a **new `## 9`** of `ai-docs/domain-invariants.md` rather than into § 4, with a one-bullet statement of the rule left in § 4 where a raid-mechanic author meets it — appending rather than inserting also leaves every existing `domain-invariants.md § N` cross-reference (§ 1, § 5, § 6 are the ones that exist) pointing where it did. The chunk-created event went into § 5, beside the posting-signature mechanics, because the fact a reader needs is that a mechanic moving **no** balance owes its events and no signature — the `major` rating there is scoped to a balance-moving mechanic, and `chunk_created` is the first shipped case that is not one. **The sweep named two residues outside the decomposition's file list, both in `ai-docs/key-decisions.md` and both repaired here:** KD-37's "storing the maps is #29's", which read as pending, and KD-43's "*Rejected:* a new `internal/world` package" — which rejected that name for the **world format** and now reads at a glance as contradicting KD-46, so the rejection is scoped and the distinction stated. **Sweep method and its controls:** `grep -rniF` over `.claude/`, `AGENTS.md`, `CLAUDE.md` and the live `ai-docs/` pages (the history surfaces `learnings.md`, `harness-gaps.md`, `context-status.md` and `plans/done|ignored/**` deliberately excluded — `context-status.md` is append-only and gets this task's entry at Step 9.5), for each of the four claims the design enumerates, then re-run after the edits; each pattern was first run against a **constructed** string it must match, and each control matched. One encoding variation earned its keep: `world loader` missed KD-38's `the **world** loader`, so the re-run used `world[^a-z]{0,6}loader`. The markdown link gate was run as CI runs it — the workflow's own relative-link checker over every `*.md`, reporting 255 links checked in this run (a non-zero cardinality, so the instrument reached the corpus rather than an empty set) and failing only on `tmp/**` scratch files, which `git check-ignore` confirms are ignored and which `git ls-files tmp/` confirms are untracked, so a clean checkout never sees them. `~/lab-private/DESIGN.md` was read directly for the two corpus facts § 9 states: §2.2.2 line 69 carries the short-separate-transaction-under-the-world-lock rule, and §3.5 line 162 carries the create-before-the-transition order. **Not done, and named rather than silently skipped:** no AXIOM was added to `AGENTS.md` § Domain Rules for the lock-ordering rule — the design routes it to `ai-docs/domain-invariants.md` specifically, and promoting it would be unapproved scope plus a fan-out grep across `.claude/`.
- **Step 8, after subtask 9:** one claim-discipline fix on top (e46826a) — KD-46's closing clause said `cmd/bot` would wire `internal/world`, which is a forward prediction this delegate originated and no source in the task establishes; replaced with the spec's own out-of-scope wording, that the activation, `move` and look edges calling into it are #36's. `make comment-refs` and the link check were re-run green after it.

- **Step 9**: no panic-index row owed — the touched packages carry no production `panic`/`log.Fatal`, and the index table stays empty. No posting signature owed either: the world package calls neither `store.Post` nor `store.Move`, so it opens no basis document; its `chunk_created` registration is the whole telemetry debt and it ships in this change. Per-AC sweep re-run verbosely after a negative control showed a non-matching `-run` pattern exits 0 with `[no tests to run]`: 24 named world tests and 2 store tests passed, no skip at any level.

- **Step 9.5**: `context.md` needed nothing — subtask 9 had already placed `internal/world` in the layout and the status paragraph. No repo-root user-facing doc exists to contradict. Removal sweep over the whole PR range: nothing deleted or renamed, and the one changed value (`testdb.Binaries`) is stated in no live doc — KD-20 deliberately declines to name it, and the control confirmed the pattern matches where the count does appear.

- **Step 11**: finding 4 was a Design Amendment trigger, not a code fix. The amendment ran twice through design-review (rounds 5 and 6 both ITERATE, each finding a further Test Design claim the tree falsified) and closed at round 7 with GO, no issues and no recommendations, so no GO-notes row is owed. The owner raised the cap three times over the amendment: 4 → 5 → 6 → 7. Round 6's finding was routed to code rather than to the design text — the border case gained a six-direction table instead of the design's coverage claim being narrowed to match a one-direction test.

- **Step 11**: round 2's two majors split by route — the neighbour-read coverage gap was a code fix (c4b9416), the Test Design claims a Design Amendment that closed at design-review round 8 with one minor, folded. The owner raised the cap to 8. Verified before authorising either: with `readNeighbors` short-circuited the whole package stayed green, so the mechanism the criterion names had no verifier at all.

- **Step 11**: round 3's finding closed twice over — a sibling case on the gate path (ee7593d), then the duplication it existed to guard removed outright (49a3792) on the owner's ruling, so the register's own verifying command no longer resolves: the code it names is gone. Re-resolved as the single read site the shared tail now owns, whose mutation fails both cases. Design-review rounds 9, 10 and 11 followed, the owner raising the cap to 11; round 11 returned GO and its two notes are folded.

- **Step 11**: round 4's two findings were both false prose — a status-entry trap bullet describing a skip that no longer exists and crediting a seed placement never reads, and a test comment crediting a sibling test for an adjacency its own fixture forces. R4-3's whole-module red was NOT reproduced: two `go test ./...` runs at 122d7bd were green, `internal/testdb` at 10.3s and 9.8s, so the reviewer's reds were transient container deadlines — its own long-lived test server was up for its mutation runs at the time.

## GO notes

| # | round | note | kind | route | resolution |
|---|-------|------|------|-------|------------|
| G1 | 2 | D10 is the only API in the document without a Go signature | design-internal | folded | design § D10 @ bbf7617 — full receiver form, and `maze_id` stated as coming from the handle |
| G2 | 2 | Subtask 9's sweep set is `.claude/`, `AGENTS.md`, `ai-docs/` and repo-root docs, and it enumerates three falsified claims | design-internal | folded | design § Decomposition subtask 1 + subtask 9 @ bbf7617 — the `00003_event_log.sql` comment repair moved into Group A, subtask 9 states it is out of its sweep, `internal/maze` reachability added to its claim list |
| G3 | 2 | Subtask 4's AC3 case compares the stored map with `Generate(ch, fabric)` called with no neighbour maps | design-internal | folded | design § Test Design subtask 4 @ bbf7617 — split into an isolated case and a beside-a-neighbour case |
| G4 | 2 | Two counts that rot | design-internal | folded | design § D1 + § Test Design @ bbf7617 — literals removed, not re-measured; control: 2 hits at 95a4d16, 0 now |
| G5 | 2 | § Risks names the two-pool-connections shape but its mitigation is a doc comment plus one test | design-internal | folded | design § D5, § D11, § Risks, § Test Design @ bbf7617 — `Spec.CreateBudget` and `ErrCreateBudget`, plus the composition root's sizing rule |
| G6 | 2 | D2 does not state `NOT NULL` on `chunk_type` / `creation_cause` | design-internal | folded | design § D2 @ bbf7617 — both columns `NOT NULL`, with the CHECK's NULL semantics as the reason |
| G7 | 2 | Subtask 5 has no border-agreement case of its own | design-internal | folded | design § Test Design subtask 5 @ bbf7617 — a gate chunk beside an existing fabric neighbour, walking the shared border |
| G8 | 2 | D9's `ring` is carried separately from `spiral_index` — worth one clause saying why | design-internal | folded | design § D9 @ bbf7617 — the KD-41 no-inverse reason quoted |
| G9 | 4 | The shipped `00003_event_log.sql` comment repair and the design give different reasons for the same absent foreign key | design-internal | folded | comment fixed @ 6c2c2be (the invented dangling-read purpose dropped); design § Open questions records the consideration as examined and not carried @ fc62a86 |
| G10 | 4 | D6's "a `chunk` row is never updated after insert" carries no claim tag | design-internal | folded | design § D6 @ fc62a86 — given a **measurement**, not the reviewer's suggested `[derived → the read-does-not-regenerate case]`: that case mutates the blob from the test and would stay green if production code began issuing `UPDATE chunk`, so it cannot verify the invariant |
| G11 | 4 | D5's "every explicit row lock in non-test Go source is `internal/scheduler`'s" is pinned at a commit predating `internal/world` | design-internal | folded | design § D5 @ fc62a86 — re-pinned and names this design's own `lockMazeSQL`; the load-bearing half (none on `owner`) re-measured and sharpened to say which row it protects |
| G12 | 4 | Keep the self-controlled tag shape for every future `grep → no match` tag | design-internal | folded | design @ fc62a86 — all three tags added this round carry their control inside the tag; the existing negative-shaped tags were swept and already carried theirs |
| G13 | 8 | § Test Design's preamble opens "One hazard has now been met **twice**" and this amendment is the third instance | design-internal | folded | design § Test Design @ 78303df — the tally dropped rather than raised, since a count is what makes the sentence rot; the sweep took five more stored counts of the document's own history with it |
| G14 | 11 | The stored-row bullet sits under subtask 4 but its fixture needs `ActivateChat`, which subtask 5 creates | design-internal | folded | design § Test Design @ 07ee88e — moved to subtask 5, naming its own file, with the rule the precedent turns on stated once |
| G15 | 11 | D5 describes two structural layers where the code has three — the transaction skeleton is shared too | design-internal | folded | design § D5, § D6, § D8 and both decomposition cells @ 07ee88e — shared skeleton, per-path body, shared tail; the sweep found two more sentences a layer off |

Every row was confirmed present in the design by reading the region, not by a pattern match: three of the eight were false negatives of the orchestrator's own greps.

## Key discoveries (don't re-investigate)

- Editing a **shipped** migration's comments is established practice here, and the precedent is exact: commit `5db5aa2` edited `00003_event_log.sql`, and its only non-comment change was to that file's `maze_id` comment. DDL is still forward-only.
- `TestMigrate_hygiene` bans `ADD VALUE` in a file that also has CREATE TABLE / INSERT / UPDATE. This migration is unaffected because both enums are new (`CREATE TYPE … AS ENUM`), so tables and the registry `INSERT` may share one file.
- `internal/store/migrate_test.go`'s base-table assertion is an exhaustive `slices.Equal` set. It is **extended, never loosened** — loosening it to a subset check turns the gate green while deleting a schema guard.
- `fkcover_test.go` compares a FK's columns against the index's **leading** columns, so the partial `UNIQUE (maze_id, gate_chat_id)` does not cover the `gate_chat_id` FK; a standalone partial index does.

## AC Status

| AC | Status |
|----|--------|
| AC1 | PASS |
| AC2 | PASS |
| AC3 | PASS |
| AC4 | PASS |
| AC5 | PASS |
| AC6 | PASS |
| AC7 | PASS |
| AC8 | PASS |
| AC9 | PASS |
| AC10 | PASS |
| AC11 | PASS |
| AC12 | PASS |
| AC13 | PASS |
| AC14 | PASS |
| AC15 | PASS |
| AC16 | PASS |

## Review register

| id | raised | severity | status | verifying command |
|----|--------|----------|--------|-------------------|
| R1-1 | 1 | major | fixed@696652b | replace `bctx, cancel := context.WithTimeout(ctx, m.createBudget)` in `EnsureChunkAt` with `bctx := ctx`, then `go run ./cmd/testpg -- go test -count=1 -run TestEnsureChunkAt_BudgetIsABound ./internal/world/` — must be RED (measured: PASS at 5.07s) |
| R1-2 | 1 | major | fixed@696652b | set `spiralIndex := int64(0)`, then `ring := int64(0)`, in `ActivateChat`, and after each run `go run ./cmd/testpg -- go test -count=1 ./internal/world/` — must be RED (measured: green for both) |
| R1-3 | 1 | major | fixed@696652b | call `EnsureChunkAt` with an already-cancelled caller context — the error must not be `ErrCreateBudget` (measured: `world: budgeted call did not finish within its budget: context canceled`, `errors.Is(err, ErrCreateBudget) = true`) |
| R1-4 | 1 | major | fixed@b9159c0 | Design Amendment trigger — `grep -n "it would hang rather than return" ai-docs/plans/2026-09-17-maze-persistence.design.md` must be empty once the design-writer Subagent has amended § Test Design subtask 4 |
| R1-5 | 1 | minor | fixed@696652b | `grep -n "Set\.Depth" internal/world/depth.go` must be empty — re-run at round 2, exit 1. **Re-resolved at round 2:** this command reaches only the `depth.go` site; the finding's prose named a second, "same class, milder" site that it never covered, now carried by R2-3. |
| R1-6 | 1 | — | accepted@1 — `createLocked`'s doc comment restates the locked statement order, which the corpus and `ai-docs/domain-invariants.md` § 9 both fix as the contract, so it is a contract statement rather than DOC-4 narration | `sed -n '196,201p' internal/world/chunk.go` |
| R1-7 | 1 | — | accepted@1 — no panic-index row owed: no `panic(` / `log.Fatal*` / `log.Panic*` in any non-test file this diff adds or changes; a constructed control string matched the same pattern | `grep -nE '(^\|[^[:alnum:]_.])(panic\(\|log\.(Fatal\|Panic)[a-z]*\()' internal/world/*.go internal/store/catalog.go internal/testdb/server.go` → exit 1, control → exit 0 |
| R1-8 | 1 | — | accepted@1 — no posting signature owed: `chunk_created` moves no balance, `internal/world/event.go` is the module's only non-test `AppendEvent` caller, and no non-test `store.Post` / `store.Move` caller exists | `grep -rn "AppendEvent(\|store.Post(\|store.Move(" --include=*.go internal/ cmd/` → 52 hits, 2 outside `_test.go` |
| R1-9 | 1 | — | accepted@1 — the maze row lock is load-bearing and the concurrency suite proves it | drop `FOR NO KEY UPDATE` from `lockMazeSQL`, then `go run ./cmd/testpg -- go test -count=3 -run TestRace ./internal/world/` → RED (`TestRace_ConcurrentActivations` ×2, `TestRace_RepeatedAndRacingRequests` ×1) |
| R1-10 | 1 | — | accepted@1 — AC12's guard test reaches the clause it names | drop `AND gate_chat_id IS NOT NULL` from `readGatesSQL`, then `go run ./cmd/testpg -- go test -count=1 -run TestDepth ./internal/world/` → RED on `TestDepth_FabricChunksDoNotReachIt` |
| R1-11 | 1 | — | accepted@1 — the conditional gate-path border case does not reach green by skipping in the shipped fixture, and `borderAgrees` refuses a zero-cell walk | `go run ./cmd/testpg -- go test -count=1 -run TestActivateChat_BorderAgreementOnGatePath -v ./internal/world/` → PASS, `grep -c SKIP` → 0 |
| R1-12 | 1 | — | accepted@1 — the Propagation Rule sweep's two falsified claims are repaired in the live architecture page: `internal/gate` now reads "called by `internal/world`", `internal/maze` now reads "and by `internal/world`, which calls `Generate`" | `grep -n "which nothing calls yet\|only the world loader reaches" ai-docs/context.md` → empty |
| R2-1 | 2 | major | fixed@21116b0 | `grep -n "would simply be false there" ai-docs/plans/2026-09-17-maze-persistence.design.md` must be empty once the `design-writer` Subagent has amended § Test Design subtask 4 |
| R2-2 | 2 | major | fixed@c4b9416 | short-circuit `readNeighbors` (`internal/world/chunk.go:70`) to `return nil, nil`, then `go run ./cmd/testpg -- go test -count=1 ./internal/world/` — must be RED (measured at `dd4477c`: `ok … 0.694s`, whole package green) |
| R2-3 | 2 | minor | fixed@c4b9416 | `grep -n "never Post's" internal/world/event.go` must be empty; reads `31:// path, never Post's.` at `dd4477c`. The second site of round 1's finding 5, which R1-5's own command never reached |
| R2-4 | 2 | — | accepted@2 — below severity floor: seven `t.Fatalf` assertions format a `*int64` with `%v`, so a failure prints an address rather than the value (measured under the R1-2 mutation: `stored spiral_index = 0x35155e822118, want 1`). Diagnostics only — the assertions themselves discriminate. | `grep -n "spiral_index = %v\|ring = %v\|gate_chat_id = %v" internal/world/activate_test.go` → lines 44, 48, 115, 130, 133, 299, 302 |
| R2-5 | 2 | — | accepted@2 — below severity floor: `testBudgetGraceLimit`'s doc comment says a call taking this long "proves the short budget was never applied"; strictly it is a 10×-headroom instrument (200 ms budget, 2 s limit, 5 s net), not a proof. Test-fixture prose, and the headroom is real. | `sed -n '19,25p' internal/world/helpers_test.go` |
| R2-6 | 2 | — | accepted@2 — re-resolved, not a defect: R1-3's verifying command ("the error must not be `ErrCreateBudget`") assumed the code route of the two its finding offered; the **doc** route was taken instead, and D5 and § Risks now state the sentinel means "this call's context ended", the caller's own cancellation included. Code, both comments and the design agree. Round 3 should read R1-3 as closed on the doc route, not re-run its command. | `grep -n "A caller's own cancellation surfaces identically" ai-docs/plans/2026-09-17-maze-persistence.design.md` |
| R3-1 | 3 | major | fixed@49a3792 | replace `neighbors, err := m.readNeighbors(bctx, tx, chosen)` and its error check in `ActivateChat` (`internal/world/activate.go:122`) with `var neighbors []maze.Map`, adding the `internal/maze` import, then `go run ./cmd/testpg -- go test -count=1 ./internal/world/` — must be RED (measured at `8f171b7`: `ok … 0.728s`, whole package green). The gate-path half of R2-2, whose own command reaches only the explorer site; R2-2 itself is closed and its command is RED as required. **Re-resolved at round 4 — the code this command names is gone:** `49a3792` moved the read into the shared tail, so `ActivateChat` has no `readNeighbors` call to replace. Re-resolved command: insert `neighbors = nil` after `generateInsertAndAppend`'s own read (`internal/world/chunk.go:176-179`), confirm `go build ./...` exit 0, then `go run ./cmd/testpg -- go test -count=1 -v ./internal/world/` — must be RED on **both** `TestEnsureChunkAt_NeighborReadFeedsGeneration` and `TestActivateChat_NeighborReadFeedsGeneration`. Measured at `1b2cac2`: exactly those two FAIL (`chunk_test.go:222: border disagreement at {6 -6}/{-6 0}: 0 vs 1` and `activate_test.go:354: border disagreement at {-6 0}/{6 -6}: 1 vs 0`), 41 PASS, restored tree `ok 1.077s` |
| R3-2 | 3 | — | accepted@3 — below severity floor: the new `ai-docs/harness-gaps.md` entry uses `learnings.md`'s field skeleton and carries no `**at:**`, against 79 of 80 entries and the file header's own skeleton. Not gated (both harness-gaps guards exit 0) and `AGENTS.md` § *Learning Log* reads "same skeleton plus a required `target:` field", so it is a harness ambiguity, not a clear violation | `grep -c "^\*\*Observed:\*\*" ai-docs/harness-gaps.md` → 79 against `grep -c "^### "` → 80 |
| R3-3 | 3 | — | accepted@3 — below severity floor: `findBorderLocal` (`internal/world/chunk_test.go:228`) re-implements the `At`/`Locate` crossing walk `borderAgrees` already performs; six lines of test-local duplication, no behaviour at stake | `sed -n '228,239p' internal/world/chunk_test.go` beside `sed -n '88,116p' internal/world/helpers_test.go` |
| R3-4 | 3 | — | accepted@3 — not a ledger bypass: the new case's `UPDATE chunk SET faces` is a test fixture, and `chunk` is neither a balance column nor an inventory table; established precedent at `chunk_test.go:145` | `grep -n "UPDATE chunk" internal/world/*_test.go` → the two fixture sites, none outside `_test.go` |
| R4-1 | 4 | major | fixed@122d7bd | `sed -n '571p' ai-docs/context-status.md` must no longer say the gate-path border case "executes only when the spiral puts the gate adjacent" nor that "the fixture's seed is what keeps it a test rather than an instrument". Falsified at `1b2cac2` by `grep -n "t\.Skip" internal/world/activate_test.go` → exit 1, empty, against a constructed control (`printf 't.Skip("x")` newline, piped to `grep -c "t\.Skip"` → 1), and by `grep -n "^func Next" internal/gate/next.go` → `func Next(created, gates []hexgrid.Chunk, k int) (hexgrid.Chunk, error)`, which takes no seed |
| R4-2 | 4 | minor | fixed@122d7bd | `grep -n "TestActivateChat_BorderAgreementOnGatePath" internal/world/activate_test.go` must return only the sibling's own `func` line, not a pointer to it from another case's doc comment; reads `276:// which TestActivateChat_BorderAgreementOnGatePath already forces` at `1b2cac2` |
| R4-3 | 4 | — | accepted@4 — not a defect in this diff: `internal/testdb` went red in all three whole-module runs of this round and every failure is a podman HTTP deadline (`.../stop?t=10` and `container exec create`, `context deadline exceeded`) with the named tests themselves reporting `--- PASS`; the package ran green alone (`ok … 469.925s`), and the only `internal/testdb` lines this diff changes (`Binaries` 7→8, `TestCeiling_formula` literals) are read by neither failing test | `go test -count=1 -v ./internal/testdb/` → `--- PASS: TestContainerExists_followsAServersLifetime`, `--- PASS: TestStartServer_PGDATAMountHoldsEveryClientCountProvisioned`, package FAIL from leaktest after `testdb: terminating container: … context deadline exceeded` |
| R4-4 | 4 | — | accepted@4 — below severity floor: `cc6c695` adds the only typographic apostrophes in the live `ai-docs/` set (`call’s`, `caller’s`), four occurrences over two lines. No gate reads them and the character has precedent in `ai-docs/plans/done/2026-09-02-ledger-post-core.design.md` | `git grep -c "’" -- .` → `ai-docs/domain-invariants.md:1`, `ai-docs/key-decisions.md:1`, `ai-docs/plans/done/2026-09-02-ledger-post-core.design.md:2` |
| R4-5 | 4 | — | accepted@4 — R1-6 re-examined against the change, not re-opened: `createLocked`'s doc comment grew from 6 lines to 13 at `a3522b1`, and the added half is design rationale (why activation cannot share the locked body), not narration of an implementation; the one symbol it names, `generateInsertAndAppend`, is same-package, which `ai-docs/doc-convention.md` § DOC-4's exemption keeps | `sed -n '203,215p' internal/world/chunk.go` |
| R4-6 | 4 | — | accepted@4 — below severity floor: D5's `a78c549` tag reads "its definition and its only invocation, both inside `internal/world/chunk.go`'s shared tail"; the definition is at `chunk.go:70`, outside the tail, and only the invocation is inside it. The load-bearing half — one invocation, none in `activate.go` — is true as written | `grep -rn "readNeighbors" --include=*.go internal/` → `chunk.go:68` comment, `chunk.go:70` definition, `chunk.go:176` the one invocation, inside `generateInsertAndAppend` |
| R4-7 | 4 | — | accepted@4 — below severity floor: `TestEnsureChunkAt_ReadUsesStoredRowNotConstants`'s doc comment says the maze "may legitimately hold … either chunk type at the same coordinate across seasons"; a `maze` row is `UNIQUE (biome, season)` and `PRIMARY KEY (maze_id, q, r)` plus the never-updated invariant fix one type per coordinate **per maze**, so the property is across mazes, not within one. Loose scoping in a justification clause; the hand-off it justifies is real and is discriminated | `sed -n '530,540p' internal/world/chunk_test.go` beside `sed -n '19p' internal/store/migrations/00009_world_persistence.sql` |

## Files touched

- `internal/store/migrations/00009_world_persistence.sql` (new)
- `internal/store/migrations/00003_event_log.sql` (comments only)
- `internal/store/catalog.go`
- `internal/store/migrate_test.go`
- `internal/store/migrate_process_test.go`
- `internal/world/doc.go`, `errors.go`, `enums.go`, `chunkmap.go`, `main_test.go`, `helpers_test.go`, `chunkmap_test.go`, `enums_test.go`, `testdata/chunkmap.golden` (new package)
- `internal/testdb/server.go` (`Binaries` 7→8), `internal/testdb/server_test.go` (`TestCeiling_formula` literals)
- `internal/world/maze.go`, `internal/world/maze_test.go`
- `internal/world/chunk.go`, `internal/world/event.go`, `internal/world/chunk_test.go`, `internal/world/helpers_test.go` (extended: `openTestMaze`, `createOwner`, `borderAgrees`, `testCreateBudget`)
- `internal/world/activate.go`, `internal/world/activate_test.go`, `internal/world/chunk.go` (refactored: shared `lockedTxBody`), `internal/world/helpers_test.go` (`borderAgrees` rewritten)
- `internal/world/depth.go`, `internal/world/depth_test.go`, `internal/world/discovery.go`, `internal/world/discovery_test.go`
- `internal/world/race_test.go`
- `ai-docs/key-decisions.md` (KD-46 to KD-49 added; KD-41 amended; KD-37 and KD-43 clauses corrected by the subtask-9 sweep)
- `ai-docs/domain-invariants.md` (§ 4 gains the no-shared-lock bullet, § 5 the no-balance-mechanic paragraph, new § 9), `ai-docs/context.md` (architecture layout, status paragraph, status date)

## Self-Review (Round 1)

**Verdict:** REJECT

| # | File:line | Severity | Finding | Status |
|---|-----------|----------|---------|--------|
| 1 | internal/world/chunk_test.go:378 | major | **`TestEnsureChunkAt_BudgetIsABound` is cosmetic — it passes with the budget removed outright.** Mutating `EnsureChunkAt` (`internal/world/chunk.go:95`) to `bctx := ctx`, dropping `context.WithTimeout` entirely, leaves the case green: `--- PASS: TestEnsureChunkAt_BudgetIsABound (5.07s)` against 0.2 s unmutated. Both assertions survive because `wrapBudget` (`internal/world/chunk.go:113`) wraps whatever context error arrives, including the test's own 5 s ceiling, so `ErrCreateBudget` + `context.DeadlineExceeded` hold either way. D5's whole-call budget — the § Risks mitigation for the pool-exhaustion hang — therefore has no discriminating verifier. Fix: measure elapsed wall time in the case and fail when the call outlives the 200 ms budget by an order of magnitude, so the 5 s ceiling can no longer supply the assertion. | ✅ Fixed (`696652b`) |
| 2 | internal/world/activate.go:119 | major | **AC16's `spiral_index` and `ring` have no discriminating test.** Replacing `spiralIndex := gate.SpiralIndex(chosen)` with `int64(0)` leaves `go test ./internal/world/` green; replacing `ring := hexgrid.ChunkDistance(hexgrid.Chunk{}, chosen)` with `int64(0)` does too. Every fixture that reads either value (`activate_test.go:46`, `activate_test.go:254-260`) activates the first chat of a fresh maze, which always takes the centre chunk, where `gate.SpiralIndex(Chunk{}) == 0` and `ChunkDistance(origin, origin) == 0` — so both assertions are 0 == 0. AC16 is recorded PASS in `## AC Status` on that evidence, and the payload is telemetry a reader joins on. Fix: an activation whose gate lands off-centre (pre-create chunks, or assert on the second chat of `TestActivateChat_TwoChatsRespectSpacing`) asserting the event payload's `spiral_index` and `ring` and the stored `chunk.spiral_index` at non-zero values. | ✅ Fixed (`696652b`) |
| 3 | internal/world/errors.go:30 | major | **`ErrCreateBudget`'s doc comment states a condition the code falsifies (DOC-5 — unchecked behavioural claim).** The comment reads "is returned, wrapping the context deadline error, when a budgeted call does not finish within Spec's CreateBudget". Measured, calling `EnsureChunkAt` with an already-cancelled caller context: `PROBE err = world: budgeted call did not finish within its budget: context canceled` / `errors.Is(err, ErrCreateBudget) = true` / `errors.Is(err, context.DeadlineExceeded) = false`. So the sentinel is also returned for the caller's own cancellation, wrapping `context.Canceled`, and #36 would read an exhausted pool or a held maze row lock where a player's request was simply cancelled. Same-class sweep over this change (DOC-5 requires it): `internal/world/chunk.go:110-112`, `wrapBudget`'s own "when the budget's context is what ended the operation", is false in the same way; `internal/world/maze.go:32-38` and `internal/world/chunk.go:88-93` state no exclusivity and are sound. Fix: either `wrapBudget` takes the parent context too and returns `err` unchanged when the parent is what is done, or both comments stop claiming the condition. | ✅ Fixed (`696652b`) |
| 4 | ai-docs/plans/2026-09-17-maze-persistence.design.md:119 | major | **§ Test Design subtask 4 states a mechanism the shipped code falsifies.** The budget bullet reads: "The case is written so it **fails** if the budget is never applied — it would hang rather than return, which the test's own generous ceiling turns into a failure." Measured (finding 1): with the budget removed the call does not hang, does not fail, and passes in 5.07 s, because `wrapBudget` converts the caller's own ceiling into `ErrCreateBudget`. **Design Amendment trigger** — spawn the `design-writer` Subagent to amend `ai-docs/plans/2026-09-17-maze-persistence.design.md` § Test Design subtask 4 to state what the case actually discriminates once finding 1 is fixed; recipe at `.claude/skills/task/SKILL.md` Step 11 fail-loud table. Do **not** edit the design directly. | ✅ Fixed (design amended, `b9159c0`) |
| 5 | internal/world/depth.go:31 | minor | **DOC-4's review-judged half: a bare unqualified name used as a pointer.** `Depth`'s doc comment reads "matching the placement package's own Set.Depth contract" — a naked `Type.Method` of another package of this module, which is the exact form `ai-docs/doc-convention.md` § DOC-4 names as banned-and-refused-in-review (the package-qualified spelling would have been caught by `make comment-refs` instead). The same class, milder, at `internal/world/event.go:31` ("AppendEvent's no-posting path, never Post's"). Fix: state the contract — "(0, false) when the maze holds no gate" already does — and drop the pointer. | ✅ Fixed (`696652b`) |

**What was checked.**

*Gates, all re-run against the shipped tree at `35ba4da`:* `go vet ./...` (exit 0) · `golangci-lint run` (0 issues) · `golangci-lint fmt -d` (empty) · `make comment-refs` (green) · `make file-limits` (green) · `make import-guard` (green) · `go mod tidy && git diff --exit-code go.mod go.sum` (clean) · `go run ./cmd/testpg -- go test -count=1 ./...` (27 packages ok, 0 FAIL) · `go run ./cmd/testpg -- go test -race -count=1 ./internal/world/...` (ok, no race).

*Per-AC.* AC1 `TestOpen_HappyPath` (biome/seed/season read back, one row) · AC2 `TestEnsureChunkAt_Creation` · AC3 `TestEnsureChunkAt_EqualsGeneration_Isolated` and `_BesideNeighbor`, plus `TestChunkmap_RoundTrip` and the golden · AC4 `TestEnsureChunkAt_BorderAgreement` (both orders) and `TestActivateChat_BorderAgreementOnGatePath` · AC5 `TestRace_NeighboringCreations` (all six directions) · AC6 `TestEnsureChunkAt_Idempotent`, `TestEnsureChunkAt_ReadDoesNotRegenerate`, `TestRace_RepeatedAndRacingRequests` · AC7 `TestRace_CallerRollbackLeavesChunkBehind` · AC8 `TestActivateChat_FirstActivation`, `TestActivateChat_MatchesGateNext` · AC9 `TestActivateChat_Reactivation` + the partial unique index · AC10 `TestRace_ConcurrentActivations`, `TestActivateChat_TwoChatsRespectSpacing` · AC11 `TestDepth_GateCentreIsZero`, `TestDepth_NoGate` · AC12 `TestDepth_FabricChunksDoNotReachIt` · AC13 `TestDepth_LaterGateLowersEarlierDepth` · AC14 the five `TestRecordDiscovery_*` cases · AC15 `TestEnsureChunkAt_Event` + `TestActivateChat_Reactivation`'s event count · AC16 `TestEnsureChunkAt_Event`'s absent-key assertions and `TestActivateChat_Event` — **AC16 is finding 2**: its positive half asserts only zeroes.

*Mutations run (each confirmed to BUILD first).* Budget removed → PASS (finding 1). `spiralIndex := 0` → green (finding 2). `ring := 0` → green (finding 2). `FOR NO KEY UPDATE` dropped from `lockMazeSQL` → RED on two race cases, so the lock is under test. `AND gate_chat_id IS NOT NULL` dropped from `readGatesSQL` → RED on `TestDepth_FabricChunksDoNotReachIt`, so AC12's guard test reaches its clause. The tree was restored and `git status --porcelain` confirmed clean after each.

*Domain invariants.* No balance column is written anywhere in the diff and no inventory table is touched — the world package opens no basis document. `chunk_created` declares its type in the migration and its mirror in `store/catalog.go` in the same change, moves no balance, and therefore owes no posting signature; `internal/world/event.go:53` is the module's only non-test `AppendEvent` caller and no non-test `store.Post` / `store.Move` caller exists (52 matches, 2 outside `_test.go`). No tuning value is a Go literal or constant: the seed, the generation inputs, `k` and the create budget all arrive through `Spec`. The schema change is additive — three new tables, two new enum types created rather than altered, one new `event_type_definition` row at id 17 — with no column renamed or repurposed. No outbound send path and no retry loop. No `time.Now()`, no unseeded `math/rand`, no map-iteration order on a generation or replay path. No token, DSN or `api_id`/`api_hash` in any added file.

*Design conformance.* All nine decomposition subtasks landed with the files they name. Every one of the 12 `## GO notes` rows is routed `folded`; I resolved G1 (D10's full receiver form), G3 (the split AC3 cases), G5 (`Spec.CreateBudget` + `ErrCreateBudget` + the sizing rule), G6 (both columns `NOT NULL` in the migration), G7 (the subtask-5 border case), G9 (the `00003_event_log.sql` comment repair), G10 (D6's measurement tag) and G11 (D5's re-pinned lock inventory) by reading the region, and each is present. The design carries no "AC<N> verified by:" line, so there was no AC-verification grep set to re-run; the design's own measured tags I did re-run instead: D5's explicit-row-lock inventory (scheduler's `scheduled_task`/`process_liveness` plus `internal/world`'s `maze`, none on `owner`) ✓, D6's `UPDATE chunk` negative (one hit, `chunk_test.go:145`) ✓, D8's `UPDATE owner` negative with its `UPDATE scheduled_task` control ✓, D9's "no view reads `depth`/`maze_id`" ✓, KD-49's "first event type any production code emits" ✓.

*Prose claims re-derived rather than read* (`ai-docs/domain-invariants.md` § 4 and new § 9, `ai-docs/key-decisions.md` KD-46–KD-49, `ai-docs/context.md`, `ai-docs/context-status.md`): the explicit-row-lock inventory, the "no shipped view reads it" claim, the "first event type any production code in this module emits" claim, and the two Propagation-Rule residues the subtask-9 sweep named — all four confirmed at `35ba4da`.

*Not raised, recorded in the register:* `createLocked`'s statement-order comment (R1-6), the empty panic index (R1-7), the absent posting signature (R1-8), and the four instrument checks R1-9 to R1-12.

*Spawn prompt:* within the closed list — invocation line, `Spec:`, `Design:`, `Progress:` and a commit range. The range given (`3c525e3..HEAD`, the merge-base with `main`) is wider than the `base_commit` the progress header records (`bbf7617`); reviewed over the prompt's range, which is a superset and adds only the spec, design and interview-state commits.

## Self-Review (Round 2)

**Verdict:** REJECT

| # | File:line | Severity | Finding | Status |
|---|-----------|----------|---------|--------|
| 1 | ai-docs/plans/2026-09-17-maze-persistence.design.md:113 | major | **§ Test Design subtask 4's AC3 bullet states a discriminator the tree falsifies.** The *Beside a neighbour* sub-case is justified by: "the neighbour's stored map passed in, because a created neighbour's stored map is itself an input to generation and **an equality against a no-neighbour call would simply be false there**." It would be true, not false. Every border face is derived from `borderKey(seed, a, b)` and `borderCandidates(lattice, a, b)`, both of which canonicalise the chunk pair (`internal/maze/seed.go:116`, `internal/maze/portal.go:20`; each doc comment states "computing it from either chunk's own call site yields the identical key" / "identical list"), so `Generate(chB, fabric)` and `Generate(chB, fabric, mapA)` agree whenever `mapA` is the generator's own output for the same seed. Measured, not argued: with `readNeighbors` short-circuited to `return nil, nil` — so the stored map is generated with **no** neighbour — the case that compares it against `Generate(chB, fabric, mapA)` still passes: `--- PASS: TestEnsureChunkAt_EqualsGeneration_BesideNeighbor (0.11s)`. This is the third § Test Design discriminator claim in this document the tree falsifies (design-review rounds 5 and 6 each caught one), and it is exactly the hazard this section's own preamble at line 82 names — "a discriminator that merely *happens* to differ … is worth nothing until the defect has been put in and the case watched". **Design Amendment trigger** — spawn the `design-writer` Subagent to amend `ai-docs/plans/2026-09-17-maze-persistence.design.md` § Test Design subtask 4's AC3 bullet, and the AC4 and AC5 bullets at lines 115 and 142, which describe cases that discriminate nothing either; recipe at `.claude/skills/task/SKILL.md` Step 11 fail-loud table. Do **not** edit the design directly. | ✅ Fixed (design amended, `21116b0`) |
| 2 | internal/world/chunk.go:70 | major | **AC4 and AC5 have no discriminating verifier: the whole neighbour-read mechanism deletes with the suite green.** Spec § Scope 3 and AC4 name the mechanism — a new chunk "takes its shared border with an existing neighbour **from that neighbour's stored map**" — and the design builds `readNeighbors`, the `neighbors []maze.Map` parameter and both call sites around it. Mutation: `readNeighbors` short-circuited to `return nil, nil` (`go build ./...` exit 0, confirmed before reading any test result), so `Generate` receives no neighbour map on any creation path. `go run ./cmd/testpg -- go test -count=1 ./internal/world/` → `ok github.com/maratik123/lab-game/internal/world 0.694s` — the **entire** package green, every AC4/AC5 verifier included: `--- PASS: TestEnsureChunkAt_BorderAgreement` (all 12 direction × order subtests), `--- PASS: TestActivateChat_BorderAgreementOnGatePath`, `--- PASS: TestRace_NeighboringCreations` (all 6 subtests), `--- PASS: TestEnsureChunkAt_EqualsGeneration_BesideNeighbor`. Border agreement is delivered by the generator's symmetric derivation alone, so the read changes nothing observable today — but it is the only thing holding AC4 the day a stored map is not what the current generator would derive, which D4 makes a **designed** state ("a maze holding chunks of several versions is the designed state") and which the `generation_version` column exists for. Fix, in a shape this file already uses at `internal/world/chunk_test.go:145`: create chunk A, flip a border face pair in its stored blob, then create the adjacent chunk B and assert the shared border agrees with A's **stored** map — with the read in place it must; with the read removed B derives the pristine border and the case fails. | ✅ Fixed (`c4b9416`) |

**Minor and nit items — 1, not given a table row** (`internal/world/event.go`): the DOC-4 review-judged half at `internal/world/event.go:31`, `"AppendEvent's no-posting path, never Post's"`. `Post` is an `internal/store` symbol this file never calls, named by bare unqualified name as a comparative pointer (the qualified spelling `store.Post` would have been refused by `make comment-refs`). This is the **second** site round 1's finding 5 named — "same class, milder" — and R1-5's own verifying command reached only the first, so the row closed green while this one stood; it is carried from here on by its own register row, R2-3, with a command that reaches it. Suggested fix: state the contract and drop the pointer — "It moves no balance, so it writes no journal entry." It is not the difference between verdicts: findings 1 and 2 are.

**What was checked.**

*Round-1 closures, each re-verified by running its register command rather than by reading the fix.* **R1-1 CLOSED** — replacing `context.WithTimeout(ctx, m.createBudget)` with `bctx := ctx` in `EnsureChunkAt` (build confirmed first) now gives `chunk_test.go:449: elapsed = 5.004055565s, want under 2s` / `--- FAIL: TestEnsureChunkAt_BudgetIsABound (5.10s)`; neither `errors.Is` assertion fired, which is exactly what the amended design predicts. **R1-2 CLOSED** — `spiralIndex := int64(0)` → `--- FAIL: TestActivateChat_MatchesGateNext … stored spiral_index = …, want 1`; `ring := int64(0)` → `--- FAIL … payload ring = …, want 1`. Both mutants built first; the case's own off-centre guard (`wantSpiral == 0 || wantRing == 0` → `t.Fatalf`) is what makes that 1 a real value rather than a coincidence. **R1-3 CLOSED on the documentation route** — the finding offered two fixes and the second was taken, so its register command no longer matches the shipped choice; recorded as R2-6. `internal/world/errors.go:30` and `internal/world/chunk.go:108` now both state that a caller's own cancellation surfaces as `ErrCreateBudget`, and D5 and § Risks say the same, so code, comments and design agree. **R1-4 CLOSED** — `grep -n "it would hang rather than return" …design.md` → exit 1. **R1-5 CLOSED on the site its command reaches**; its second site is R2-3 above. **R1-7 re-run** — the panic grep over `internal/world/*.go internal/store/catalog.go internal/testdb/server.go` exits 1 against a constructed two-line control that exits 0. **R1-12 re-run** — exit 1, empty.

*Gates, all re-run against the shipped tree at `dd4477c`:* `go vet ./...` (exit 0) · `golangci-lint run` (exit 0) · `golangci-lint fmt -d` (0 bytes) · `make comment-refs` (green) · `make file-limits` (green) · `make import-guard` (green) · `go mod tidy && git diff --exit-code go.mod go.sum` (clean) · `go run ./cmd/testpg -- go test -count=1 ./...` (27 `ok`, 0 `FAIL`) · `go run ./cmd/testpg -- go test -race -count=1 ./internal/world/...` (`ok … 3.299s`, 0 `DATA RACE`).

*Newly introduced since round 1's reviewed tree (`35ba4da..dd4477c`), read in full.* `internal/world/activate_test.go` (the off-centre spiral/ring assertions; the gate-path case's `t.Skip` → `t.Fatalf`), `internal/world/chunk_test.go` (the elapsed-time assertion; the border table widened to six directions), `internal/world/helpers_test.go` (`testBudgetGraceLimit`), and the comment-only edits in `chunk.go`, `depth.go`, `errors.go`. No new exported item, no new package, no new `go` statement, no new panic site, no schema change, no dependency change.

*The gate-path border case is no longer satisfiable by skipping, and its new comment's premises hold.* `go test -count=1 -v -run 'TestActivateChat_BorderAgreementOnGatePath|TestEnsureChunkAt_BorderAgreement' ./internal/world/` → 14 `--- PASS`, `grep -c SKIP` → 0, and the `t.Fatalf` fallthrough is not reached. Its two stated premises were re-derived rather than read: `gate.Next` yields the centre because `Spiral()`'s first element is `hexgrid.Chunk{}` and the only created chunk is `{Q:1,R:0}`; and `{Q:1,R:0}` is `{0,0}`'s `DirE` neighbour by `internal/hexgrid/coord.go:29`. The walk therefore crosses on `DirW`, a **non-canonical** direction — the case the round-1 `borderAgrees` rewrite was made for.

*Design prose added in this range, every claim re-derived rather than read.* `[measured 0f282c1:internal/gate/spiral.go:67-73]` — resolved at that commit, quotes correctly. `[measured b9159c0:internal/gate/next.go:5-16]` and `:16` — both quote correctly. The KD-41 ring sentence `grep -o` reproduces verbatim. The mutation claim "mutant FAILs on the elapsed assertion and on neither `errors.Is`, restored tree PASSes" reproduces exactly. One claim in the section did not survive: finding 1's, and only that one.

*Domain invariants, re-checked over the delta.* The delta writes no balance column, touches no inventory table, opens no basis document, adds no event type, changes no persisted enum or state string, adds no tuning value as a Go literal or constant (`testBudgetGraceLimit` is a test instrument), adds no outbound send path or retry loop, introduces no `time.Now()` / unseeded `math/rand` / map-iteration order on a generation or replay path, and adds no token, DSN or `api_id`/`api_hash`.

*Progress-file required fields present* — `Branch`, `base_commit`, `Last build`, `current_step`, `last_passed_gate`, `## Decisions log`. `parent_skill` is correctly absent (`/task` is the parent flow); `entry_args` present. Content not reviewed, per this agent's own instruction 2.

*Tree state.* Five mutations were applied and reverted; `git status --porcelain` was read and confirmed empty after each, and after the last (`HEAD` = `dd4477c`, no diff).

*Spawn prompt:* within the closed list — invocation line, `Spec:`, `Design:`, `Progress:`, commit range. As in round 1 the range (`3c525e3..HEAD`) is wider than the header's `base_commit` (`bbf7617`); reviewed over the prompt's range, a superset adding only spec, design and interview-state commits.

## Self-Review (Round 3)

**Verdict:** REJECT

| # | File:line | Severity | Finding | Status |
|---|-----------|----------|---------|--------|
| 1 | internal/world/activate.go:122 | major | **AC4's neighbour-read mechanism has no discriminating verifier on the gate creation path — the second of its two call sites.** Spec § Scope 3 and AC4 name the mechanism: a new chunk "takes its shared border with an existing neighbour **from that neighbour's stored map**". `ActivateChat` does **not** run through `createLocked`; its closure carries its own copy of the read-then-generate sequence (`internal/world/activate.go:122` against `internal/world/chunk.go:218`), so the two sites drift independently. Mutation confined to the gate site — `neighbors, err := m.readNeighbors(bctx, tx, chosen)` and its error check replaced by `var neighbors []maze.Map` (`go build ./...` exit 0, confirmed before reading any test result): `go run ./cmd/testpg -- go test -count=1 ./internal/world/` → `ok github.com/maratik123/lab-game/internal/world 0.728s` — the **whole** package green, `TestActivateChat_BorderAgreementOnGatePath` included. Round 2's finding 2 was this defect over both sites; `c4b9416` closed the explorer site and left this one. (The explorer site is genuinely closed — re-running R2-2's own command, `readNeighbors` short-circuited to `return nil, nil`, gives a single `--- FAIL: TestEnsureChunkAt_NeighborReadFeedsGeneration` / `chunk_test.go:222: border disagreement at {6 -6}/{-6 0}: 0 vs 1`.) This is the same fix-one-site-of-two shape R1-5 produced in this run, which round 2 had to raise again as R2-3. Consequence: on the mixed-version day D4 calls "the designed state", a gate chunk created beside a stored chunk whose map is not what today's generator derives takes the pristine border and disagrees with its neighbour — AC4 violated, with nothing to catch the regression. Fix: the sibling of `TestEnsureChunkAt_NeighborReadFeedsGeneration` on the gate path — create a fabric chunk, flip a border face in its **stored** blob, activate a chat, and assert the shared border agrees with the mutated stored map; `TestActivateChat_BorderAgreementOnGatePath` already forces the adjacency at seed 104, so the fixture is in hand. **Consequential Design Amendment:** § Test Design subtask 5's bullet at `ai-docs/plans/2026-09-17-maze-persistence.design.md:130` currently reads "Like its subtask-4 sibling it pins agreement, not the neighbour read … and the read has its own case under subtask 4"; once that case exists, spawn the `design-writer` Subagent to amend the bullet. Do **not** edit the design directly. | ✅ Fixed (`ee7593d`, then `49a3792`) |

**Minor and nit items — 2, not given a table row** (`ai-docs/harness-gaps.md`, `internal/world/chunk_test.go`). First, the new harness-gaps entry uses `ai-docs/learnings.md`'s skeleton (`**What happened:**` / `**Rule:**` / `**Kind:**` / `**Escalated?**`) rather than the one `ai-docs/harness-gaps.md`'s own header declares (`**target:**` / `**Observed:**` / `**Gap:**` / `**Proposed edit:**` / `**at:**`), and carries no `**at:**`; it is 1 of 80 entries in that shape, the other 79 carrying `**Observed:**`. Not gated — `check-harness-gaps-forge.sh` and `check-harness-gaps-targets.sh` both exit 0 — and `AGENTS.md` § *Learning Log* does say "same skeleton plus a required `target:` field", which is a defensible reading of the file's own header, so this is a harness ambiguity rather than a clear violation. Second, `findBorderLocal` (`internal/world/chunk_test.go:228`, added this round) re-implements the `At`/`Locate` crossing walk `borderAgrees` already performs in `helpers_test.go`; test-local duplication of six lines. Neither is the difference between verdicts: finding 1 is.

**What was checked.**

*Round-2 closures, each re-verified by running its register command rather than by reading the fix.* **R2-2 CLOSED** — `readNeighbors` short-circuited to `return nil, nil` (build confirmed first) → `GATE-RED`, a single `--- FAIL: TestEnsureChunkAt_NeighborReadFeedsGeneration`, `chunk_test.go:222: border disagreement at {6 -6}/{-6 0}: 0 vs 1`; tree restored, `git status --porcelain` empty. The residue at the *other* call site is finding 1. **R2-1 CLOSED** — `grep -n "would simply be false there" …design.md` → exit 1. **R2-3 CLOSED** — `grep -n "never Post's" internal/world/event.go` → exit 1; the comment now reads "It moves no balance, so it appends the bare event with no accompanying posting", which is true of the shipped call (`store.AppendEvent` alone, no posting). **R2-6 re-read, not re-run**, per its own row: D5 states the sentinel means "this call's context ended", and `wrapBudget` (`internal/world/chunk.go:113-118`) fires on any non-nil `ctx.Err()`, so code, both comments and the design agree. **R1-4, R1-5, R1-12 re-run** — exit 1, all empty.

*Gates, all re-run against the shipped tree at `8f171b7`:* `go vet ./...` (exit 0) · `golangci-lint run` (`0 issues.`, exit 0) · `golangci-lint fmt -d` (empty) · `make comment-refs` (exit 0) · `make file-limits` (exit 0) · `make import-guard` (exit 0) · `go mod tidy && git diff --exit-code go.mod go.sum` (clean) · `go run ./cmd/testpg -- go test -count=1 ./...` (27 `ok`, 0 `FAIL`, 0 `SKIP`) · `go run ./cmd/testpg -- go test -race -count=1 ./internal/world/... ./internal/store/...` (`ok` both, 0 `DATA RACE`) · `make cover-ratchet` (`91.54% holds against 91.95%`, tolerance 0.60 pp) · `check-review-register.sh` on this file (exit 0) · `check-harness-gaps-forge.sh`, `check-harness-gaps-targets.sh` (exit 0).

*Newly introduced since round 2's reviewed tree (`dd4477c..8f171b7`), read in full.* `internal/world/chunk_test.go` (+77: `TestEnsureChunkAt_NeighborReadFeedsGeneration` and `findBorderLocal`), `internal/world/event.go` (comment only), `ai-docs/plans/…design.md` (§ D4, § D7, § Test Design), `ai-docs/harness-gaps.md` (one entry), the progress file. No new exported item, no new package, no new `go` statement, no new panic site, no schema change, no migration, no dependency change, no new event type, no `//nolint`.

*The new case reaches the clause it names* (Pattern 2). It is not a "reports nothing" assertion but a positive one, and the mutation above is its own proof: the fixture flips a border face in A's **stored** blob, so a fresh derivation and the stored map disagree by construction, and `borderAgrees` walks every shared face including the flipped one — the failure line names the mutated pair. Unmutated it passes in 0.16 s; with the mechanism removed it is the only case in the package that fails.

*Design conformance and prose claims re-derived rather than read* (Pattern 1 — the delta is mostly prose). The design carries no `AC<N> verified by:` line (`grep -n "verified by:"` → exit 1), so there is no AC-verification grep set, as in rounds 1 and 2. The three tags added in this range: `[measured c4b9416:internal/maze/seed.go:112-119]` resolves and quotes `borderKey`'s doc verbatim; `[measured c4b9416:internal/maze/portal.go:12-20]` resolves and quotes `borderCandidates`'s doc verbatim; `internal/maze` is unchanged between `c4b9416` and HEAD, so both pins still describe the tree. The mutation tag — "a single `--- FAIL: TestEnsureChunkAt_NeighborReadFeedsGeneration`, whose line reads `border disagreement at {6 -6}/{-6 0}: 0 vs 1`; restored tree → `ok`" — **reproduced verbatim**, including the coordinates. § Test Design subtask 5's amended D8 clause ("no case here pins" the before-the-lock refusal order) is accurate: `TestActivateChat_NotAChat` (`activate_test.go:214-233`) asserts `ErrNotAChat` and `count(*) FROM chunk = 0`, neither of which distinguishes a refusal before the lock from one after it. The subtask-4 refusal clause's new half is sound: `lockMazeSQL` yields `ErrMazeMissing` via `pgx.ErrNoRows` (`chunk.go:140-143`), which a foreign-key violation from the insert would not.

*GO notes.* G13 (round 8, `folded`) verified present by reading the region: § Test Design's preamble now opens "This hazard has recurred in this package" with no tally, and names three instances rather than counting them. Its resolution cell also claims "the sweep took five more stored counts of the document's own history" — five is exactly what the diff carries (D7's "two reasons that coincide", the contract-silence bullet's "two reasons that were run", subtask 4's "in two sub-cases", § Open questions' "Round 1's first open question", and the preamble's own "twice"). Every other row was verified in round 1.

*The harness-gaps entry's central claim was run, not read.* "`check-review-register.sh` refuses it: the gate joins round 1's own `✅ Fixed` table row to the same id" — constructed a three-table fixture with `R1-5` at `open 🔁@2`, a round-1 row marked `✅ Fixed`, and a round-2 row leading with `R1-5`: exit 1, `R1-5 reads "open 🔁@2" while round 1 marks finding 5 Fixed`. Exactly as the entry states. The `learnings.md` entry added in this range was likewise re-derived: `(*Registry).Check` returns `ErrNoSignature` immediately for an undeclared type (`internal/contract/check.go:241-242`), `declarations` holds `{Type: ManualCorrection(), Signature: AnyBalanced()}` alone (`internal/contract/declared.go:8-10`), and `ErrNoDocument` is the empty-range return (`check.go:277-278`) — all three as written.

*Domain invariants, re-checked over the delta.* The delta writes no balance column and touches no inventory table — the one `UPDATE` it adds is `UPDATE chunk SET faces` from a test fixture, and `chunk` is neither, with the established precedent at `chunk_test.go:145`. No basis document is opened, no posting signature is owed, no event type is added or changed, no persisted enum or state string moves, no tuning value arrives as a Go literal or named constant (seed `1600` is a test fixture), no outbound send path or retry loop appears, no `time.Now()` / unseeded `math/rand` / map-iteration order reaches a generation or replay path, and no token, DSN or `api_id`/`api_hash` is added.

*Documentation.* Both new comments start with their identifier and state contracts rather than narrating. `make comment-refs` is green, and the review-judged halves hold: neither carries a bare unqualified outward pointer, and the DOC-5 behavioural claim in `TestEnsureChunkAt_NeighborReadFeedsGeneration`'s comment — "which is only possible if B's creation actually read A's stored map rather than deriving the border fresh" — is the one I mutated and confirmed. `event.go`'s replacement clause drops the `Post` pointer that R2-3 named.

*Objection quality.* No `⚠️ Objected` row exists in any round table; nothing to evaluate under checklist § 7.

*Progress-file required fields present* — `Branch`, `base_commit`, `Last build`, `Issue`, `Spec`, `current_step`, `last_passed_gate`, `entry_args`, `## Decisions log`. `parent_skill` correctly absent (`/task` is the parent flow). Content not reviewed, per this agent's own instruction 2.

*Tree state.* Two mutations applied and reverted (`chunk.go`'s `readNeighbors`, `activate.go`'s gate-path call), each restored from a `tmp/*.bak` copy rather than by `git restore`; `git status --porcelain` read and confirmed empty after each and at the end, `HEAD` = `8f171b7`.

*Spawn prompt:* within the closed list — invocation line, `Spec:`, `Design:`, `Progress:`, commit range. As in rounds 1 and 2 the range (`3c525e3..HEAD`) is wider than the header's `base_commit` (`bbf7617`); reviewed over the prompt's range, a superset adding only the spec, design and interview-state commits.

## Self-Review (Round 4)

**Verdict:** REJECT

| # | File:line | Severity | Finding | Status |
|---|-----------|----------|---------|--------|
| 1 | ai-docs/context-status.md:571 | major | **The Step-9.5 entry states a trap the shipped tree falsifies, on both of its halves, and neither correction propagated here.** The bullet reads: "**A conditional test case can reach green by skipping.** The border-agreement case on the gate path executes only when the spiral puts the gate adjacent; the fixture's seed is what keeps it a test rather than an instrument." Both clauses were true at `35ba4da`, where the entry was written; both were falsified later in this same PR. **(a) It no longer skips.** `59b076f` replaced the `t.Skip` with a `t.Fatalf` naming the fixture — measured at `1b2cac2`, `grep -n "t\.Skip" internal/world/activate_test.go` → exit 1, empty, against a constructed control (`printf 't.Skip("x")\n'` piped to `grep -c "t\.Skip"` → 1); the same grep at `35ba4da` reads `227:	t.Skip("fixture's gate did not land adjacent to the fabric chunk; no border to walk")`. **(b) The seed holds nothing honest.** `grep -n "^func Next" internal/gate/next.go` → `16:func Next(created, gates []hexgrid.Chunk, k int) (hexgrid.Chunk, error) {` — placement takes no seed, and this design records exactly that correction in § Test Design subtask 5: "**The world seed is not what holds this case honest, and an earlier draft of this bullet said it was:** … tuning `Spec.Seed` moves the faces and never the adjacency — a later reader told otherwise would tune it and change nothing." `ai-docs/context-status.md` **is** that later reader's page: its own header calls it the log of "traps … worth not rediscovering", "read on demand when touching the area an entry covers". An AGENTS.md § *Propagation Rule* miss (step 4 — every LIVE doc must agree), and the third time in this run a repair landed at one surface and left its twin standing (R1-5 → R2-3, R2-2 → R3-1, now this). **Not a Design or Spec Amendment trigger** — the design is already correct and the spec is silent; the fix is an edit to *this PR's own, still-unmerged* entry (`git log --oneline 3c525e3..HEAD -- ai-docs/context-status.md` → the single commit `35ba4da`), which is finishing an entry for a task that has not completed rather than rewriting a past task's, so the file's append-only header does not bar it. Fix: state the trap that was actually found — a conditional case can reach green by skipping, which is why this one now asserts adjacency and fails naming the fixture — and drop the seed clause or replace it with what does force the adjacency (the ring-1 fabric chunk is the only created chunk, so placement yields the centre). | ✅ Fixed (`122d7bd`) |
| 2 | internal/world/activate_test.go:276 | minor | **DOC-4's review-judged half: a bare unqualified name used as a pointer, in a comment this round adds.** `TestActivateChat_NeighborReadFeedsGeneration`'s doc comment reads "(the fixture's first activation lands on the centre, which TestActivateChat_BorderAgreementOnGatePath already forces into adjacency with a ring-1 chunk)". That is the "see such-and-such" form `ai-docs/doc-convention.md` § DOC-4 names as "banned, and … refused in review rather than by the gate"; the exemption beside it keeps only "a same-package symbol **that is the contract**", and a sibling test is not this test's contract. It is also wrong about what it points at: this case's adjacency is forced by **its own** fixture (`internal/world/activate_test.go:289-295`, the ring-1 `EnsureChunkAt` plus the in-body comment that states the same fact in full), not by the sibling's. So the parenthetical is the "sentence [that] exists only to carry the reference", which DOC-4 says goes with it. Third instance of this class in this run (R1-5, R2-3). Fix: delete the parenthetical — the in-body comment already carries the fact. | ✅ Fixed (`122d7bd`) |

**What was checked.**

*Round-3 closure, re-verified by running its register command rather than by reading the fix — and re-resolved first, because the command no longer resolved.* **R3-1 CLOSED.** Its recorded command names `neighbors, err := m.readNeighbors(bctx, tx, chosen)` in `ActivateChat`, which `49a3792` deleted by moving the read into the shared tail; the orchestrator recorded the re-resolution in the Decisions log but not in the register row, so I re-resolved it into the row itself (§ *locator drift* — a verifier-side re-resolution, no amendment, no round). Re-resolved and run: `neighbors = nil` inserted after `generateInsertAndAppend`'s own read (`internal/world/chunk.go:176-179`), `go build ./...` exit 0 confirmed as a separate step, then `go run ./cmd/testpg -- go test -count=1 -v ./internal/world/` → **RED on exactly the two cases and no others** — `--- FAIL: TestEnsureChunkAt_NeighborReadFeedsGeneration` (`chunk_test.go:222: border disagreement at {6 -6}/{-6 0}: 0 vs 1`) and `--- FAIL: TestActivateChat_NeighborReadFeedsGeneration` (`activate_test.go:354: border disagreement at {-6 0}/{6 -6}: 1 vs 0`), 41 `--- PASS`. That is D5's own `49a3792` tag reproduced verbatim, coordinates included, and it is what makes the refactor load-bearing rather than cosmetic: one change now fails both paths.

*Gates, all re-run against the shipped tree at `1b2cac2`.* `go vet ./...` (exit 0) · `golangci-lint run` (exit 0) · `golangci-lint fmt -d` (0 bytes) · `make comment-refs` (exit 0) · `make file-limits` (exit 0) · `make import-guard` (exit 0) · `go mod tidy && git diff --exit-code go.mod go.sum` (clean) · `make cover-ratchet` (`91.56% holds against 91.95%`, tolerance 0.60 pp) · `go run ./cmd/testpg -- go test -race -count=1 ./internal/world/ ./internal/store/` (`ok` both, 0 `DATA RACE`) · `check-review-register.sh` on this file (exit 0) · `check-spec-shape.sh` and `check-ac-shape.sh` on the spec (exit 0).

*The whole-module suite is NOT green on this host, and the reason is the instrument, not the diff — recorded rather than waved through* (R4-3). `go run ./cmd/testpg -- go test -count=1 ./...` was run twice: 26 `ok` and `FAIL github.com/maratik123/lab-game/internal/testdb` both times, no other package red, `internal/world` `ok` in both. Every failure message is a podman HTTP deadline, never an assertion — `--- FAIL: TestContainerExists_followsAServersLifetime` at `container_test.go:92: stopping the server: … Post ".../containers/…/stop?t=10": context deadline exceeded`, and `--- FAIL: TestStartServer_PGDATAMountHoldsEveryClientCountProvisioned` at `server_test.go:393` with the identical message. Run alone the package went `ok … 469.925s` once and, on a second isolated run, reported `--- PASS` for **both** named tests and failed only in `leaktest` after `testdb: terminating container: … context deadline exceeded` left a `testcontainers-go.(*Reaper).connect` goroutine alive. The first `make cover-ratchet` was blocked by the same package and passed on a re-run with a long-lived server up. The diff's only `internal/testdb` lines are `Binaries` 7→8 and the `TestCeiling_formula` literals; neither failing test reads `Binaries`, and neither touches container lifecycle. Attribution to the host rather than to the change is therefore evidence-backed, but this reviewer could not observe one whole-module green run, and that is a fact about this round, not a claim about CI.

*Mutations run, each confirmed to BUILD first, each reverted from a `tmp/*.bak` copy rather than by `git restore`, with `git status --porcelain` read empty after each.* **(1)** shared-tail neighbour read defeated → RED on both neighbour-read cases and nothing else (above). **(2)** `readChunk`'s `decode(ch, typ, version, …)` changed to pass `maze.Version` → `--- FAIL: TestEnsureChunkAt_ReadUsesStoredRowNotConstants`, `chunk_test.go:607: version = 1, want 2`, **and no other case in the package**, codec cases included — the design's `a78c549` tag reproduced exactly. **(3)** the same call changed to pass `ChunkTypeFabric` → `--- FAIL: TestEnsureChunkAt_ReadUsesStoredRowNotConstants`, `chunk_test.go:607: type = fabric, want gate`, alone. The new case therefore discriminates the version hand-off and the type hand-off independently, which is the class its doc comment claims and which the codec cases (in memory, no column) cannot reach. Baseline before and after: `ok github.com/maratik123/lab-game/internal/world 1.077s`.

*Newly introduced since round 3's reviewed tree (`8f171b7..HEAD`), read in full.* `internal/world/chunk.go` (the read moved into `generateInsertAndAppend`, the `neighbors` parameter dropped, both doc comments rewritten), `internal/world/activate.go` (the call-site deletion), `internal/world/activate_test.go` (+85, `TestActivateChat_NeighborReadFeedsGeneration`), `internal/world/chunk_test.go` (+80, `TestEnsureChunkAt_ReadUsesStoredRowNotConstants`), plus prose in the design, `ai-docs/domain-invariants.md`, `ai-docs/key-decisions.md` and this file. No new exported item, no new package, no new `go` statement, no new panic site, no `//nolint`, no schema change, no migration, no dependency change, no new event type, no new balance path. The refactor preserves the statement order D5 fixes on both paths — explorer: lock, re-read chunk, read neighbours, generate, insert, append; gate: lock, re-read gate, read created, read gates, `Next`, read neighbours, generate, insert, append — and the error wrapping is `wrapBudget(ctx, …)` in both, as before.

*Prose claims re-derived rather than read (Pattern 1 — the delta is predominantly prose).* D5's `a78c549` tag: `grep -rn "readNeighbors" --include=*.go internal/` → three hits, the doc line, the definition and one invocation, none in `activate.go` (R4-6 records the one loose clause). D5's `68f983b:internal/world/chunk.go:120-126` Read tag quotes `lockedTxBody`'s comment verbatim. D6's `a78c549` `UPDATE chunk` tag: four hits, all in `_test.go` (`chunk_test.go:145,207,592`, `activate_test.go:330`), none in non-test source — the pattern matches real occurrences, which is its own control. § Open questions' `a78c549` `DELETE FROM maze` tag: one hit, `chunk_test.go:457`, test source. The design carries no `AC<N> verified by:` line (`grep -n "verified by" …design.md` → exit 1), as in rounds 1–3. **One claim in this range did not survive: finding 1's, and it is not in the design — it is in `ai-docs/context-status.md`, the one live page this round's prose sweep reached that nobody had re-checked since `35ba4da`.** The rest of that entry was swept claim by claim: "Nothing imports it yet" holds (`grep -rn "lab-game/internal/world" --include=*.go .` → zero hits outside the package, against a control of 8 for `internal/gate`); the depth, lock-rule, budget-scope, season and foreign-key decisions, the other five traps and all four invariants each re-derive against the tree.

*GO notes.* The two round-11 rows are new in this range and were verified by reading the region, not by pattern match. **G14** — the stored-row bullet now sits under § Test Design **subtask 5** (the block beginning at design line 123), names its own file (`internal/world/chunk_test.go`) and states the rule once ("a case belongs to the subtask that can first compile it"). **G15** — D5 now names three layers (shared skeleton, per-path body, shared tail) and the sweep reached D6 ("only a miss opens D5's shared skeleton"), D8 (step 3) and both decomposition cells (subtasks 4 and 5). Every earlier row was verified in round 1; all 15 are routed `folded`, so no `owner (3) left` row is outstanding and the spec stands as written.

*Domain invariants, re-checked over the delta.* No balance column is written and no inventory table is touched — the two `UPDATE chunk` statements this range adds are test fixtures, and `chunk` is neither (R3-4's precedent). No basis document is opened, no posting signature is owed, no event type is added or changed, no persisted enum or state string moves, no tuning value arrives as a Go literal or named constant (seeds 1601 and 1700 are test fixtures), no outbound send path or retry loop appears, no `time.Now()` / unseeded `math/rand` / map-iteration order reaches a generation or replay path, and no token, DSN or `api_id`/`api_hash` is added. The panic grep over the changed non-test files (`internal/world/*.go`) exits 1 against a constructed control that exits 0, so no `ai-docs/panic-index.md` row is owed.

*Documentation.* Both rewritten doc comments open with their identifier and read in the third person. `make comment-refs` is green, so the lexical half of DOC-4 holds; of the two review-judged halves, the narration half is clean (R4-5 records `createLocked`'s grown comment as rationale, not narration) and the bare-pointer half is finding 2. DOC-5: `generateInsertAndAppend`'s new claim — "Both creation paths route their neighbour read through here, so omitting it would take a change to this function rather than to a call site" — is the one I mutated, and it holds; the same-class sweep over every comment added in this range found the loose scoping recorded as R4-7 and nothing false.

*Objection quality.* No `⚠️ Objected` row exists in any round table; nothing to evaluate under checklist § 7.

*Progress-file required fields present* — `Branch`, `base_commit`, `Last build`, `Issue`, `Spec`, `current_step`, `last_passed_gate`, `entry_args`, `## Decisions log`. `parent_skill` correctly absent (`/task` is the parent flow). Content not reviewed, per this agent's own instruction 2.

*Tree state.* Three mutations applied and reverted; `git status --porcelain` read empty after each and at the end, `HEAD` = `1b2cac2`. A long-lived test server was brought up with `make test-db-up` for the mutation runs (the host's podman was taking minutes per anonymous container) and removed with `make test-db-down` — `testpg: shared server down` — before this section was written.

*Spawn prompt:* within the closed list — invocation line, `Spec:`, `Design:`, `Progress:`, commit range, nothing else. As in rounds 1–3 the range (`3c525e3..HEAD`) is wider than the header's `base_commit` (`bbf7617`); reviewed over the prompt's range, a superset adding only the spec, design and interview-state commits. `git diff --stat 3c525e3..HEAD` is non-empty (39 files), so the empty-window refusal does not apply.
