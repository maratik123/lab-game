# Progress: World generation core — hex topology and the deterministic chunk generator — ACTIVE
_Updated: 2026-09-13 04:10_

> Read THIS FIRST → ready to continue. No need to re-read the codebase.

**Branch:** feat/2026-09-12-world-generation-hex-chunk-generator
**base_commit:** 4315aef
**Last build:** PASS

**Issue:** #27
**Spec:** ai-docs/plans/2026-09-12-world-generation-hex-chunk-generator.spec.md

**current_step:** Step 7 — design-review round 8 on the amended design; then Group D (15-16), Step 9, self-review round 5
**last_passed_gate:** `go build` + `go vet` + `golangci-lint fmt -d` + `golangci-lint run` + `go test -count=1 ./...` + `go test -race` + `make comment-refs` + `make import-guard` | 2026-09-13T02:20Z | 3146b55
**entry_args:** 27

## Next action

**Do this immediately:** every subtask of all three groups is complete and self-review round 3 closed. The next action is self-review round 4 (the owner raised the cap to 4), then Step 12. The binding per-subtask specification remains `ai-docs/plans/2026-09-12-world-generation-hex-chunk-generator.design.md` § Decomposition; the `## Subtasks` rows below are one-line summaries for orientation and are **not** the contract. Two standing facts a re-entering agent needs: `origin/main` is merged in, so the review range is `c07f8c1..HEAD` and not `base_commit..HEAD`, and the register ids are the canonical `R<round>-<finding>` after round 3 re-labelled a global sequence the gate could not parse.

## Subtasks

- [x] 1. `internal/detguard` — the shared determinism predicates, each paired with a scratch red case carrying the blind shape
- [x] 2. `internal/hexgrid` — axial coordinate, six directions, `Opposite`, canonical `Face`/`FaceOf`, `Dims`, `ChunkOf`, `ChunkDistance`
- [x] 3. `internal/maze` derivation core — fixed-width preimage helpers with their G115 suppressions, the domain-tagged keys, the ChaCha8 stream behind an unexported one-method interface
- [x] 4. `Params` and its validation — decimal shares, bias, the enum-indexed weight array, the rounding
- [x] 5. The chunk cell graph — index mapping, six-neighbour adjacency, border-cell predicate, interior-face enumeration
- [x] 6. The five algorithms over the non-island induced subgraph — backtracker, Kruskal, frontier Prim, growing tree, Wilson
- [x] 7. The extra-passage pass and border-portal selection, with the canonical-lesser-chunk candidate ordering
- [x] 8. `Generator`, `New`, `Cell`, the chunks-consulted function, the `PrefabClaimer` boundary
- [x] 9. The property suite, the cell golden with its per-algorithm sections, this package's `guards_test.go`
- [x] 10. The benchmarks — one cell, and one per algorithm under a single-weight input
- [x] 11. **Re-opened by the amendment** — remove the architecture gate and revert the propagation its CI job required, across the nine paths the design enumerates
- [x] 12. Close the open question in the design corpus and record the engineering decisions
- [x] 13. **Added by the amendment** — KD-37 edited in place (never reverted: it postdates the gate commit), striking the clause the removal falsifies and re-anchoring the encoding rationale
- [x] 14. **Added by the amendment** — strike the "and CPU architectures" clause from `internal/maze/doc.go`, the last durable surface still asserting the struck axis. Code change-type, so it is its own Group C

## Decisions log

Append-only, one line per non-trivial decision. Each line is prefixed with the step or phase that made it. Never edit or remove prior entries.

- **Step 7**: design-review rounds 1, 2 and 3 all returned ITERATE, each finding at least one major that would have reached code; the round cap of 3 was exhausted without a GO.
- **Step 7**: the owner raised the round cap to an explicit 4 — `cap: 4 (was 3)`, their words "Raise cap to 4" — after being shown that the round-3 major was verified independently; the orchestrator did not raise it and offered the per-instance re-review exemption as a separate option, which the owner did not take.
- **Step 7**: design-review round 4 returned GO with five minors and four recommendations; all nine were classified `design-internal` against the four triggers of the AXIOM *the orchestrator originates no spec row* — none matched, so no owner question was raised, `design-writer` folded all nine, and design-review did not run again.
- **Step 7**: two facts were disputed between the reviewer and the designer (whether `internal/srcguard` may host lifted predicates; where `exhaustive` sits in `.golangci.yml`); the orchestrator resolved both from source rather than choosing a delegate, and both went to the designer — `srcguard`'s own package comment reserves predicates to the owning package, and `exhaustive` is at lines 20 and 44-45 as the designer pinned it.
- **Step 8**: progress file created at `base_commit` 4315aef with `go build ./...` green; no code exists yet, so `Last build: PASS` describes the pre-existing tree.
- **Step 8 (subtask 1)**: `internal/detguard` implements the five bans (clock/rand-v1/hash-maphash/crypto-rand/math imports, unpinned `math/rand/v2` identifiers, floating-point types, `math` import, decimal float accessors) and a best-effort map-range detector via a file-local assignment scan (no `go/types` dependency added). Each predicate has a scratch red case; the float-accessor case carries the blind shape (method-result only, no bare declaration).
- **Step 8 (subtask 2)**: `internal/hexgrid` — `Direction` is `int8`, canonical order `DirE,DirNE,DirNW,DirW,DirSW,DirSE`; `Opposite` pairs (E,W),(NE,SW),(NW,SE); `FaceOf` canonicalises to the earlier direction of the opposite pair. `ChunkOf` floor-divides; `ChunkDistance` uses the standard axial hex-distance formula widened to `int64` before subtracting.
- **Step 8 (subtask 3)**: `internal/maze` derivation core — `worldKey(seed)` folds the domain tag `"lab-game/maze/v1"` and the seed once; `cellKey`/`chunkKey`/`borderKey` each fold `worldKey` with their own purpose byte (`'c','i','a','s','x','b'`) so every stream/value is domain-separated by construction. `borderKey` canonicalises its chunk pair by (Q,R) before folding. `stream` is an unexported one-method interface; `newStream` is the sole `math/rand/v2` reference. `boundedDraw` is total (bound ≤1 returns 0, untouched) and uses reject-above-limit sampling, never a bare modulo. `testdata/derive.golden` pins the raw preimage encoding table plus the derived keys/cell seeds/border-key symmetry; minted via `-update` and read back before commit.
- **Step 8 (subtask 5 design note, decided during subtask 4)**: `nonBorderCellCount(d) = max(0,Cols-2)*max(0,Rows-2)` — derived directly from a cell's six neighbour deltas: all six neighbours of local coordinate (lq,lr) stay inside the chunk iff lq∈[1,Cols-2] and lr∈[1,Rows-2]. Lives in `params.go` (needed by `Params.validate`, ahead of subtask 5's own chunk-graph file) and will be reused, not re-derived, by `chunkgraph.go`.
- **Step 8 (subtask 9, gap fix round 2 — instrument was blind)**: the orchestrator mutation-tested the gap-fix test itself and measured it green against a live mutant (`Generator` given `memoOpen edgeSet`/`memoSet bool` fields, `buildOwnFabric` reusing the first chunk's `open` for every later chunk) — the exact failure class the design's own sentence names ("also what would catch a memo added later"), reported with the three-cell control matrix (mutant+shared-gen: green/blind; mutant+two-Generators: RED; no-mutant+two-Generators: green). Root cause: the sorted and shuffled passes shared one `*Generator`, so the sorted pass warmed any cross-call memo and the shuffled pass read the same warm state — any state outlasting one `Cell` call was invisible to the test by construction. Fix: give each pass its own freshly-constructed `Generator` over the same seed/params, so no state can outlive a single `Cell` call between the two passes. Verified in this same turn, with cp-backups (never `git checkout`/`restore`) restored afterward: the corrected test goes RED (`exit 1`, naming the diverging coordinate) with the mutant injected and green with the mutant removed — the anti-vacuity setup assertion from round 1 is unchanged.
- **Step 8 (subtask 9, gap fix round 1)**: the orchestrator caught that subtask 9's return was missing one of the three named order-independence scenarios in design § Test Design → *Determinism* — "evaluating the coordinate table in an order shuffled by a test-local fixed seed yields the same values as evaluating it in sorted order" (design line 738) — after searching several encodings and finding no match outside the production draw helper. Added `TestCell_ShuffledEvaluationOrderMatchesSortedOrder` to `generate_test.go`, reusing the package's own `shuffle`/`newStream` rather than a fresh import, with a setup assertion that the shuffle actually produced a different order (so the test cannot pass vacuously). Re-derived the `AC1`/`AC2` progress-table rows from what that same paragraph states the golden does and does not discharge, rather than from the presence/absence of a dedicated test per clause: AC1 is now TESTED in full (all three order-independence shapes plus the golden's own separate-process discharge); AC2 stays PARTIAL, with the toolchain axis TESTED via the golden and the architecture axis correctly attributed to Group B subtask 11 rather than to any Group A gap, and endianness named as the design's own accepted argued residue, not a test to write.
- **Step 8 (subtask 10)**: `bench_test.go` — `BenchmarkCell` (equal-weight reference Params) and `BenchmarkCellPerAlgorithm` (one sub-benchmark per algorithm, single-weight Params each). No threshold asserted; measured once locally at `-benchtime=1x` to confirm compilation and a sane order of magnitude (~0.7-0.9ms/cell on this host) — the real numbers belong in the PR body per the design, not in a tracked comment. Group A (subtasks 1-10) is now complete; ready for `/context-reset` handoff into Group B (subtasks 11-12, harness change-type, orchestrator tier).
- **Step 8 (subtask 11)**: `make test-arch` rebuilds and re-runs **whole packages**, not a `-run` filter over the named determinism tests — a pattern that matches nothing reports the clean answer, and the block's test names are exactly the ones that move. Both word sizes are **measured** off the ELF class byte of built binaries: the cross side from the very binary that is then executed, the host side from the reference package rebuilt at `GOHOSTARCH` (so an ambient `GOARCH` in the caller's environment cannot move the reference), which keeps the assertion free of any architecture-name-to-width table. The three outcomes are separated by the **exit status alone** (`126`/`127` = could not exec): the shell's own diagnostic is localised — measured here as a Russian "Ошибка формата выполняемого файла" — so no gate may parse it. All four paths were watched: `386` agreed (both packages, 32-bit against a 64-bit host); `ARCH_GOARCH=arm` (32-bit, unexecutable here) SKIPped locally and REFUSED under `CI=true`; `ARCH_GOARCH=amd64` refused as same-width. The **discriminating** probe for the disagreed path was a test injected under a cp-backup that fails only when the word size is 32 — green under the host `go test ./internal/hexgrid/`, RED under `make test-arch` with the failure line naming word size 32, which is what proves the gate executes a genuinely 32-bit binary rather than reporting on a host rebuild; backup restored and `git status` re-verified clean afterwards.
- **Step 8 (subtask 11, CI wiring)**: added as a **fifth sibling job** (`test-arch`, named Architecture) rather than a second step of the existing Test job, which is a stated deviation from the design's "invoked from CI's existing Go test job": that job's own cluster comment fixes "one make target apiece" and forbids sequencing, and a second step there would have let a red `make test` hide the determinism gate entirely — the green-instrument failure the design argues against. Gating is identical to its four siblings. Propagated in the same change per the propagation group for an added CI job: `AGENTS.md` (the gate command list and the CI enumeration), `ai-docs/claude-tools-hierarchy.md` (a CI row, and the Race row's "one of the four" → five), `ai-docs/go-test-conventions.md` (the no-filter gate list), and the CI group's failure-class taxonomy — a new `arch` class with its own reproducer in both CI skills plus the class enumerations and verdict rows in the Dependabot reference. The new class is not folded into `test` because `test`'s reproducer (`go test ./...`) cannot reproduce an architecture finding and its NO-REPRODUCE verdict routes to a rebase comment, which is the wrong action for a determinism disagreement.
- **Step 8 (subtask 11, paths filter)**: `'**/*.golden'` added to the `go` paths filter. The goldens are new on this branch (`git ls-tree 4315aef` carries none under `internal/maze`), and the filter block's own rule is that a new Go artefact class is added in the PR that introduces it — without the entry a commit editing only `cells.golden` reaches no Go job, the Architecture job included, which is precisely the comparison that gate exists for. **Observed and deliberately NOT fixed here** (pre-existing, outside this subtask's charter): `internal/health/testdata/**/*.fixture` is matched by no filter key either, and has not been since it landed — a triage item, not a silent widening.
- **Step 8 (subtask 11, unmeasured half)**: whether a GitHub-hosted `ubuntu-latest` runner can execute a 32-bit binary is the one half no local run can settle, and the design named it as such. The direction is written down rather than assumed — the gate REFUSES there rather than skipping — so the first CI run on this branch either passes or says so loudly; read the Architecture job on that run before reading anything else.
- **Step 8 (Group B close)**: `make verify` — now carrying `test-arch` — is green end to end on the landed tree. And the gate's own subject was checked rather than assumed: running the 32-bit `internal/maze` test binary with `-test.v` reports `--- PASS` for `TestDerive_Golden`, `TestCellsGolden`, `TestCellsGolden_EveryIslandCellHasAllSixFacesAsWallInTheMintedTable`, the face-agreement sweep and both order-independence cases, with **no** `--- SKIP` among them — so the determinism block the design names is executing under the second architecture, not passing by absence.
- **Step 8 (subtask 12)**: `docs/DESIGN.md` §16.2 item 2 now strikes the intra-chunk algorithm choice explicitly (`~~выбор конкретного maze-алгоритма внутри чанка~~ — **закрыт**`) rather than deleting the phrase, so a reader meets "closed" where they used to meet "open" instead of wondering whether it was dropped; the remaining sub-questions of that item (entrance placement, prefab and world-config authoring) are untouched. §2.2.2 gains one sub-bullet under the spanning-structure line recording exactly the owner's decision and nothing else — the per-chunk draw from the chunk seed, the five-algorithm set, and the weights as a biome parameter. Two candidates were drafted and **dropped as redesign**: that a zero weight makes an algorithm unreachable, and that a drawn algorithm's remaining parameters come from the same biome config — both true and both owner-sourced, but neither is the decision this subtask is confined to recording. Both edits are in Russian, and `doc-edit-guard.sh` snapshot/verify bracketed each file.
- **Step 8 (subtask 12, key decisions)**: KD-37..KD-40 under a new *World generation* section — the keyed hash chain and its domain tag (with the purpose byte's reason, the specified big-endian encoding, and the two checked facts about ChaCha8: per-architecture assembly, and an upstream stability golden that `go doc` cannot see); the topology/generator package split, whose load-bearing consequence is that the determinism predicate reads *zero permitted* rather than *exactly one*; connectivity built and never repaired, cited against the corpus line that rejects post-materialisation repair (resolved, not remembered — `docs/DESIGN.md` line 74); and the three share denominators, which are three different numbers and look like one.
- **Step 8 (subtask 12, sweep)**: case-insensitive and multi-encoding over every tracked `*.md` / `*.yml` / `*.yaml` / `*.json` (Russian and English spellings, "открыт / остал / не выбран / не определ / open question / undecided / choice of"), excluding only the scratch directory and the two history surfaces. **The live class had exactly one member**, `docs/DESIGN.md` §16.2, now closed. The surviving hits are this task's own spec, interview state and design, which state the question as the task's premise and retire to `plans/ignored/` at Step 12 — rewriting them would falsify the record of what the task was for, not close a claim.
- **Step 8 (subtask 9)**: `testdata/cells.golden` minted (seed 20260912, dims 16x16, island share 0.05, extra-passage share 0.15, growing-tree bias 0.5, equal weights, nil hook): the origin chunk in full, the diagonally-below-left chunk in full, and the border ring of the chunk below the origin, plus one single-weight section per algorithm over its own named chunk (each section's `chunk(...).algorithm=` line confirms the intended algorithm was actually drawn there). **Scope reduction from the design's own text, stated rather than left implicit**: dumps are full-chunk (256 cells) rather than also cross-checked cell-by-cell against a hand-reviewed table — the mint review here is automated (`TestCellsGolden_EveryIslandCellHasAllSixFacesAsWallInTheMintedTable` cross-checks the golden's own "all six faces wall" lines against a live re-derivation of the origin chunk's island set) rather than a manual line-by-line read of ~1850 lines; this is a narrower substitute for the design's "review the mint" step and is named here as a deviation. `property_test.go` adds the island-walled scenario (both halves: every island face is a wall from both sides; the passage-reachable component equals the non-island set exactly, over chunks (-2,-2)..(1,1)), the island-share tolerance check (±0.01 at the pinned instrument), the AC19 single-weight sweep, and a multi-chunk connectivity sweep over several seeds. `guards_test.go` applies `detguard.Check` to the package's own directory and confirms via AST that `connectedOverInduced` is called only from `island.go` and that `chunkKey`/`borderKey` are called only from `generate.go`.
- **Step 8 (subtask 8)**: `generate.go`'s `Cell` derives `own` from `chunksConsulted(dims, coord)[0]` (a real dependency, not a restated twin) and lazily builds its own chunk's fabric only once per call, only when an interior face is actually needed and the coordinate is not claimed — a claimed coordinate never triggers a fabric build at all. Border faces always go through the portal rule regardless of any claim, on either side. `New` validates once; `Cell` returns without an error/panic path. Caught and fixed during this subtask's own test-writing (not a shipped defect): the first connectivity-sanity test used a region with a partial chunk slice at its edge, which is not itself internally connected — fixed to whole-chunk-aligned bounds; and a locality "near vs far from origin" test compared coordinates at different positions *within* their chunks (interior vs border), which conflates position-in-chunk with distance-from-origin — fixed to compare the same local offset at two different chunk distances.
- **Step 8 (subtask 7)**: `cycles.go`'s `addExtraPassages` only ever draws from `nonIslandInteriorFaces` still closed, so it structurally cannot open a border face. `portal.go`'s `borderCandidates` enumerates the canonically-lesser chunk's own cells row-major × six directions, keeping faces whose destination is the greater chunk — verified identical when called with the pair reversed. `selectPortals` draws a count of 1 or 2 via `boundedDraw(s,2)+1`, capped by candidate count; the two diagonal borders (delta (+1,-1) and its mirror) verified to carry exactly one candidate at three different Dims, and exactly one portal is drawn there on every sampled seed.
- **Step 8 (subtask 6)**: `algorithms.go` implements the five over an `edgeSet` (canonical-pair-keyed open-face set), each dispatched from `buildSpanningStructure`. Backtracker is an explicit-stack DFS; Kruskal shuffles non-island interior faces and uses a component-label array with relabel-on-merge (its one call site, per the design — no shared disjoint-set helper introduced); frontier Prim maintains an explicit frontier list distinct from Kruskal/backtracker; growing tree shares one function for both extremes via `biasedNewest`; Wilson's loop erasure is the standard "overwrite next-step per cell" trick, replayed from the walk start along the surviving pointers. `Algorithm.String()` (spec's lower_snake_case names) added as production code, since a later golden needs to print the drawn algorithm.
- **Step 8 (subtask 5)**: `chunkgraph.go` implements `isBorderCell` via the direct edge check (lq==0 etc.) — verified equal to the interior-condition reading `nonBorderCellCount` uses, by a test asserting the two counts agree. `interiorFaces()` keeps a face only from its lower-index endpoint's iteration, giving a Dims-only deterministic order for later shuffling. `island.go` added `islandTarget` (round(share × total cells), the share's own denominator, distinct from `nonBorderCellCount`'s capacity denominator) and extended `Params.validate` with the general "rounded target exceeds capacity" rejection alongside the existing degenerate-dims blanket rejection — the blanket one still needed since a share can be positive yet round to a target of 0 at those dims. `selectIslands`/`connectedOverInduced` scope the flood fill to the chunk-induced subgraph only, per the design's "no neighbouring chunk may reconnect it" requirement.

- **Step 8 (amendment)**: the owner ruled during Group B's return that the game targets linux amd64 for now — "Ч пока не планирую запускать игру где-то кроме linux amd64" — and, asked what that meant for the architecture gate, answered "Убрать совсем". Recorded verbatim as `prior_qa` round 5 entries 1 and 2 BEFORE any row was written, per trigger (d) of the AXIOM *the orchestrator originates no spec row*.
- **Step 8 (amendment)**: `spec-writer` narrowed AC2 to the toolchain clause and added a Key decisions row recording the ruling with the owner's words as its anchor; `design-writer` removed the § Test Design *Architecture* block, inverted subtask 11 from building the gate to removing it, and re-anchored KD-37's encoding rationale from "same bytes on 32- and 64-bit" to "specified rather than inherited from the host". The determinism mechanisms themselves were explicitly kept — the amendment removes a gate, not a property.
- **Step 8 (amendment)**: the orchestrator's own sweep found subtask 11's removal enumeration incomplete by one file (`ai-docs/key-decisions.md`), and found that the narrow pattern `test-arch|ARCH_GOARCH` under-reports this class because `.claude/skills/dependabot-pr/reference.md` names only the failure class `arch`. Corrected; the row's file set and a wide-pattern sweep are now 9-for-9 with the symmetric difference empty in both directions, and all 19 ACs of the amended spec are still referenced by the design (both checks run with a control that reports a difference when one is introduced).
- **Step 8 (amendment)**: the design-review round cap was exhausted at 4 and the Spec Amendment recipe requires a re-review; the owner raised it to an explicit 5 — `cap: 5 (was 4)` — choosing the re-review over the per-instance exemption the orchestrator recommended. The orchestrator did not raise it and invented no bypass.

- **Step 7 (amendment re-review)**: design-review round 5 returned ITERATE with two majors — subtask 11's removal set enumerated at file but not site granularity, with the design's own verification pattern control-verified blind to the count-word residue the gate commit left; and `internal/maze/doc.go` still asserting the struck architecture axis in a shipped package comment, which no earlier round or orchestrator sweep had looked for because every sweep was written for the gate rather than for the claim. Acceptance for the removal is now the git delta `git diff e216d5d^..HEAD` over the gate commit's eight non-plans paths, with `ai-docs/key-decisions.md` explicitly outside that rule because KD-37 postdates the gate commit — the designer caught that defect in the reviewer's own proposed fix, and the orchestrator confirmed it from git history before accepting it. Subtasks 13 and 14 and a third group (Group C, code, for `doc.go`) were added; a third group is forced by the change-type and size-cap rules together, not chosen.
- **Step 7 (amendment re-review)**: the cap was exhausted at 5 and the owner raised it to an explicit 6 — `cap: 6 (was 5)` — choosing the re-review again. The orchestrator recommended the re-review this time, against its own earlier recommendation to skip one, on the record that five rounds produced five real defects and that both post-amendment rounds found a previously unexamined surface.

- **Step 8 (subtask 11, removal)**: the delta premise was re-measured before anything was touched, **with a control**: `git log --oneline 4315aef..e216d5d^` and `e216d5d..HEAD` over the eight paths are both empty, while the same command over `docs/DESIGN.md` names a commit — so the empty pair is evidence about the tree, not about a pattern that matches nothing. The removal itself is `git checkout e216d5d^ -- <the seven wholly-reverted paths>` plus the workflow restored the same way and then re-given the one kept entry; the working tree was verified clean first, so the whole-file restore could not drop an uncommitted edit (the hazard `AGENTS.md` attaches to that command shape).
- **Step 8 (subtask 11, acceptance)**: **the design's literal acceptance command cannot be satisfied before the commit lands, and that is a property of the command, not of the tree.** `git diff e216d5d^..HEAD -- <path>` compares two *commits* and reads neither the index nor the working tree, so while the row also says "after `git add`", the two-dot form reports the pre-removal gate for every path until HEAD moves. Both forms were therefore run: `git diff --cached e216d5d^ -- <path>` after staging (the index form the "after `git add`" clause actually describes), and the literal two-dot form again after the commit. Both give EMPTY for all eight paths except `.github/workflows/ci.yml`, whose sole residual is the kept `'**/*.golden'` filter entry and its rewritten comment — and both were run with the `docs/DESIGN.md` control reporting NONEMPTY, so neither clean sweep is a blind instrument. One bad control was caught and discarded rather than recorded: `internal/maze/doc.go` reported EMPTY, which looked like blindness but is correct — that file was created *before* the gate commit, so it genuinely has no delta across it.
- **Step 8 (subtask 11, cross-checks beyond the delta)**: the delta is the acceptance, but three independent sweeps were run against it and all agree. A wide-pattern sweep (`test-arch|ARCH_GOARCH|ARCH_PKGS|ARCH_REFERENCE_PKG|arch-host-reference`) over every tracked file outside `ai-docs/plans/` leaves exactly one hit, `ai-docs/key-decisions.md` — which is subtask 13's charter and deliberately outside the delta rule — with a `test-race` control confirming the sweep hits when there is something to hit. The count-word residues are back to four/three in both files that carried them. And a bare-word `architectur` sweep over the same set finds only two live assertions of the struck axis: KD-37 (subtask 13) and `internal/maze/doc.go:10` (subtask 14) — nothing else, which is what makes subtask 14 the last durable surface rather than merely the next one.
- **Step 8 (subtask 11, the entry that stays)**: the `'**/*.golden'` paths-filter entry is kept, its comment rewritten from the removed job to the surviving reason — without the entry a commit editing only a golden matches no Go path and reaches no Go job, so the Test job that compares the golden would not run on the very commit that rewrote it. `actionlint .github/workflows/ci.yml` was run and green **before** the file was staged, per the AXIOM; `shellcheck` was not engaged, the Makefile recipe body having gone away with the target.
- **Step 8 (subtask 11, gates)**: `make -n test-arch` now exits 2 with no rule for the target, which is the removal's own smoke test. `make -n verify` resolves every remaining prerequisite, so editing the target list left no dangling name. Green: `fmt-check`, `build`, `vet`, `lint`, `file-limits`, `shellcheck`, `actionlint`, `comment-refs`, `tidy-check`, `import-guard`, the six harness checkers, all sixteen guard regression suites, the hook-body shellcheck, and the relative-link check. The link check was read for **cardinality** before its verdict — 242 links over the tracked tree, and the sixteen failures it first reported were all under the ignored `tmp/base/` scratch copy, which CI never checks out; `git check-ignore` and an empty `git ls-files tmp/` confirmed that before the result was accepted.
- **Step 8 (subtask 13)**: KD-37's three clauses edited in place, since the entry postdates the gate commit and the delta rule would have deleted it whole. Struck: the closing `make test-arch` clause, and the per-architecture ChaCha8 assembly fact (evidence for a removed instrument, which invites a reader to rebuild it). The two-facts lead-in was re-worded to one fact rather than struck, so the surviving upstream-stability clause does not open on a dangling "and". Re-anchored: the encoding rationale now leads with *specified rather than inherited from the host* — what makes the bytes a function of the inputs alone, and therefore what lets a golden pin them at all — with byte-identity across word sizes demoted to an incidental consequence and the owner's ruling recorded beside it, carrying the design's own sentence that the amendment removed a gate, not a property.
- **Step 8 (subtask 13, whole-section re-read)**: the mandated re-read was run over the whole `## World generation` section, not the three edited clauses. After the edit the section's only hits for `test-arch|GOARCH|architectur|word size|32-bit|64-bit|per-architecture|amd64|arm64|loong64|riscv64|CPU` are the three inside the sentence deliberately written to record the ruling; KD-38, KD-39 and KD-40 carry none. Shape counts held across the edit (11 `## ` sections, 40 `**KD-` lines, 107 lines), so no slice was truncated. Citation guard, `comment-refs` and the link check re-run green afterwards.
- **Step 8 (Group B close, superseding an earlier directive)**: the "unmeasured half" entry above tells a future reader to read the Architecture job on this branch's first CI run before anything else. That directive is now void — there is no Architecture job, and the unmeasured question (whether a hosted runner can execute a 32-bit binary) is no longer asked by anything on this branch. The entry stays because this log is append-only; this line is what retires it. The two `## Key discoveries` measurements about `GOARCH=386` and arm64 remain true as measurements and are left alone — they record what this machine can do, not what any gate now requires.
- **Step 8 (subtask 14, Group C)**: struck the `and CPU architectures` clause from `internal/maze/doc.go`'s package comment, leaving "the same result across processes and Go versions" — exactly AC2's amended scope. The mechanisms sentence (no clock, no unseeded source, no floating-point arithmetic) is untouched, since it serves the surviving clause. Gates run: `go build ./...`, `go vet ./...`, `golangci-lint fmt -d` (zero-line diff), `golangci-lint run`, `go test ./internal/maze/...`, `make comment-refs` — all green. This was the last durable surface asserting the struck axis (confirmed by subtask 11's bare-word `architectur` sweep, which named only KD-37 and this file); AC2 flips from PARTIAL to TESTED. Group C (subtask 14 alone) is now complete — all 14 subtasks done.

- **Step 8 (Group B finding)**: the Group B implementor found the design's acceptance command unhonourable as written — `git diff e216d5d^..HEAD` compares two commits and reads neither index nor working tree, so the row's "after `git add`" qualifier was inert for it. The qualifier came from design-review round 6's own recommendation, correct about index-walking checkers and wrong about a commit-range diff. The implementor ran BOTH forms with controls rather than silently substituting one, `design-writer` reproduced the blindness with a staged probe (0 lines against 9), and the row now binds each form to the moment it is run with the staging concern re-attached to the checkers it governs.
- **Step 8**: all 14 subtasks are complete; the gate removal's acceptance re-verified at HEAD by the orchestrator — seven paths at zero diff lines, `.github/workflows/ci.yml` carrying only the kept `'**/*.golden'` entry and its rewritten comment, with `docs/DESIGN.md` as a control reporting 21 lines.
- **Step 7 (third amendment)**: the cap was exhausted at 6 and the owner raised it to an explicit 7 — `cap: 7 (was 6)` — choosing the re-review over the orchestrator's recommended exemption for the second time, on the ground that the defective instruction originated in a review recommendation and so the gate's own advice is owed the gate.

- **Step 9**: all nine gates green — build, vet, `fmt -d` at zero diff lines, lint, test, race, comment-refs, import-guard, actionlint. `go.mod` and `go.sum` are unchanged across the branch, so the tidy gate is not engaged, and no `*.sh` changed, so shellcheck is not engaged. The panic index is unchanged: the only panic sites are two in a `_test.go` helper, which the recipe skips. The domain-invariant sweep is clean on all five probes with all five controls firing — no balance mutation, no posting, no balance constant compiled into Go, no `time.Now()`, no secret; the `math/rand` hits are the designed ChaCha8 stream and `internal/detguard`'s own ban machinery.
- **Step 9**: the per-AC sweep ran the orchestrator's own command per criterion rather than reading a delegate's table. AC18's first attempt produced a clean verdict from an EMPTY file list — the pattern carried only the Latin spelling against a Russian corpus — and was caught only because the section-16 site had been read separately; the corrected instrument is a conjunction with two controls, one that must fire and one that must not.

- **Step 9.5**: `ai-docs/context-status.md` gained this run's entry with the PR locator as the literal placeholder the CI guard greps for, and `ai-docs/context.md` gained the three packages in its layout paragraph and its status bullet, with the status date bumped. No open question needed resolving on the orientation page — the intra-chunk-algorithm question was never listed there; it lives in the design corpus's own section, which subtask 12 struck.
- **Step 9.5**: the removal-surface sweep found the orientation page's "each test gate — the suite, the race gate, the coverage ratchet, the fallback path" sentence still TRUE, because the revert restored `ci.yml`'s four-sibling cluster; had the architecture gate stayed, that sentence would have been false and unnoticed. Three tallies of repository artefacts were caught in the orchestrator's own draft prose and removed rather than re-measured — a package count and a golden count; `two commits` survives only as a statement about `git diff`'s semantics, not as a count of anything here.

- **Step 11 (round 1)**: all seven findings fixed, none objected, and none a Spec or Design Amendment trigger. Each register row was closed by running its OWN verifying command rather than a variant: the cell-seed mutant now fails the new two-generator scenario, the map-parameter range mutant now reddens the widened guard (with the clean tree still green, so no production code was ranging over a parameter map), the `randV2Local` kill now reddens the rand/v2 red case whose fixture no longer collides with a banned decimal selector, the `Contains` `return true` mutant now reddens its own negative case, and the outward-pointer sweep returns nothing with a working control.
- **Step 11 (round 1)**: one of the orchestrator's own DOC-4 edits broke a sentence — cutting the pointer out of `generate_test.go`'s comment left the following line dangling and still quoting the design. Caught by reading the edited region rather than by a gate, since a broken comment compiles. The whole parenthetical was rewritten.
- **Step 11 (round 1)**: AC17's figure was re-derived after the round's last edit rather than carried over from Step 9, per the batch-ordering rule.

- **Step 11 (round 2)**: the Go fixes were authored by `code-writer` in Mode B, not in-thread. Round 1's batch was authored in-thread, which was a charter-fit violation the owner caught: `ai-docs/delegation-rules.md` § Phase 1 — Fit puts a predominantly-`.go` diff with `code-writer`, and the step name does not change that. Recorded in `ai-docs/learnings.md`, with the dangling citation that made the misreading easy recorded in `ai-docs/harness-gaps.md`.
- **Step 11 (round 2)**: the delegate's acceptance exercised one of the four arms its new red cases cover and said so. The orchestrator then ran a per-arm matrix — dropping the parameter, named-result, receiver and struct-field arm each alone — and every one of the four red cases went RED for its own arm, with each mutant confirmed to build and vet first and the restore verified byte-identical. Three of those four red cases had never been seen red before that matrix, which is the class this whole review loop has been finding.
- **Step 11 (round 2)**: `origin/main` was merged in at the owner's instruction before the fixes. Four conflicts, all in files both sides had appended to or edited; the two append-only logs were resolved by taking main's side whole and re-appending this branch's delta, verified by the line reconciliation main's own log names (`merged == theirs + ours - base`) plus an explicit check that every heading from both sides survives. The merged tree measures 91.40% coverage, above both sides' marks. The domain-invariant sweep was re-run with main's updated ledger pattern, since the clean result from Step 9 had been obtained with the superseded one.

- **Step 11 (round 3)**: the register gate had never run against this file's register. Its join key is `<letters><round>-<finding>`; the ids were a global `SR-<n>` sequence, which the script skips as unparseable, so every Fixed row reported as having no register row. Re-labelled onto the canonical form. Round 2's second finding is round 1's fourth re-opened, so its round-2 position carries a `superseded→R1-4` row rather than a duplicate of a permanent id.
- **Step 11 (round 3)**: the reviewer's stated consequence for that finding — that the next commit of this file would be refused — did not hold. The orchestrator tested it: the hook is present in `.claude/settings.json`, the script exits 1 on this file, and a commit staging it succeeded. The shell hypothesis was ruled out by measurement (the hook body reaches the script under both bash and sh). Cause undetermined; recorded as a harness gap rather than guessed at, and it means every earlier "the register and the round table agree" claim in this run rested on a gate that never ran — the agreement itself was checked by hand and holds.
- **Step 11 (round 3)**: the named-map-type fix was verified by the orchestrator beyond the delegate's own acceptance: the production shape now reddens the maze guard, the literal shape has not regressed, each of the four positional arms dropped alone reddens both cases of its own pair rather than one shared path, and neutralising the new package-wide collector reddens all six named-type cases while leaving the literal ones green. A first collector mutant failed to build and its silence was discarded rather than recorded.

- **Step 10 (cap arithmetic)**: the self-review round cap was raised twice by the owner, and both raises are recorded here in the form the skill mandates rather than only in conversation — `cap: 4 (was 3)` after round 3's REJECT, on the ground that the map-range predicate's reach had failed three rounds running; and no third raise, because round 4's REJECT was answered by narrowing the design's claim instead. Round 4 also caught that the first of those raises reached the progress file as prose without the echo, while all four design-review raises carried both the echo and a log entry — the asymmetry was the finding.
- **Step 11 (round 4) — Design Amendment, owner-approved.** Four rounds each found a different shape the map-range predicate cannot see (parameter, then its missing red case, then named type, now call result and selector), and the orchestrator measured five more open. Deciding map-ness from an identifier's name approximates a type question, and the design excludes `go/types`, so the claim was the defect rather than the code: the owner ruled "Сузить заявление" (`answer 6.1`). The design now states three mitigations in order of strength — the weight array removes the question, the guard is an approximation and a pre-check, and the goldens are the proof, since map order reaching a drawn value would move `cells.golden` and split the order-independence scenario. No live defect exists: all thirty-six production `range` statements were checked and the five with map-ish operands range over slice results.
- **Step 11 (round 4)**: `design-writer` flagged that Step 12's inbox propagation reads only the spec's `## Deferred`, so the blind-shape row would need a spec row — and asked the orchestrator to decide it, since originating a spec row is the orchestrator's. The flag is refuted: `inbox-propagation.md` says the parser "walks one or more spec/design files" and derives a `source_label` suffix of ` spec` or ` design` per source, with a `.design.md` worked example. The design row will be raised; no spec amendment was opened.
- **Step 11 (round 4)**: `grep` in this environment is `ugrep 7.8.4`, which refuses a sufficiently complex pattern with an error rather than matching — a refusal reads as zero hits wherever stderr is not watched. One of the orchestrator's own verification patterns hit it while checking this amendment. Step 9's two load-bearing sweeps (AC18's conjunction and the DOC-4 outward-pointer sweep) were therefore re-run with the tool's stderr surfaced and their controls re-fired: zero tool errors, so both clean verdicts stand as measurements rather than as refusals.

- **Step 11 (round 4) — both caps raised by the owner in one answer, echoed here because a raise recorded only in conversation is what round 4 found wrong.** `design-review cap: 8 (was 7)` and `self-review cap: 5 (was 4)`, chosen over the two cheaper routes offered (a per-instance exemption from the amendment's re-review, or lifting subtasks 15-16 out of the design into the deferred row). The route is: design-review round 8 on the amended design, then Group D via `code-writer`, then a refreshed Step 9 per-AC sweep, then self-review round 5.

## GO notes

| # | round | note | kind | route | resolution |
|---|-------|------|------|-------|------------|
| G1 | 4 | "AC11's first clause … has no discriminating assertion" | design-internal | folded | design § Test Design → *Islands are walled* @ 4315aef — both sides read `Wall`, and the passage-reachable component equals the non-island set exactly |
| G2 | 4 | "The `GOARCH` block re-runs … and the cells golden covers three chunks under equal weights" | design-internal | folded | design § Test Design golden + subtask 9 @ 4315aef — the golden gained a single-weight section per algorithm |
| G3 | 4 | "Subtask 11 … states two expectations … but not the direction when the second `GOARCH` cannot execute at all" | design-internal | folded | design § Risks + subtask 11 @ 4315aef — three outcomes separated; loud skip locally, loud refusal in CI |
| G4 | 4 | "Two `[measured …]` tags sit on sentences about artefacts this task creates" | design-internal | folded | design § Approach + § Risks @ 4315aef — `[measured]` kept on the existing-code half, `[derived]` on the design-decision half |
| G5 | 4 | "§ Test Design → *Guards* states the predicate as \"exactly one identifier referenced from `math/rand/v2`\"" | design-internal | folded | design § Guards + subtask 1 @ 4315aef — restated as zero permitted, never "exactly one" |
| G6 | 4 | "Untagged incidental facts worth a tag or a trim" | design-internal | folded | design § Approach @ 4315aef — `encoding/hex` and the three `…guard` packages now carry measurements |
| G7 | 4 | "`internal/config.ChunkBalance.Cols/Rows` are `int` while `hexgrid.Dims` is `int32`" | design-internal | folded | design § Approach @ 4315aef — the narrowing conversion and its G115 remedy handed to #28 |
| G8 | 4 | "Group A carries ten determinism-critical subtasks at the mandated `sonnet`/`medium` tier" | design-internal | folded | design § Test Design + subtask 9 @ 4315aef — mint review named as an obligation, read against the island rule and the algorithm column |
| G9 | 4 | "**Round-trip required:** before Step 8, update the design doc to incorporate each note/recommendation above" | design-internal | folded | design round 5 @ 4315aef — all nine items verified landed by reading the diff, not by the delegate's return summary |
| G10 | 6 | "Subtask 11 carries three tallies about the repository" | design-internal | folded | design § Decomposition row 11 @ bd6a52b — both "eight"s dropped, the Makefile clause reduced to "the target, its variables and their comment block" |
| G11 | 6 | "Two factual claims about existing state are load-bearing and untagged" | design-internal | folded | design rows 11, 13, 14 @ bd6a52b — three `[measured bd6a52b · …]` tags, each re-verified before it was written |
| G12 | 6 | "Subtask 13(b) says "the lead-in counts the facts, so it moves with them"" | design-internal | folded | design § Decomposition row 13 @ bd6a52b — stated outright: the lead-in is re-worded to one fact, not struck, because the upstream-stability fact is AC2's upstream half |
| G13 | 6 | "Subtask 14's `Depends on` column is `—`" | design-internal | folded | design § Decomposition row 14 @ bd6a52b — now `3`, the subtask that creates `doc.go` |
| G14 | 6 | "Keep the delta acceptance literal when subtask 11 runs" | design-internal | folded | design § Decomposition row 11 @ bd6a52b — per path, after `git add`, with the index-walk reason stated |
| G15 | 6 | "`actionlint .github/workflows/ci.yml` before staging the workflow edit" | design-internal | folded | design § Decomposition row 11 @ bd6a52b — actionlint required; shellcheck explicitly not engaged, the recipe body going away with the target |
| G16 | 6 | "After subtask 13, re-read KD-37 **whole** rather than the three edited clauses" | design-internal | folded | design § Decomposition row 13 @ bd6a52b — the whole-section re-read is stated as the obligation |
| G17 | 6 | "**Round-trip required:** before Step 8, update the design doc to incorporate each note/recommendation above" | design-internal | folded | design round 7 @ bd6a52b — all seven verified landed by reading the diff, not by the delegate's return summary |
| G18 | 7 | "The amendment's two decisive sentences assert external-tool behaviour untagged" | design-internal | folded | design § Decomposition row 11 @ 9647cf0 — both forms tagged `[measured git 2.55.0 · git help diff → …]`, classified as toolchain rather than commit-pinned facts |
| G19 | 7 | "a `git ls-files` sweep" | design-internal | folded | design § Decomposition row 11 @ 9647cf0 — narrowed: `--cached` and the staged-set gate are blind to an unstaged edit, `ls-files` only to a file not in the index at all; verified in a throwaway repo |
| G20 | 7 | "§ Test Design's sibling paragraph still says "The acceptance is the delta" with no moment-binding" | design-internal | folded | design § Test Design @ 9647cf0 — the twin now names no command form and points at row 11, so the qualifier travels with the form |
| G21 | 7 | "The row's new control-pairing rule names `docs/DESIGN.md`, "which subtask 12 edited"" | design-internal | folded | design § Decomposition row 11 @ 9647cf0 — replaced by the property that makes a control discriminating: a path already changed between `e216d5d^` and the current HEAD |
| G22 | 7 | "`Algorithm.String()` ships as exported production code whose only callers are tests" | design-internal | folded | design § API sketch @ 9647cf0 — recorded as production surface on merit: it satisfies `fmt.Stringer`, governs every formatted render, and carries the canonical spelling the golden's algorithm column uses; the maze-renders/#28-parses seam is recorded with it |
| G23 | 7 | "Issues 1-4 all live in one paragraph-pair. Fold them together and re-read both surfaces whole" | design-internal | folded | design @ 9647cf0 — the whole-read caught a rotting tag the clause view hid: the sole-commit premise was pinned on the HEAD-relative `e216d5d..HEAD`, which had become non-empty; re-anchored on `4315aef..e216d5d^` and `e216d5d^..e216d5d`, verified 0 and 1 by the orchestrator |
| G24 | 7 | "for Step 9/10 rather than the fold: `internal/maze/prefab.go`'s `PrefabClaimer` comment ends "per the design's own ordering"" | design-internal | deferred to Step 9 | deferred to Step 9 — DOC-4 review-judged narration; the orchestrator found a second site the reviewer did not name, `internal/maze/generate_test.go:62` |
| G25 | 7 | "**Round-trip required:** before Step 8, update the design doc to incorporate each note/recommendation above" | design-internal | folded | design round 8 @ 9647cf0 — all five items verified landed by reading the diff, plus the rotting tag re-measured against git |

## Key discoveries (don't re-investigate)

- **A second architecture is a build-time choice here, not a runner question.** `GOARCH=386 go test -count=1 ./internal/backoff/` runs natively, exit 0, no emulation; word size drops 64 → 32. ChaCha8 ships per-architecture assembly for amd64/arm64/loong64/riscv64 but **not** 386, so a 386 run compares a golden minted on the assembly path against `chacha8_generic.go`, while SHA-256 has its own `sha256block_386.s`. `GOARCH=arm64` cross-builds but cannot execute here (`exec format error`), so 386 is the available instrument and arm64 is not.
- **The upstream ChaCha8 stability gate is live, not a dormant file.** `$GOROOT/src/math/rand/v2/chacha8_test.go` carries a golden test commented "to make sure algorithm never changes", and `go test -run 'TestChaCha8…' math/rand/v2` passes. `go doc` cannot see it — which is why the design's original "no stability promise exists" negative was false.
- **`internal/srcguard` will not host predicates.** Its package comment reserves its scope to walking and parsing: "Every predicate — what a guard actually forbids — stays in the package that owns the proposition." Hence `internal/detguard`, composing srcguard's walker.
- **Where the residual risk sits, per the reviewer:** subtasks 6 (five traversals) and 9 (property suite + golden mint) carry the least pre-decided detail, and G1's residual risk lands in the **mint review** — a golden is minted by the code it then guards, so a wrong mint is self-consistent and every later run agrees with it. Read the minted table against the island rule and the per-chunk algorithm column, not only the diff's shape.
- **Kruskal and MST-formulated Prim produce the identical spanning tree** on the same random weights (minimum spanning trees are unique for distinct weights), so the design's frontier-based Prim is what makes the fifth algorithm a distinct traversal rather than a duplicate. The owner ruled that edge-condition similarity is acceptable and struck the distinctness AC, so nothing asserts a separating statistic.

## AC Status

| AC | Status | Verifying command |
|----|--------|-------------------|
| AC1 | PASS | `go test -count=1 -run '^TestCell_(RepeatEvaluationIsIdentical|ShuffledEvaluationOrderMatchesSortedOrder|RaceSafeAcrossGoroutines)$' ./internal/maze/` — all three pass; the shuffled-order case was mutation-tested by the orchestrator (a cross-call memo on Generator: builds, vets, test RED; removed: green). The separate-process clause is discharged by the golden's mint/check split, per the design. |
| AC2 | PASS | `go test -count=1 -run '^Test(CellsGolden|Derive_Golden)$' ./internal/maze/` — both goldens compare at the Go version `go.mod` names, and CI pins it by `go-version-file: go.mod` in every Go job. The architecture clause was struck by the owner's ruling, so AC2 now has one clause. |
| AC3 | PASS | `go test -count=1 -run '^TestCell_(FaceAgreementOverAMultiChunkRegion|ClaimedBorderCoordinateCarriesPortalRuleBorderFaces)$' ./internal/maze/` — agreement asserted over a multi-chunk region and, separately, across a claimed chunk's border ring. |
| AC4 | PASS | `go test -count=1 ./internal/hexgrid/` — 0 FAIL, 0 SKIP over the topology suite: neighbour symmetry, `Opposite`, and one canonical face per pair computed by `FaceOf` from either side. |
| AC5 | PASS | `go test -count=1 -run '^TestChunkGraph_IndexMappingRoundTrips$|^TestParams_' ./internal/maze/` plus the `./internal/hexgrid/` chunk tests — `ChunkOf` floor-divides and the dimensions are a `Params` input. |
| AC6 | PASS | `go test -count=1 -run 'ChunkDistance|Distance' ./internal/hexgrid/` — zero exactly when both coordinates lie in one chunk, and counted on the chunk grid rather than in hexes. |
| AC7 | PASS | `go test -count=1 -run '^TestAlgorithms_EachBuildsASpanningTree$|^TestSelectIslands_NonIslandCellsStayConnectedOverInducedSubgraph$' ./internal/maze/` — connectivity over the chunk-induced subgraph, with no repair stage anywhere on the path that produced it. |
| AC8 | PASS | `go test -count=1 -run '^TestSelectPortals_' ./internal/maze/` — one-or-two capped by the candidate count, the two diagonal borders always exactly one, and candidate agreement from either side. |
| AC9 | PASS | `go test -count=1 -run '^TestConnectivity_MultiChunkRegionOverASeedSweep$|^TestCell_ConnectivityOverAMultiChunkRegionNilHook$' ./internal/maze/`. |
| AC10 | PASS | `go test -count=1 -run '^TestAddExtraPassages_' ./internal/maze/` — a zero share yields exactly a tree, a positive share adds a cycle, the count is capped by availability, and the pass never opens a border face. |
| AC11 | PASS | `go test -count=1 -run '^TestIslandsAreWalled_|^TestIslandShare_|^TestSelectIslands_' ./internal/maze/` — every island face reads Wall from both sides and the passage-reachable component equals the non-island set exactly; the tolerance is pinned at 0.01 in the test rather than chosen after measuring. The orchestrator's own mint review found 13 all-wall cells in every full chunk, which is round(0.05 of 256), and zero of them on a chunk border, across all seven chunks the golden covers. |
| AC12 | PASS | `go test -count=1 -run '^TestChunksConsulted_' ./internal/maze/` — the consulted set's size is invariant with distance from the origin, every member is the cell's own chunk or a face-neighbour's, and the set of chunk offsets relative to each coordinate's own chunk agrees for two coordinates at the same position within their respective chunks. |
| AC13 | PASS | `go test -count=1 -run '^TestCell_(NoHookNoCoordinateIsMarkedAsPrefab|ClaimedInteriorCoordinateDefersAllSixFacesAndHasNoSeed|ClaimSuppressesFabricComparedToNoHook|ClaimerIsAskedAboutEveryCoordinate)$' ./internal/maze/`. |
| AC14 | PASS | `go test -count=1 -run '^TestDrawAlgorithm_' ./internal/maze/` — the same algorithm on repeat evaluation, a zero-weight algorithm never drawn, and a weight change changing which algorithm a chunk draws. |
| AC15 | PASS | `go test -count=1 -run '^TestGrowingTree_TwoBiasesProduceDifferentStructures$|^TestBiasThreshold_' ./internal/maze/` — a drawn algorithm's further parameter arrives as a generation input, and changing it changes the structures that algorithm builds. |
| AC16 | PASS | `go test -count=1 -run '^TestCell_SeedIsIndependentOfChunkDimensions$|^TestCellSeed_' ./internal/maze/` — two generators differing only in chunk dimensions yield the same cell seed for every sampled coordinate while their faces differ somewhere, so the assertion cannot hold vacuously; and the value is pairwise distinct over a coordinate table. The first pattern is the one that reaches the discharging test — round 1 replaced a same-signature tautology with it, and the old `^TestCellSeed_`-only command no longer reached the clause this row claims. |
| AC17 | PASS | `go test -run '^$' -bench 'BenchmarkCell$' -benchtime=10x ./internal/maze/` reports 730024 ns/op at the close of self-review round 1, so one cell's cost is measurable on demand. The no-threshold half was measured as a negative with a control: a recursive search for `-bench` and `benchtime` across `Makefile`, `.github/workflows/ci.yml`, `.githooks/` and `ai-docs/scripts/` exits 1 while the same pattern fires on a constructed gate line, and `go help testflag` states "By default, no benchmarks are run." |
| AC18 | PASS | The orchestrator's own conjunction sweep, since this criterion's verification is entirely a command: over every tracked `*.md` outside this run's plan files and the history logs, no line both names the class (maze-algorithm in either script, or algorithm near chunk) and frames it as open (открыт, не решен, не выбран, TBD, open question, undecided). BOTH controls behaved: a constructed open line matches, a constructed decided line does not. `docs/DESIGN.md:482` carries the clause struck through and marked закрыт. A first attempt returned a clean verdict from an EMPTY file list, because the pattern held only the Latin spelling against a Russian corpus. |
| AC19 | PASS | `go test -count=1 -run '^TestAlgorithmDraw_SingleWeightSweep$|^TestAlgorithms_' ./internal/maze/`, plus the orchestrator's mint review: the golden carries one single-weight section per algorithm, and each reports its own algorithm in the per-chunk column — backtracker, kruskal, prim, growing_tree and wilson_walk all present. |


### Step 9 observations routed to self-review

- `internal/maze/golden_test.go:104,136` — `panic(err)` inside `cellsGoldenLines()`, a helper that takes no `testing.TB` and so cannot call `t.Fatal`. Not a panic-index matter (the recipe skips `_test.go` files, and `ai-docs/panic-index.md` stays the single empty row) and no gate objects; whether the helper should take a `tb testing.TB` instead is self-review's call.
- Two outward-pointer comments of the DOC-4 review-judged narration kind, both passing `make comment-refs` because neither carries a path: `internal/maze/prefab.go:7-8` "per the design's own ordering" (raised by design-review round 7) and `internal/maze/generate_test.go:62` "per the design's own Determinism" (found by the orchestrator; the reviewer named only the first).

## Review register

| id | raised | severity | status | verifying command |
|----|--------|----------|--------|-------------------|
| R1-1 | 1 | major | fixed@624175d · verified@2 | Inject `seed = cellSeed(gen.seed, coord) ^ uint64(dims.Cols-16)` at `internal/maze/generate.go:123` (identity at the golden's 16x16, so both goldens stay green), then `go test -count=1 ./internal/maze/` — must be RED. Measured at round 1: GREEN. |
| R1-2 | 1 | major | fixed@2bdfa83 · verified@2 | Add `for k := range islands { _ = k }` to `nonIslandCells` in `internal/maze/algorithms.go` (a map arriving as a parameter), then `go test -count=1 -run '^TestGuard_DeterminismPredicates$' ./internal/maze/` — must be RED. Measured at round 1: GREEN. Control (a local `make(map[int]bool)` ranged in the same place): RED. |
| R1-3 | 1 | major | fixed@2bdfa83 · verified@2 | Replace `if randV2Local != "" {` with `if randV2Local == "\x00impossible-local-name" {` in `internal/detguard/detguard.go` (builds; identifier stays used), then `go test -count=1 ./internal/detguard/` — must be RED. Measured at round 1: GREEN. |
| R1-4 | 1 | minor | fixed@5a78f72 | **Round 1's command was blind and is replaced.** `grep -rnE '(see |per the |§|\.md|\bAC[0-9]|\bKD-[0-9]|the design|the spec|the criterion)' internal/maze/*.go internal/hexgrid/*.go internal/detguard/*.go \| grep -E '//'` — must return no comment that points the reader elsewhere rather than stating the fact itself; pair it with a constructed pointer line as a control. The eight document pointers round 1 named are gone (the narrow pattern is clean at HEAD). The widened pattern, control firing, leaves one site the narrow one could not see: `internal/maze/generate.go:35` `// see PrefabClaimer.` |
| R1-5 | 1 | minor | fixed@2bdfa83 · verified@2 | `grep -n 'float64' internal/detguard/detguard_test.go` around the `DecimalFloatAccessorBlindShape` fixture — the fixture must contain no `float64` identifier, as its own comment claims. Measured at round 1: `func f(d decimal.Decimal) float64` at line 106. |
| R1-6 | 1 | minor | fixed@624175d · verified@2 | A test asserting that for two coordinates at the same position within their respective chunks, `chunksConsulted`'s results agree after subtracting each coordinate's own chunk, must exist and pass. Measured at round 1: absent — `internal/maze/generate_test.go:211-223` asserts set size only. |
| R1-7 | 1 | minor | fixed@624175d · verified@2 | Replace `Dims.Contains`'s body with `return true` in `internal/hexgrid/chunk.go`, then `go test -count=1 ./internal/hexgrid/ ./internal/maze/` — must be RED. Measured at round 1: GREEN. |
| R1-8 | 1 | nit | accepted@1 — below severity floor | `internal/maze/golden_test.go:104,136` `panic(err)` in a `testing.TB`-less helper; `internal/maze/chunkgraph_test.go:108-119` name/assertion mismatch; `internal/maze/cycles_test.go:55-71` compares `open` against the same enumeration the pass draws from; `internal/maze/draw.go:52-66` doc overstates `weightedPick` on an all-zero weight set; `internal/maze/seed_test.go:72-79` hand-rolled big-endian; `internal/hexgrid/chunk.go:23-33` divide-by-zero on a zero `Dims`. |
| R1-9 | 1 | — | accepted@1 — not a defect | `hexgrid.ChunkOf`/`Origin`/`Distance` divide by `Dims.Cols`/`Rows` and panic on a zero `Dims`. The precondition and its guarantor are stated on the `Dims` type's own doc comment ("a caller's constructor's concern"), `maze.Params.validate` is that constructor, and no sibling function performs the check — so the `…Unchecked` AXIOM is not engaged. Re-raise only with a caller that reaches the division without a validating constructor. |
| R1-10 | 1 | — | accepted@1 — not a defect | Subtask 11's gate-removal acceptance re-verified at HEAD: `for p in <the eight paths>; do git diff e216d5d^..HEAD -- "$p" \| wc -l; done` → 0 for seven, 17 for `.github/workflows/ci.yml` (the kept `'**/*.golden'` entry and its rewritten comment only); control `git diff e216d5d^..HEAD -- docs/DESIGN.md` → 21. Wide-pattern and bare-word `architectur` sweeps outside `ai-docs/plans/` leave no live assertion of the struck axis. |
| R1-11 | 1 | — | accepted@1 — not a defect | No map is ranged over anywhere in either package's non-test files today: `grep -nE 'for[[:space:]].*range' internal/maze/*.go internal/hexgrid/*.go internal/detguard/*.go \| grep -v _test.go` returns only slice and array ranges. R1-2 is about the guard's reach, not about a live determinism defect. |
| R2-1 | 2 | major | fixed@5a78f72 | Replace the `case *ast.FuncType:` arm in `internal/detguard/detguard.go` with `noteMapFields(out, node.Results)` alone — dropping the map-typed **parameter** shape the round-1 fix added — then `go test -count=1 ./internal/detguard/` and `go test -count=1 -run '^TestGuard_DeterminismPredicates$' ./internal/maze/ ./internal/hexgrid/`. At least one must be RED. Measured at round 2: all three packages GREEN. |
| R2-2 | 2 | minor | superseded→R1-4 | Round 2 finding 2 is round 1 finding 4 re-opened, not a new finding: the row that carries it is `R1-4`, and an id is permanent across rounds. This row exists so the register gate's round-and-position join has a match for the round-2 table's second row rather than reporting it missing. |
| R2-3 | 2 | minor | fixed@5a78f72 — the AC16 and AC12 rows' own re-sync rides in the progress commit that carries this cell, since a register row cannot cite the commit containing it | `go test -count=1 -run '^TestCellSeed_' -v ./internal/maze/` — the AC16 row's own recorded command must reach the test that discharges the clause its prose claims ("independent of the chunk dimensions"). Measured at round 2: it reaches only `TestCellSeed_PairwiseDistinctOverACoordinateTable`; the discharging test is now `TestCell_SeedIsIndependentOfChunkDimensions`, which the pattern does not match. |
| R2-4 | 2 | — | accepted@2 — not a defect | `collectMapTypedIdents` keys its results by identifier NAME in one file-local namespace, so a name that is map-typed in one function and slice-typed in another over-reports. The direction is safe — a false positive reds the suite rather than hiding a range — the doc comment says "best-effort, file-local", and no current false positive exists (`go test ./internal/maze/ ./internal/hexgrid/ ./internal/detguard/` GREEN at HEAD). Re-raise only with a measured false positive that blocks a legitimate construct. |
| R2-5 | 2 | — | accepted@2 — not a defect | The decimal float-accessor red case re-probed against its new reduced fixture: with `bannedDecimalSelectors` made dead, `TestCheck_DecimalFloatAccessorBlindShapeFlagged` goes RED ("want a problem for the decimal Float64() accessor call…, got none"). Still discriminating, and now structurally so rather than by capitalisation. |
| R3-1 | 3 | major | fixed@4c1f700 | Insert `for k := range open { _ = k }` immediately after `nonIslandCount := g.cellCount() - len(islands)` in `addExtraPassages` (`internal/maze/cycles.go`), then `go test -count=1 -run '^TestGuard_DeterminismPredicates$' ./internal/maze/` — must be RED. Pair it with the same range over `islands` in the same place, which must also be RED. Measured at round 3, three cells, each mutant confirmed to build and vet first: `open` (a named map type) GREEN — the guard is blind; `islands` (a literal `map[int]bool`) RED (`[.../cycles.go: ranges over a map]`); clean tree GREEN. |
| R3-2 | 3 | major | fixed@4c1f700 — this row's own fix is the progress-file edit that carries this cell, so it cannot cite the commit containing it | `ai-docs/scripts/check-review-register.sh ai-docs/plans/2026-09-12-world-generation-hex-chunk-generator.progress.md` — must exit 0. Measured at round 3: exit 1, ten lines of the form `round 1 finding 1 is marked Fixed and has no register row`, for every numbered finding of rounds 1 and 2. Cause isolated with a three-cell control on a minimal file: id `R1-1` → FAIL (row skipped as unparseable, never compared); id `R1-1` + `fixed@<sha>` → PASS; id `R1-1` + `open` → the gate's own intended failure fires. The ids of rows R3-1 onward already use the canonical `R<round>-<n>`; closing this row means re-labelling the legacy `SR-<n>` rows and re-running the command above. |
| R3-3 | 3 | minor | fixed@4c1f700 — this row's own fix is the progress-file edit that carries this cell, so it cannot cite the commit containing it | Read the progress file's `## Next action` block against its own `## Subtasks` list and `## AC Status` table: no sentence of it may instruct a re-entering agent to do work already marked `[x]`, and no claim in it may contradict an AC row. Measured at round 3: it opens "Do this immediately: Group C is subtask 14 alone — strike the `and CPU architectures` clause from `internal/maze/doc.go`" while subtask 14 is `[x]`, `internal/maze/doc.go:9` already reads "across processes and Go versions" (no architecture clause), and it asserts "AC2 … is PARTIAL today" while the AC Status table records AC2 as PASS. |
| R3-4 | 3 | nit | accepted@3 — below severity floor | `internal/detguard/detguard_test.go:189-195` `TestCheck_MapReceiverRangeFlagged`'s fixture `func (m map[string]int) f()` is not compilable Go — measured: `go vet` on that source reports `invalid receiver type map[string]int`. A receiver base type must be a defined type, so `*ast.MapType` can never appear as a receiver in code the toolchain accepts; the `node.Recv` arm and its red case therefore cover a shape that cannot occur. Harmless over-coverage that fails closed; recorded so it is not rediscovered. |
| R3-5 | 3 | — | accepted@3 — not a defect | An implementation swap between two of the five algorithms is caught by the cells golden alone, not by any behavioural assertion. Measured: with `buildSpanningStructure`'s `AlgorithmWilson` arm dispatched to `buildBacktracker` (mutant builds and vets), the suite goes RED on `TestCellsGolden` only. This is the designed position — the spec's Key decisions row *Must the five be provably different in what they build?* answers "No … no statistic has to separate them" on the owner's ruling, and the golden's per-algorithm single-weight sections are what pin each implementation. Re-raise only if the golden's per-algorithm sections are removed. |
| R3-6 | 3 | — | accepted@3 — not a defect | `weightedPick` (`internal/maze/draw.go:52`) states an unchecked precondition ("The caller guarantees at least one positive entry") without the `…Unchecked` suffix. The guarantor exists and is reached on every path: `Params.validate` rejects `p.Weights.totalWeight() == 0` (`internal/maze/params.go:43-45`) and `New` is the only constructor. No sibling function performs the check, so the AXIOM is not engaged — the same reasoning R1-9 recorded for `hexgrid.Dims`. Re-raise only with a caller that reaches `weightedPick` without passing `Params.validate`. |
| R4-1 | 4 | major | fixed@7fc7df6 (design amended@7fc7df6 — see the Design Amendment rows in the decisions log) | Insert `for k := range islands { _ = k }` immediately after `islands := selectIslands(…)` in `internal/maze/generate.go` (two tabs, inside `buildOwnFabric`), then `go test -count=1 -run '^TestGuard_DeterminismPredicates$' ./internal/maze/` — must be RED. Pair it with the same range over `open` in the same place, which must also be RED (the control). Measured at round 4, three cells, each mutant confirmed to build and vet first: `islands` (a call result) GREEN — the guard is blind; `open` (a declared identifier) RED (`[.../generate.go: ranges over a map]`); clean tree GREEN. Closing the row additionally requires each of these scratch shapes to be flagged: `m := g()` where `g` returns a map; `for k := range g()`; `for k := range v.m` on a map struct field; `m := edgeSet(raw)` (a conversion); a function-local `type local map[string]bool`; `for k := range outer["x"]` on a nested map. Measured at round 4: all six returned `[]`. |
| R4-2 | 4 | major | fixed@7fc7df6 (design amended@7fc7df6 — see the Design Amendment rows in the decisions log) | `go vet` each struct-field red-case fixture as a standalone package — `TestCheck_MapStructFieldRangeFlagged` (`internal/detguard/detguard_test.go:216`) and `TestCheck_NamedMapTypeStructFieldRangeFlagged` (`:257`) — both must compile. Then a scratch case whose fixture ranges over a map struct field through a **selector** (`func f(v T) { for k := range v.m { _ = k } }`) must be flagged. Measured at round 4: both fixtures fail `go vet` with `undefined: m` (`vet: pkg/a.go:10:17: undefined: m`), and the selector shape returns `[]`. |
| R4-3 | 4 | minor | fixed@7fc7df6 (design amended@7fc7df6 — see the Design Amendment rows in the decisions log) | Replace `collectNamedMapTypes`'s fixpoint loop (`internal/detguard/detguard.go:150-162`) with a single pass over `decls`, then `go test -count=1 ./internal/detguard/ ./internal/maze/ ./internal/hexgrid/` — at least one must be RED. Measured at round 4: all three GREEN, the mutant confirmed to build and vet first. Closing the row also requires a red case for `type edgeAlias edgeSet` with the alias declared **before** its target, and the doc comment's "through one level of naming" reconciled with the transitive resolution the loop actually performs. |
| R4-4 | 4 | minor | fixed@7fc7df6 (design amended@7fc7df6 — see the Design Amendment rows in the decisions log) | `grep -rn 'pgregory.net/rapid' internal/maze/` must return a hit in the face-agreement scenario, and the AC3 test must sweep more than one world seed and reach coordinates far from the origin. Measured at round 4: **no hit anywhere** in `internal/maze`, and `TestCell_FaceAgreementOverAMultiChunkRegion` runs one seed (20260912) over q,r ∈ [-20,20] — negative in both axes but adjacent to the origin. The missing scenario written out deterministically (5 seeds × 5 far bases at ±(1<<20), ±(1<<28) and ±2147483600, ±3 cells each) PASSES on the clean tree, so this is coverage and not a live defect, and it is a live instrument rather than a vacuous one: under the `borderKey` canonical-order mutant it goes RED (`face disagreement at {1048575 1048575} dir 1: 2 vs 1`). |
| R4-5 | 4 | minor | fixed@7fc7df6 — the echo and a decisions-log entry now carry both self-review raises | `grep -n 'cap: ' ai-docs/plans/2026-09-12-world-generation-hex-chunk-generator.progress.md` — a self-review round cap above the charter value of 3 must carry the `cap: N (was M)` echo `.claude/skills/task/SKILL.md:208` mandates plus a Decisions-log entry recording the owner's words, exactly as the four design-review raises at lines 43, 71, 74 and 88 do. Measured at round 4: four echoes, every one of them design-review (`cap: 4 (was 3)`, `cap: 5 (was 4)`, `cap: 6 (was 5)`, `cap: 7 (was 6)`); the sole self-review statement is `## Next action`'s parenthetical "(the owner raised the cap to 4)", with no echo and no log entry. |
| R4-6 | 4 | nit | accepted@4 — below severity floor | Eleven near-identical map-shape red cases (`internal/detguard/detguard_test.go:129-283`) are separate top-level functions, where the same file's `TestCheck_BannedImportsFlagged` (`:29`) already uses a `[]struct` table with `t.Run`; AGENTS.md § *Go Test Conventions* makes table-driven the default shape for a case set past two. Counter-argument recorded so it is not re-litigated: each case's per-shape doc comment carries rationale a table's `name` field would compress. Re-raise only if the set grows again. |
| R4-7 | 4 | — | accepted@4 — not a defect | `pgregory.net/rapid` is a pre-existing direct dependency the design records as measured (design lines 597-598), `go.mod`/`go.sum` are unchanged across `c07f8c1..HEAD` (`git diff --stat` empty), and its timestamped `.fail` artefacts are gitignored — `git check-ignore -v` names `.gitignore:46` (`**/testdata/rapid/`). Re-raise only if rapid becomes a new or version-moved dependency. |
| R4-8 | 4 | — | accepted@4 — not a defect | Two production mutations no earlier round had run, both caught: `hexgrid.FaceOf`'s canonicalisation flipped (`d < opp` → `d > opp`) reds `TestFaceOf_CanonicalDirectionIsAlwaysTheEarlierOfThePair`; `borderKey` stripped of `canonicalOrder` reds the face-agreement sweep. Recorded because the first result looks like a hole and is not: the flipped `FaceOf` leaves `internal/maze` GREEN, because a consistently-swapped representative is still canonical, so agreement holds either way — `internal/hexgrid` is where that mutant must be caught and is. |
| R4-9 | 4 | — | accepted@4 — not a defect | R1-11 re-verified at HEAD by enumerating all 36 `range` statements in the three packages' non-test files: the five that range over a call result (`cycles.go:24`, `algorithms.go:103,164,182,225`) all range over **slice** returns, and no map is ranged over anywhere. R4-1 is therefore about the guard's reach, not a live determinism defect — while also being the reason those five sites are decided "not a map" by default rather than by knowing. |

## Files touched

- `internal/detguard/` (doc.go, detguard.go, main_test.go, detguard_test.go)
- `internal/hexgrid/` (doc.go, coord.go, face.go, chunk.go, main_test.go, coord_test.go, chunk_test.go, guards_test.go)
- `Makefile` — net **unchanged** on this branch: the `test-arch` target, its three variables and their comment block were added by the gate commit and removed again by subtask 11, restoring the file to its pre-gate state
- `.github/workflows/ci.yml` — the only surviving line of the gate commit is the `'**/*.golden'` entry in the `go` paths filter, with a comment rewritten to its own reason. The `test-arch` job and the sibling-cluster comment's count bumps are gone
- `AGENTS.md`, `ai-docs/claude-tools-hierarchy.md`, `ai-docs/go-test-conventions.md`, `.claude/skills/pr-ci-failed/SKILL.md`, `.claude/skills/main-ci-failed/SKILL.md`, `.claude/skills/dependabot-pr/reference.md` — all net **unchanged** on this branch: the propagation the added CI job and the added `arch` failure class owed was reverted wholesale by subtask 11
- `docs/DESIGN.md` (§2.2.2 records the decision, §16.2 strikes the open question), `ai-docs/key-decisions.md` (KD-37..KD-40, with KD-37 reconciled in place by subtask 13)
- `internal/maze/` (doc.go, seed.go, draw.go, params.go, algorithm.go, chunkgraph.go, island.go, algorithms.go, cycles.go, portal.go, generate.go, prefab.go, main_test.go, seed_test.go, draw_test.go, params_test.go, algorithm_test.go, chunkgraph_test.go, island_test.go, algorithms_test.go, cycles_test.go, portal_test.go, generate_test.go, prefab_test.go, property_test.go, golden_test.go, guards_test.go, bench_test.go, testdata/derive.golden, testdata/cells.golden)

## Self-Review (Round 1)

**Verdict:** REJECT

| # | File:line | Severity | Finding | Status |
|---|-----------|----------|---------|--------|
| 1 | internal/maze/seed_test.go:104-116 | major | AC16's sharp form is not asserted anywhere. `TestCellSeed_FunctionOfWorldSeedAndCoordAloneNotChunkDimensions` calls `cellSeed(seed, c)` twice — a signature that takes no `Dims` — so it cannot fail for the reason its name gives, and its own comment concedes it "documents" rather than asserts. The design's named scenario (§ API sketch and § Test Design → *Derivation*: "two generators differing only in chunk dimensions yielding **the same** cell seed and **different** faces") is absent. Measured: with `internal/maze/generate.go:123` changed to `seed = cellSeed(gen.seed, coord) ^ uint64(dims.Cols-16)` — a cell seed that is a function of the chunk dimensions, which AC16 forbids — the mutant builds, vets, and the whole suite reports `ok github.com/maratik123/lab-game/internal/maze 9.913s`. Both goldens are blind to it: `derive.golden` pins `cellSeed` below `Cell`, and `cells.golden` is minted at 16x16 where the mutant is the identity. Fix: add the two-generator scenario the design names (same seed and params, two different `Dims`; assert equal `Cell.Seed` and unequal `Cell.Faces`) and drop or rename the tautological test. | ✅ Fixed |
| 2 | internal/detguard/detguard.go:111-155 | major | The map-range predicate is blind to a map arriving as a **function parameter** — the shape every map in `internal/maze` travels in (`islands map[int]bool` through eight functions, `open edgeSet`, `portals`). `collectMapTypedIdents` scans only `*ast.ValueSpec` and `*ast.AssignStmt`, so a parameter, a struct field, or a function result is never flagged. Design § Risks → *Map iteration order reaching a drawn value* states the mitigation as "a structural guard forbids ranging over a map anywhere in either package's non-test files, **with no carve-out**"; this is a carve-out over the primary case. Measured, three cells: adding `for k := range islands { _ = k }` to `nonIslandCells` in `internal/maze/algorithms.go` builds and leaves `TestGuard_DeterminismPredicates` GREEN; the same range over a local `make(map[int]bool)` in the same place goes RED (`detguard.Check(maze) problems: [.../algorithms.go: ranges over a map]`); the unmutated tree is GREEN. No `go/types` dependency is needed — a map-typed parameter, receiver, result and struct field are all decidable from the `FuncDecl`/`StructType` AST the guard already walks. | ✅ Fixed |
| 3 | internal/detguard/detguard_test.go:56-67 | major | `TestCheck_RandV2NonConstructorFlagged` is a cosmetic red case for the predicate the design calls the load-bearing one (§ Test Design → *Guards*: "no identifier from `math/rand/v2` other than the ChaCha8 constructor — zero occurrences permitted"). Its fixture is `var _ = rand.Float64()`, and `Float64` is also a key of `bannedDecimalSelectors`, which matches on selector name alone with no receiver check; after `sort.Strings` the decimal message sorts first, so the assertion `strings.Contains(problems[0], "Float64")` is satisfied by the *wrong* predicate. Measured: with the `math/rand/v2` branch made dead (`if randV2Local == "\x00impossible-local-name" {` — builds, identifier still used), the whole package reports `ok github.com/maratik123/lab-game/internal/detguard 0.003s`. Fix: give the fixture a `math/rand/v2` identifier that is not a banned decimal selector (e.g. `rand.N`, `rand.Uint64`) and assert on the `math/rand/v2` message, not on a bare substring shared with another predicate. | ✅ Fixed |
| 4 | internal/maze/prefab.go:8 | major | DOC-4 review-judged half — comments that point the reader elsewhere by a bare unqualified name. Eight sites, all passing `make comment-refs` because none carries a lexical class: `internal/maze/prefab.go:8` "per the design's own ordering"; `internal/maze/island.go:65` "the chunk-induced subgraph the design requires"; `internal/maze/algorithms.go:122` "this design's one call site"; `internal/maze/params.go:37` "the very criterion that asks"; `internal/maze/algorithm.go:19` "the spec's own lower_snake_case names"; `internal/maze/generate_test.go:62` "per the design's own Determinism section"; `internal/maze/property_test.go:128` "the instrument the design fixes"; `internal/detguard/detguard_test.go:101` "the blind shape the design calls for". `ai-docs/doc-convention.md` § DOC-4 → *What the gate decides, and what review decides* bans this and routes it here; GO note G24 deferred exactly this class to Step 9, which routed it to self-review. Fix: state the property self-containedly and delete the pointer (the design and the spec keep the citation; the comment does not). | ✅ Fixed |
| 5 | internal/detguard/detguard_test.go:100-106 | minor | The test's own doc comment claims the fixture carries "no bare float64 declaration anywhere", but the fixture declares `func f(d decimal.Decimal) float64`, which the float-type predicate matches on its own. The case still discriminates — with the decimal predicate dead it goes RED on `want one naming Float64`, because the float-type message spells `float64` in lower case — but that is a capitalisation accident, not the design of the fixture, and the comment misdescribes the instrument. Fix: remove `float64` from the fixture (return `decimal.Decimal`, or discard the accessor's results as `g` already does) so the claim is true and the discrimination is structural. | ✅ Fixed |
| 6 | internal/maze/generate_test.go:211-223 | minor | Design § Test Design → *Locality — AC12* names three scenarios; the third — "for two coordinates at the same position **within** their respective chunks, the sets agree **after subtracting each coordinate's own chunk** — the set of *relative* chunk offsets is the invariant" — is not implemented. `TestChunksConsulted_SizeInvariantWithDistanceFromOrigin` asserts only `len(near) != len(far)`, which a set of the right size but the wrong shape would pass. | ✅ Fixed |
| 7 | internal/hexgrid/chunk.go:43-45 | minor | `Dims.Contains` has no discriminating test. `chunk_test.go:43` calls `refDims.Contains(ch, c)` with `ch := refDims.ChunkOf(c)`, and `Contains` is defined as `d.ChunkOf(c) == ch` — so the assertion is `ChunkOf(c) == ChunkOf(c)` and can never be false. Measured: with the body replaced by `return true`, `go test -count=1 ./internal/hexgrid/ ./internal/maze/` stays GREEN. The design § Test Design → *Topology* names the missing half — "the chunks' cell sets recover the region exactly". (The companion `count > 16*16` assertion in that test **is** discriminating: truncating division would put 31x31 region cells in the origin chunk.) | ✅ Fixed |

**Nits (6, no rows): `internal/maze/golden_test.go`, `internal/maze/chunkgraph_test.go`, `internal/maze/cycles_test.go`, `internal/maze/draw.go`, `internal/maze/seed_test.go`, `internal/hexgrid/chunk.go`** — enumerated in register row R1-8, entered as `accepted@1 — below severity floor`.

### What was checked

- **Gates, all re-run at HEAD:** `go vet ./...` GREEN · `golangci-lint run` GREEN · `make comment-refs` GREEN · `make file-limits` GREEN · `make import-guard` GREEN · `go test -count=1 ./internal/maze/ ./internal/hexgrid/ ./internal/detguard/` GREEN · `go test -race -count=1` on the same three packages GREEN (28.6s, no `WARNING: DATA RACE`).
- **All 19 AC verifiers from the AC Status table re-run against the shipped tree: AC1–AC16 and AC19 PASS.** AC17: `go test -run '^$' -bench 'BenchmarkCell$' -benchtime=10x ./internal/maze/` → `674752 ns/op`, and the no-threshold negative re-run with a control (`grep -rnE '\-bench|benchtime' Makefile .github/workflows/ci.yml .githooks/ ai-docs/scripts/` → exit 1; the same pattern fires on a constructed gate line). AC18: the conjunction sweep re-run over 104 tracked `*.md` outside this run's plan files and the two history logs → no live hit, with control A (a constructed open line) firing and control B (a constructed decided line) clean; `docs/DESIGN.md:482` carries the clause struck and marked `закрыт`.
- **Design conformance:** every file of the § Decomposition file column is present; the amendment's subtasks 11, 13 and 14 are all landed — subtask 11's delta acceptance re-verified per path at HEAD with a discriminating control (register R1-10), subtask 13's KD-37 re-anchoring present at `ai-docs/key-decisions.md:101`, subtask 14's `internal/maze/doc.go:9-10` now reading "across processes and Go versions" with no architecture clause. **No Spec or Design Amendment trigger:** every finding above is a code or test fix against what the design already says, not a contradiction of it.
- **GO-notes round-trip:** all 25 rows carry a route. G1–G23 and G25 are `folded` and spot-verified in the design at the pinned revisions (G5 "zero permitted, never exactly one" at § Decomposition row 1 and § Test Design → *Guards*; G13 subtask 14's `Depends on` now `3`; G20 the § Test Design twin naming no command form; G22 `Algorithm.String()` recorded in § API sketch). G24 is the `deferred to Step 9` row and is finding #4 above.
- **Safety:** no `panic(` / `log.Fatal*` / `log.Panic*` in any non-test file of the three packages, so `ai-docs/panic-index.md` is correctly unchanged. Every `//nolint` carries a specific linter and a reason. No `_ = err`; no `==` comparison of a wrapped error; `New` wraps with `%w`. No goroutine is started by production code in this diff. `Generator` holds no memo and is asserted concurrency-safe under `-race`.
- **Domain invariants:** no balance column written, no posting, no basis document, no persisted enum, no schema change, no outbound send path, no secret. No `time.Now()`, no unseeded `math/rand`, no map ranging in production (register R1-11), no floating-point type. No balance constant compiled into Go — every share and the bias arrive as `decimal.Decimal` fields of `Params`, which fixes no configuration key name.
- **Mutation-tested rather than read:** finding 1 (the cell seed's Dims-independence), finding 2 (the map-range predicate, with a three-cell control matrix), finding 3 (the `math/rand/v2` red case, with the mutant confirmed to build first), finding 7 (`Dims.Contains`), and the decimal-accessor red case — which **passed**: with its predicate dead it goes RED, so it is a live instrument. Every mutation was applied and restored through a `cp` backup under `tmp/`, never `git checkout`/`git restore`, with `git status --porcelain` re-verified empty after each.
- **File sizes:** largest non-test file `internal/maze/algorithms.go` at 321 raw lines, largest test file `internal/maze/generate_test.go` at 268 — both below the 500 soft band. Coverage ratchet raised 90.48 → 91.05.

## Self-Review (Round 2)

**Verdict:** REJECT

| # | File:line | Severity | Finding | Status |
|---|-----------|----------|---------|--------|
| 1 | internal/detguard/detguard.go:135-138 | major | The round-1 fix to the map-range predicate has **no red case anywhere**, so the very defect R1-2 named can regress with the whole tree green. Design § Decomposition row 1 requires "each predicate paired with its own scratch red case carrying the *blind* shape", and its parenthetical models exactly this obligation ("a method-result float, not only a declaration") — for the map ban the blind shape is now known to be the parameter, and `detguard_test.go` still exercises only a local `make(map…)` (`TestCheck_MapRangeFlagged`) and an inline composite literal (`TestCheck_DirectMapCompositeLitRangeFlagged`). Measured: with the `case *ast.FuncType:` arm reduced to `noteMapFields(out, node.Results)` — dropping the parameter shape — the mutant builds and vets, `go test -count=1 ./internal/detguard/` reports `ok … 0.002s`, and `go test -count=1 -run '^TestGuard_DeterminismPredicates$' ./internal/maze/ ./internal/hexgrid/` reports `ok` for both. Fix: add a scratch red case whose fixture ranges over a map arriving as a **function parameter** (and, since the same commit added them, one each for a map-typed receiver, named result and struct field). | ✅ Fixed |
| 2 | internal/maze/generate.go:35 | minor | Ninth site of the DOC-4 review-judged class, and **round 1's own verifying command was blind to it**: the pattern `the design\|the spec\|the criterion` cannot see `// see PrefabClaimer.`, which is the banned shape named verbatim in `ai-docs/doc-convention.md` § DOC-4 ("a bare unqualified name used as a pointer — \"see such-and-such\""). The clause carries no fact the sentence has not already stated ("A prefab-claimed cell defers every interior face and carries no seed"), so § DOC-4's "where a sentence exists only to carry the reference, the sentence goes with it" applies to the clause. Rated `minor` rather than `major`, and the reasoning is recorded rather than assumed: unlike round 1's eight sites, which pointed outward at a document that rots independently, this one names a **same-package symbol that is the contract** — the case § DOC-4's exemption keeps — so only the signpost wording is at fault, not the reference. Measured with a control: the widened pattern returns this one line, and fires on a constructed pointer line. | ✅ Fixed |
| 3 | ai-docs/plans/2026-09-12-world-generation-hex-chunk-generator.progress.md AC16 row | minor | The AC16 row's recorded verifying command no longer reaches the test that discharges the clause its prose claims. Round 1's fix deleted `TestCellSeed_FunctionOfWorldSeedAndCoordAloneNotChunkDimensions` and added `TestCell_SeedIsIndependentOfChunkDimensions`, which `-run '^TestCellSeed_'` does not match. Measured: `go test -count=1 -run '^TestCellSeed_' -v ./internal/maze/` runs exactly one test, `TestCellSeed_PairwiseDistinctOverACoordinateTable`, while the row still reads "independent of the chunk dimensions". A recorded verifier is a claim, and this one is now false. (AC12's command still reaches its new test, but that row's prose likewise does not mention the relative-offset scenario.) Fix: re-sync both rows' commands and prose. Not an amendment trigger — the spec and design are unchanged and correct; only the progress file's own record drifted. | ✅ Fixed |

**Nits: none new.** R1-8's six remain `accepted@1 — below severity floor`, unchanged by this round's diff.

### What was checked

- **Round-1 closure, per register row, by running each row's own verifying command — all seven confirmed:** R1-1 RED (`--- FAIL: TestCell_SeedIsIndependentOfChunkDimensions` … "cell seed for {-3 -3} is 6683376823882574717 under {16 16} and 11763367249826976897 under {12 12}") · R1-2 RED (`[.../algorithms.go: ranges over a map]`) · R1-3 RED (`--- FAIL: TestCheck_RandV2NonConstructorFlagged`) · R1-4 narrow pattern clean, all eight document pointers gone · R1-5 the blind-shape fixture is now free of `float64` · R1-6 `TestChunksConsulted_RelativeOffsetsAgreeAtTheSamePositionInAChunk` PASS · R1-7 RED with twelve named `Contains(...) = true, want false` lines. Every mutant was confirmed to **build and vet** before its verdict was read.
- **Gates re-run at HEAD:** `go vet ./...` GREEN · `golangci-lint run` GREEN (`0 issues.`) · `golangci-lint fmt -d` 0 diff lines · `make comment-refs` GREEN · `make file-limits` GREEN · `make import-guard` GREEN · `go test -count=1` GREEN on all three packages · `go test -race -count=1` GREEN (28.8s, no `WARNING: DATA RACE`). One false alarm, recorded so a later round does not chase it: the first `golangci-lint run` came back RED on `tmp/sr2-ctl.go`, a scratch control file this review had written — `golangci-lint` reads `tmp/` even though git ignores it. Removing the scratch restored `0 issues.`; nothing in the diff was implicated.
- **All 19 AC verifiers re-run at HEAD: AC1–AC16 and AC19 PASS**, AC17 and AC18 unchanged since round 1 and re-confirmed there. AC16 passes but on a stale locator — finding #3.
- **The fix diff (`f66d42e..HEAD`) reviewed for newly introduced defects.** The three fix commits touch `internal/detguard` (predicate + two red cases), `internal/maze` and `internal/hexgrid` (three new tests, five comment rewrites, one test deleted). The five DOC-4 rewrites each now state the property self-containedly and introduce no new pointer. `TestCell_SeedIsIndependentOfChunkDimensions` carries its own anti-vacuity assertion (`facesDiffer`), and `TestContains_RejectsAChunkThatDoesNotHoldTheCell` asserts both directions. `ai-docs/coverage-ratchet.txt` rises 91.05 → 91.07.
- **Two properties of the new guard code examined and ruled not defects** (register R2-4, R2-5): the name-keyed file-local namespace can over-report across two functions that reuse a name, which fails closed and has no current instance; and the decimal blind-shape red case was re-probed against its new, reduced fixture and still goes RED when its predicate is dead — the reduction removed the `float64` contamination without costing discrimination.
- **Unchanged since round 1 and not re-litigated:** the domain-invariant sweep, the production panic audit, the subtask-11 delta acceptance, the GO-notes round trip, and file sizes — this round's diff touches none of them. No Spec or Design Amendment trigger: every finding is a fix against what the design already says.

## Self-Review (Round 3)

**Verdict:** REJECT

| # | File:line | Severity | Finding | Status |
|---|-----------|----------|---------|--------|
| 1 | internal/detguard/detguard.go:160-164 | major | The map-range predicate is blind to a **named map type** — the type `internal/maze` actually threads its edge sets through. `noteIfMapType` matches only `*ast.MapType`, so `type edgeSet map[edgeKey]bool` (`internal/maze/algorithms.go:18`) types an identifier as `*ast.Ident` and is never registered, in any of the four positions the round-2 fix added: `open edgeSet` as a parameter (`cycles.go:13`, `algorithms.go:70`), as a local (`generate.go:71`), as a composite literal `edgeSet{}` (`algorithms.go:92,125,153,205,251`), and as the receiver of `add`/`has` (`algorithms.go:20,24`). Design § Risks → *Map iteration order reaching a drawn value* (design line 523-524) states the mitigation as "a structural guard forbids ranging over a map anywhere in either package's non-test files, **with no carve-out** — which stays a whole-package invariant now that no legitimate caller wants one"; § Test Design → *Guards* (design line 826) repeats it as "And no ranging over a map"; § Decomposition row 1 (design line 448) requires "each predicate paired with its own scratch red case carrying the *blind* shape". Measured, three cells, each mutant confirmed to build and vet first: `for k := range open { _ = k }` in `addExtraPassages` leaves `TestGuard_DeterminismPredicates` GREEN; the same range over `islands` in the same place goes RED (`detguard.Check(maze) problems: [.../cycles.go: ranges over a map]`); the unmutated tree is GREEN. The collector's own doc comment (`detguard.go:105-114`) presents its coverage as closed — "Four shapes reach an identifier: …" — with no mention of the named-type gap, which is part of why two rounds of parameter-shape fixes left it standing. Fix: resolve a package-declared map type name (an `*ast.TypeSpec` whose type is an `*ast.MapType`, collected across the package's files since `edgeSet` is declared in `algorithms.go` and ranged from `cycles.go`/`generate.go`) and treat identifiers of that type as map-typed in all of the shapes the collector already walks, including a composite literal and a range over one; add the scratch red case carrying this blind shape; and correct the doc comment's enumeration. Not an amendment trigger — the design already says "no carve-out"; it is the code that does not meet it. | ✅ Fixed |
| 2 | ai-docs/plans/2026-09-12-world-generation-hex-chunk-generator.progress.md `## Review register` | major | **The register gate is blind to this file's entire register, and it hard-fails every commit that stages the file.** `ai-docs/templates/progress-format.md:78` fixes the id as `R<round>-<n>` ("**id** `R<round>-<n>` is permanent"); this run uses a single global sequence, `R1-1`…`R2-5`. `ai-docs/scripts/check-review-register.sh` joins a round table's finding `n` of round `N` to the register row whose id matches `^[A-Za-z]*N-n$` and **skips** any id that does not parse, so `R1-1` never enters the comparison at all. Measured at HEAD, on the committed file: exit 1, with ten lines `round <N> finding <n> is marked Fixed and has no register row` covering every numbered finding of rounds 1 and 2 — the same result on `git show HEAD:<path>`, so it predates this round's edits. Cause isolated with a three-cell control on a minimal file: `R1-1` + `fixed@abc1234` → FAIL "has no register row"; `R1-1` + `fixed@abc1234` → PASS; `R1-1` + `open` → the gate's own intended failure (`R1-1 reads "open" while round 1 marks finding 1 Fixed`). Two consequences, and the second is the one that bites now: the gate the self-review contract calls the durable cross-round-state protection has never been able to reach this run (it is not partial protection — every row is skipped, which is Pattern 3's shape exactly), and the `PreToolUse` hook at `.claude/settings.json:72` runs that script over every staged `*.progress.md` and exits 2, so the next `git commit` staging this file is refused. Fix: re-label the legacy `SR-<n>` rows to the canonical `R<round>-<n>` (rounds 1 and 2's numbered findings are R1-1..R1-7 → R1-1..R1-7 and R2-1/R2-3 → R2-1/R2-2; the accepted-class rows R1-8..R1-11, R2-4, R2-5 carry no round-table finding number and may keep a free-form id, which the script skips by design), update every in-file citation of a renamed id, and re-run the command until it exits 0. Rows R3-1 onward already use the canonical form. Not an amendment trigger — the spec and design say nothing about the register; the canonical template already does. | ✅ Fixed |
| 3 | ai-docs/plans/2026-09-12-world-generation-hex-chunk-generator.progress.md `## Next action` | minor | The `## Next action` block is stale by two rounds and contradicts two other sections of its own file. It reads "**Do this immediately:** Group C is subtask 14 alone — strike the `and CPU architectures` clause from `internal/maze/doc.go` … After it lands, AC2 flips from PARTIAL to TESTED — it is PARTIAL today for that one reason and no other." Measured: subtask 14 is `[x]` in `## Subtasks`, the Decisions log's own `Step 8 (subtask 14, Group C)` entry records it landed, `internal/maze/doc.go:9` already reads "the same result across processes and Go versions" with no architecture clause, and the `## AC Status` table records AC2 as PASS. The file's header instructs a re-entering agent to "Read THIS FIRST → ready to continue. No need to re-read the codebase", so this block is exactly what a post-compaction re-entry acts on — it would send that agent to redo landed work. Same class as round 2's finding #3: a recorded claim about this run's own state that is now false. Not an amendment trigger — the spec and design are unchanged and correct. Fix: re-point the block at the current step. | ✅ Fixed |

**Nits (1, no row): `internal/detguard/detguard_test.go`** — enumerated in register row R3-4, entered as `accepted@3 — below severity floor`.

### What was checked

- **Spawn prompt:** within the closed list — invocation line, `Spec:`, `Design:`, `Progress:`, and the range `c07f8c1..HEAD`. No `PROMPT-CONTAMINATION`.
- **Diff window:** `c07f8c1..HEAD` is this branch's own contribution — `c07f8c1` is the `origin/main` tip merged in at `af9fb63`, so the range excludes main's item-machine/testpg work and carries 54 files, all of them this task's.
- **Gates, all re-run at HEAD:** `go vet ./...` GREEN · `golangci-lint run` GREEN · `golangci-lint fmt -d` 0 diff lines · `make comment-refs` GREEN · `make file-limits` GREEN · `make import-guard` GREEN · `go test -count=1` GREEN on all three packages · `go test -race -count=1` GREEN (29.6s, 0 `WARNING: DATA RACE`).
- **Round-2 closure, per register row, by running each row's own verifying command — both confirmed.** R2-1: the `case *ast.FuncType:` arm reduced to `noteMapFields(out, node.Results)` alone turns `TestCheck_MapParameterRangeFlagged` RED. Not stopping there, a **per-arm matrix** was run independently rather than read off the round-2 decisions log: dropping the receiver arm reds `TestCheck_MapReceiverRangeFlagged`, the params arm reds `TestCheck_MapParameterRangeFlagged`, the results arm reds `TestCheck_MapNamedResultRangeFlagged`, the struct-field arm reds `TestCheck_MapStructFieldRangeFlagged` — four for four, each mutant confirmed to build and vet first, each restore verified byte-identical. Two of the four mutations initially failed to apply (indentation mismatch) and reported GREEN; that green was discarded as an instrument failure rather than recorded as evidence, and the mutations were re-run. R2-3: `-run '^TestCell_SeedIsIndependentOfChunkDimensions$|^TestCellSeed_'` now reaches both the discharging test and the pairwise one; AC12's command reaches all three `TestChunksConsulted_` tests including the relative-offset one.
- **Round-2 finding #2 (DOC-4) re-verified:** the widened pattern `(see |per the |§|\.md|\bAC[0-9]|\bKD-[0-9]|the design|the spec|the criterion)` over the three packages' `*.go`, filtered to comment lines, returns **0 hits**, with a constructed pointer line firing as the control. `// see PrefabClaimer.` is gone from `generate.go:34`.
- **All 19 AC verifiers re-run against the shipped tree — AC1–AC19 PASS**, each with a non-zero pass count and zero FAIL/SKIP. AC17: `674752`→`712345 ns/op` at `-benchtime=10x`, and the no-threshold negative re-run with a control (`grep -rnE '\-bench|benchtime' Makefile .github/workflows/ci.yml .githooks/ ai-docs/scripts/` exits 1 with 0 hits; the same pattern fires on a constructed gate line). AC18: the conjunction sweep was **re-built** this round after the first attempt's regex was refused by the local `grep` implementation as exceeding its complexity limit — a silent 0-hit that would have read as clean; the staged replacement reports 28 → 14 → 0 over 106 tracked `*.md`, with control A (a constructed open line) firing at every stage and control B (a constructed decided line) clean at the last. `docs/DESIGN.md:482` carries the clause struck and marked `закрыт`.
- **Five production mutations against the ACs' own mechanisms, each confirmed to build and vet first, each restored byte-identical with `git status --porcelain` empty afterwards — all five RED:** portals allowed to draw a count of zero → 6 tests RED including `TestSelectPortals_OneOrTwoCappedByCandidates` and both connectivity sweeps (AC8, AC9); `borderKey` stripped of its canonical chunk order → 4 RED including `TestCell_FaceAgreementOverAMultiChunkRegion` (AC3); the extra-passage target forced to zero → `TestAddExtraPassages_PositiveShareAddsACycle` (AC10); island selection forced to select none → 5 RED including `TestIslandShare_AchievedShareWithinTolerance` (AC11); the Wilson dispatch swapped to the backtracker → `TestCellsGolden` (AC19, register R1-19).
- **Determinism, independently of the guard:** every `range` in the three packages' non-test files enumerated by hand — 26 statements, every one over a slice, an array or a fixed direction table, none over a map. So finding #1 is about the guard's reach, not a live defect, exactly as R1-2 and R1-11 recorded for the parameter shape.
- **Safety and style:** the panic grep over the three packages' non-test files returns nothing (the only two `panic(` are in `golden_test.go`, which the recipe skips), so `ai-docs/panic-index.md` is correctly unchanged. Eight `//nolint` directives, each naming `gosec` and stating the G115 reason. Every exported item of all three packages carries a doc comment opening with its own name (`go doc -all` read per package), each package has a package comment, `Generator` states its concurrency safety. Largest non-test file 320 lines (`algorithms.go`), largest test file 339 (`generate_test.go`) — both inside the 500 soft band. `internal/*` only, no new dependency.
- **Progress-file re-entry fields:** `current_step`, `last_passed_gate` and `entry_args` are present; `parent_skill` is optional per the canonical template and correctly omitted, `/task` being the parent flow. Content not reviewed, per the agent's own instruction 2.
- **Register discipline:** the `fixed@` rows of rounds 1–2 whose subjects this round re-examined were re-opened on nothing; the two `accepted@1`/`accepted@2` rows touching the map guard (R1-11, R2-4) were read before finding #1 was minted, and it restates neither — R1-11 is about live ranges, R2-4 about the collector's name-keyed namespace, and this is about the type spelling the collector matches. New id, not a re-open. This round's own rows use the canonical `R<round>-<n>` id rather than continuing the `SR-<n>` sequence, for the reason finding #2 states.
- **The register gate itself was run rather than assumed.** The self-review contract states that a `PreToolUse` hook "refuses a commit in which a round table marks a finding `✅ Fixed` while its register row still reads `open`", and adds "do not spend a finding on it; it cannot reach you any more." That premise was executed, per this project's own rule that a green instrument is a claim about the instrument: it does not reach this file — finding #2.
- **Unchanged since round 1 and not re-litigated:** the domain-invariant sweep, the subtask-11 gate-removal delta acceptance, and the GO-notes round trip — this round's diff touches none of them.

## Self-Review (Round 4)

**Verdict:** REJECT

| # | File:line | Severity | Finding | Status |
|---|-----------|----------|---------|--------|
| 1 | internal/detguard/detguard.go:243-261 | major | The map-range predicate is blind to a map arriving as a **function call's result** — the only way the live `islands` map enters `internal/maze/generate.go`. `noteIfMapExpr` accepts exactly two right-hand sides, a composite literal and a `make(…)` call, so `islands := selectIslands(fabricGraph, …)` (`internal/maze/generate.go:81`, returning `map[int]bool`) types nothing in that file's namespace, and no other shape contributes the name `islands` there. Design line 523-524 states the mitigation as "a structural guard forbids ranging over a map anywhere in either package's non-test files, **with no carve-out**"; § Decomposition row 1 (design line 448) requires "each predicate paired with its own scratch red case carrying the *blind* shape". Measured, three cells, each mutant confirmed to build and vet first: `for k := range islands { _ = k }` after that line leaves `TestGuard_DeterminismPredicates` GREEN; the same range over `open` in the same place goes RED (`[.../generate.go: ranges over a map]`); clean tree GREEN. Five further shapes measured blind in scratch packages, each returning `[]`: `for k := range g()` (a call result ranged directly), `for k := range v.m` (a map struct field through a selector), `m := edgeSet(raw)` (a conversion to a named map type), a function-local `type local map[string]bool`, and `for k := range outer["x"]` (a nested map's value). The rewritten doc comment (`detguard.go:179-191`) still presents its enumeration as closed — "Four shapes reach an identifier: …" — which is the same property round 3's finding #1 named as the reason two rounds of parameter-shape fixes left the named-type gap standing; that round's fix clause "correct the doc comment's enumeration" was discharged only along the named-type axis. Fix: collect the package's function declarations whose result type is a map (literal or named) and treat `x := f(…)` as map-typed, match `*ast.SelectorExpr` against the field names `noteMapFields` already collects, treat a conversion to a collected named map type like `make`, collect `*ast.TypeSpec` inside function bodies, and state the residue that remains. All of it is decidable from the same package-wide AST the round-3 fix already assembles. **Not an amendment trigger** on that route — the design already says "no carve-out"; it is the code that does not meet it. Two other routes *would* be: adopting `go/types`/`go/packages` needs a dependency row in the design, and accepting the residue needs design line 523-524 to record it. | ✅ Fixed (design amended) |
| 2 | internal/detguard/detguard_test.go:214-219 | major | The struct-field arm's two red cases exercise a shape that cannot occur, while the struct-field shape that **can** occur is blind — so the arm as shipped protects nothing. Both fixtures range over a bare `m` that no scope declares, relying on the collector's file-scope name match to conflate the struct field's name with the ranged identifier: `TestCheck_MapStructFieldRangeFlagged` (`:216`) and `TestCheck_NamedMapTypeStructFieldRangeFlagged` (`:257`). Measured: each fixture written out as a standalone package fails `go vet` with `vet: pkg/a.go:10:17: undefined: m`, so neither is compilable Go and `detguard.Check` only ever runs against compilable packages. Measured: the compilable spelling, `func f(v T) { for k := range v.m { _ = k } }`, returns `[]` — `isMapRange` has no `*ast.SelectorExpr` arm. This is **not** R3-4 re-raised: that row accepted the *receiver* arm as harmless over-coverage precisely because a receiver base type must be a defined type, so the shape can occur in no compilable code; here the shape can occur and does not red. And it is not hypothetical — round 1's own memo mutation (Decisions log, subtask 9 gap fix round 2) added `memoOpen edgeSet` as a `Generator` field, which is exactly the anticipated extension a future `for … range gen.memoOpen` would read. Design line 448 asks the red case to carry the *blind* shape; for the struct-field position that is the selector, and neither case carries it. Fix: give `isMapRange` a selector arm resolved against the collected field names, and re-write both fixtures as compilable Go. | ✅ Fixed (design amended) |
| 3 | internal/detguard/detguard.go:150-162 | minor | `collectNamedMapTypes`'s fixpoint loop has no test, and the capability it exists for has no red case. Measured: reduced to a single pass over `decls` (mutant confirmed to build and vet first), `go test -count=1 ./internal/detguard/ ./internal/maze/ ./internal/hexgrid/` reports `ok` for all three. The six red cases the fix added all declare the map type directly, so none reaches the alias resolution the loop is there for; the shape the doc comment names — `type edgeAlias edgeSet` with the alias declared **before** its target, the case the comment calls out as "file-order independent" — is absent. The capability itself does work (measured in a scratch probe: flagged with the target declared first and with it declared second), so this is coverage, not a defect in the resolution. Separately, the comment says the resolution goes "through one level of naming" while the fixpoint composes arbitrarily long chains. | ✅ Fixed (design amended) |
| 4 | internal/maze/generate_test.go:107-128 | minor | Design § Test Design → *Face agreement — AC3* (line 693) closes with "Plus rapid over world seeds and coordinates, including coordinates far from the origin and negative in both axes." None of it is implemented: `internal/maze` imports `pgregory.net/rapid` nowhere (all six `rapid.Check` sites are in `internal/hexgrid`), and `TestCell_FaceAgreementOverAMultiChunkRegion` runs a single world seed (20260912) over q,r ∈ [-20,20] — negative in both axes, but chunks -2..1 at the reference 16×16, i.e. adjacent to the origin. Same class as round 1's findings #6 and #7, a design-named scenario with no implementation, and rated the same. Measured, so the gap is sized rather than asserted: the missing scenario written out deterministically (5 seeds × 5 far bases at ±(1<<20), ±(1<<28) and ±2147483600, ±3 cells each) PASSES on the clean tree — a coverage gap, not a live defect — and it is a live instrument, going RED under the `borderKey` canonical-order mutant with `face disagreement at {1048575 1048575} dir 1: 2 vs 1`. Fix: add the seed-and-far-coordinate sweep to the AC3 scenario. Not an amendment trigger — the design's clause is correct and unimplemented, not stale. | ✅ Fixed (design amended) |
| 5 | ai-docs/plans/2026-09-12-world-generation-hex-chunk-generator.progress.md `## Next action` | minor | The round-4 self-review authorisation is asserted without the record every other cap raise in this file carries. `## Next action` reads "The next action is self-review round 4 (the owner raised the cap to 4)"; `.claude/skills/task/SKILL.md:208` binds "The turn that applies a raise MUST echo `cap: N (was M)`", and AGENTS.md § *Workflow* makes the authorisation load-bearing ("after 3 REJECTs in a row, surface and stop without pushing"). Measured: `grep -n 'cap: '` over the file returns four echoes, every one of them a design-review raise (lines 43, 71, 74, 88 — `cap: 4 (was 3)` through `cap: 7 (was 6)`), each paired with a Decisions-log entry carrying the owner's words; the self-review raise has neither an echo nor an entry. Same claim class as round 2's finding #3 and round 3's finding #3 — a statement about this run's own state that the file cannot substantiate, in the block its own header tells a re-entering agent to act on. Fix: record the raise the way the other four are recorded, with the owner's words, or withdraw the parenthetical. Not an amendment trigger — the spec and design say nothing about round caps. | ✅ Fixed |

**Nits (1, no row): `internal/detguard/detguard_test.go`** — enumerated in register row R4-6, entered as `accepted@4 — below severity floor`.

### What was checked

- **Spawn prompt:** within the closed list — invocation line, `Spec:`, `Design:`, `Progress:`, and the range `c07f8c1..HEAD`. No `PROMPT-CONTAMINATION`.
- **Diff window:** `c07f8c1..HEAD` re-confirmed as this branch's own contribution — `c07f8c1` is `Merge pull request #114`, `origin/main`'s tip merged in at `af9fb63`, and it is an ancestor of HEAD; 54 files, all this task's. Round 3's fix delta is `f06d146..HEAD` (4 files: `internal/detguard/detguard.go`, `internal/detguard/detguard_test.go`, `ai-docs/coverage-ratchet.txt`, this progress file), which is where findings #1-#3 live; #4 is in the Group A test suite and #5 in `f6a4583`.
- **Gates, all re-run at HEAD:** `go vet ./...` GREEN · `golangci-lint run` GREEN · `golangci-lint fmt -d` 0 diff lines · `make comment-refs` GREEN · `make file-limits` GREEN · `make import-guard` GREEN · `go test -count=1 ./...` GREEN across all 23 packages · `go test -race -count=1` GREEN on the three task packages (0 `WARNING: DATA RACE`). `go.mod`/`go.sum` unchanged across the window, so the tidy gate is not engaged; no `*.sh` or workflow file in this round's delta, so shellcheck and actionlint are not engaged.
- **Round-3 closure, per register row, by running each row's own verifying command — all three confirmed.** R3-1: the row's own command run verbatim in `cycles.go`, both cells — `for k := range open` RED and `for k := range islands` RED, each naming `[.../cycles.go: ranges over a map]`, each mutant confirmed to build and vet first, the file restored from a `cp` backup with `git status --porcelain` empty. R3-2: `ai-docs/scripts/check-review-register.sh` on this file now exits **0**; the ten `has no register row` lines are gone. R3-3: the `## Next action` block no longer instructs work marked `[x]` and no longer contradicts the AC2 row — the residual defect in that block is a different claim class and is finding #5, not R3-3 re-opened.
- **All 19 AC verifiers re-run against the shipped tree — AC1-AC19 PASS**, each with a non-zero pass count and zero FAIL and zero SKIP (AC1 3, AC2 2, AC3 2, AC4 14, AC5 6, AC6 4, AC7 2, AC8 4, AC9 2, AC10 4, AC11 7, AC12 3, AC13 4, AC14 4, AC15 2, AC16 2, AC19 3). AC17: `686215 ns/op` at `-benchtime=10x`, and the no-threshold negative re-run with a control (`grep -rnE '\-bench|benchtime' Makefile .github/workflows/ci.yml .githooks/ ai-docs/scripts/` exits 1; the same pattern fires on a constructed gate line). AC18: the conjunction sweep re-run over **106** tracked `*.md` outside this run's plan files and the two history logs — 13 class hits, 0 that also frame the class as open — with control A (a constructed Russian open line) firing and control B (a constructed decided line) clean; `docs/DESIGN.md:482` carries the clause struck and marked `закрыт`.
- **Design conformance:** the design is unchanged since `9647cf0` and the spec since `861408b`, both before this round's delta, so nothing in the diff can have out-run them. Every file of § Decomposition's file column is present. **GO-notes round trip:** all 25 rows carry a route; G1-G23 and G25 `folded` at their pinned revisions, G24 `deferred to Step 9` and discharged as round 1's finding #4. One design clause is named and **not** implemented — § Test Design line 693's rapid half of the face-agreement scenario, finding #4.
- **Five fresh mutations, none of them re-treading an earlier round's five**, each confirmed to build and vet first, each restored from a `cp` backup under `tmp/` with `git status --porcelain` empty afterwards: a call-result map range in `generate.go` (GREEN — finding #1) with its declared-identifier control RED; `hexgrid.FaceOf`'s canonicalisation flipped (RED on `TestFaceOf_CanonicalDirectionIsAlwaysTheEarlierOfThePair`; `internal/maze` correctly GREEN, register R4-8); `borderKey` stripped of `canonicalOrder`, used as the discriminating control for finding #4's probe (RED, naming a far coordinate); `collectNamedMapTypes`'s fixpoint reduced to a single pass (all three packages GREEN — finding #3); and six scratch shapes probed through a temporary `_test.go` in `internal/detguard`, removed afterwards.
- **Determinism, independently of the guard:** all 36 `range` statements in the three packages' non-test files enumerated by hand — every one over a slice, an array or the fixed direction table, none over a map, including the five that range over a function's result. R1-11 holds at HEAD (register R4-9).
- **Safety and style:** the panic grep over the three packages' non-test files returns nothing, so `ai-docs/panic-index.md` is correctly unchanged across the whole window (`git diff --stat` empty). Eight `//nolint` directives, each naming `gosec` with the G115 reason. No error path added by this round's delta (no `err`, `panic` or `log.` line in it). No goroutine started; `internal/detguard/main_test.go` carries the mandated `leaktest.Main` `TestMain` and adds no ignore entry. Largest non-test file 320 lines (`internal/maze/algorithms.go`), `internal/detguard/detguard.go` now 285, largest test file 339 (`internal/maze/generate_test.go`) — all inside the 500 soft band. `internal/*` only.
- **DOC-4:** the widened outward-pointer sweep over the three packages' `*.go`, filtered to comment lines, returns **0 hits**, with a constructed pointer line firing as the control — the fix's new comments introduce none.
- **Domain invariants:** the round's delta touches a guard package and two plan/ratchet files. No balance column, no posting, no basis document, no persisted enum, no schema change, no outbound send path, no secret, no `time.Now()`, no unseeded `math/rand`, no floating-point type, no balance constant in Go. `ai-docs/coverage-ratchet.txt` rose 91.42 → 91.48.
- **Register discipline:** the round-3 `fixed@4c1f700` rows were re-examined over the diff since that sha only, and none is re-opened. The three `accepted` rows adjacent to finding #1 were read before any id was minted: R1-11 is about live ranges, R2-4 about the collector's name-keyed namespace over-reporting (the opposite direction — it fails closed), R3-1 about the named-type spelling; finding #1 is the call-result right-hand side and restates none of them, so it takes a new id. Finding #2's relationship to R3-4 is stated in its own row, with the property that makes R3-4's reasoning not extend. **One fact the orchestrator needs for its own tripwire arithmetic, stated because it is observable and not because it routes anything:** this is the fourth consecutive round to find a blind shape in the same predicate — parameter (R1-2), no red case for it (R2-1), named type (R3-1), call result and selector (R4-1, R4-2) — each a distinct shape, none a re-open.
- **`pgregory.net/rapid`, examined for the first time in this loop and clean** (register R4-7): a pre-existing direct dependency the design records as measured, `go.mod`/`go.sum` unchanged across the window, its timestamped `.fail` artefacts gitignored at `.gitignore:46`. The one this round's own runs produced was removed and the tree re-verified clean.
- **Progress-file re-entry fields:** `current_step`, `last_passed_gate` and `entry_args` present; `parent_skill` optional per the canonical template and correctly omitted. Content not reviewed, per the agent's own instruction 2 — except `## Next action`, which is not one of those fields and is finding #5.
