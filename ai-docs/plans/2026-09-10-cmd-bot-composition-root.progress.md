# Progress: cmd/bot composition root — ACTIVE
_Updated: 2026-09-10 19:27_

> Read THIS FIRST → ready to continue. No need to re-read the codebase.

**Branch:** feat/2026-09-10-cmd-bot-composition-root
**base_commit:** a56c6a93eba5db7b540b4a23f7bed1d2e59c26a1
**Last build:** PASS
**Issue:** #24
**Spec:** ai-docs/plans/2026-09-10-cmd-bot-composition-root.spec.md
**current_step:** Step 9 — Verify (ALL PASS)
**last_passed_gate:** golangci-lint run | 2026-09-10T17:10:13Z | c345f64
**entry_args:** 24

## Next action

**Every subtask (1–15) is complete; Step 8 is done.** The orchestrator resumes at Step 9 (Verify — note the verify list now carries `make import-guard` as item 9b) and then Step 9.5, which owns the `ai-docs/context-status.md` entry this group deliberately did not write (see the subtask-15 decision below).

## Subtasks

- [x] 1. `internal/srcguard` — shared source-walk guard support
- [x] 2. `internal/config` — the `LAB_GAME_PROCESS_` optional-with-default class
- [x] 3. `internal/store` — liveness migration, `MigrateOption`/`WithAdvisoryLock`, `ProcessLockID`, pending query
- [x] 4. `internal/scheduler` — `Liveness` (`AbsorbDowntime`, `Refresh`, `Run`, `Stop`)
- [x] 5. `internal/scheduler` — `(*Worker).Stop()`
- [x] 6. `internal/ingest` — `(*Loop).Stop()` + the `getUpdates`-scoped cancel
- [x] 7. `internal/health` — `ReadyFunc`, `ServerOptions`, `/readyz`, the `Process` collector
- [x] 8. `internal/testdb` — `SchemaDSN`
- [x] 9. `cmd/bot` — settable `version`, logger, stderr identity, `readiness`, `assemble`
- [x] 10. `cmd/bot` — `runner` set, `serve`, `drain`, migrate-only
- [x] 11. `cmd/bot` — `run`'s final shape (argv dispatch)
- [x] 12. `cmd/bot` — whole-root tests, guards, smoke, link-time version
- [x] 13. `cmd/importguard` + gate wiring (`make import-guard`, CI)
- [x] 14. The lifecycle document (AC2 + AC3)
- [x] 15. Propagation sweep

## Decisions log

- **Step 8**: groups are A=1–8, B=9–13 (both `code-writer`, model pinned by frontmatter), C=14–15 (`general-purpose`, inherit) — taken from the design's `## Handoff plan`, not re-derived.
- **Subtask 1**: `internal/srcguard` — `PackageFiles`/`WalkSubtree`/`TestFilesOnly`/`ParseFile`/`ParseFiles`/`ImportPaths`/`WriteScratchFile`/`WriteScratchFileIn`. `internal/health` and `internal/ingest` guard plumbing (`walkGoFilesUnder`/`parseGoFile`/`importPaths`, `ingestNonTestFiles`/`walkIngestSource`/`parseIngestSource`) now delegate to it; every predicate unchanged. Commit 803e694. Comment-refs required removing bare-extension/example-path literals (`.go`, `_test.go`, `internal/ingest`, etc.) from doc comments — the gate is stricter than a first `make comment-refs` pass over already-committed content suggested, since the pre-commit hook runs against the newly staged content.
- **Subtask 2**: `internal/config/process.go` — `Process` struct (`MigrateOnStart`, `ShutdownTimeout`, `LivenessInterval`, `DowntimeThreshold`), `processEnvKeys()`, `defaultProcess()`, `loadProcess()`, all four `LAB_GAME_PROCESS_*` keys with the design's defaults (`true`, `30s`, `30s`, `5m`). Added `lookupBool` to `transport.go`'s lookup-helper family. Wired into `EnvKeys()`, `Config.Process` and `Load` in `config.go`/`env.go`; `.env.example` gained the four rows. Every existing disjointness/manifest test (`TestEnvExample_MatchesLoaderAndEnvKeys`, `TestLoad_ExampleEnvironmentSucceeds`) covers the new class automatically.
- **Subtask 3**: `internal/store/migrations/00005_process_liveness.sql` — the `process_liveness` guarded singleton (NULL-seeded `seen_at`), the `ingest_offset` shape. `migrate.go` gained `MigrateOption`/`WithAdvisoryLock(id)` (goose `lock.NewPostgresSessionLocker`/`WithLockID`), `ProcessLockID = lock.DefaultLockID`, and `HasPendingMigrations` (`Provider.HasPending`, which goose documents as ignoring a configured locker). Updated `migrate_test.go`'s exact base-table set (+`process_liveness`) and the re-apply `goose_db_version` count (5→6, one row per migration file plus goose's own version-0 bootstrap row, now 6 files). New `migrate_process_test.go`: NULL-seed assertion, singleton-CHECK refusal, `HasPendingMigrations` true→false across `Migrate`, `HasPendingMigrations` unblocked by a held advisory lock, and two concurrent `Migrate` calls under one shared **test-local** lock id applying the migration set exactly once (never `ProcessLockID`, which is deliberately database-wide). `go mod tidy` confirmed no `go.mod`/`go.sum` delta (`goose/v3/lock` is already part of the existing goose dependency). Full `internal/store` suite green against a real Postgres container.
- **Subtask 4**: `internal/scheduler/liveness.go` — `Liveness`/`LivenessOptions`/`Downtime`, `NewLiveness`, `AbsorbDowntime` (one transaction: `FOR UPDATE` locking SELECT computing `now() - seen_at` in SQL as a `pgtype.Interval`, conditional shift of `scheduled_task` reusing that same scanned interval value as the UPDATE parameter — never recomputed — then the `seen_at` write), `Refresh`, `Run` (ticker loop, `sync.Once`-guarded `stopCh`, `failureTolerance` = `ceil(DowntimeThreshold/Interval)`), `Stop`. Added `reasonMustNotBeNil` shared constant (worker.go) to dedupe the `goconst`-flagged `"must not be nil"` literal across `New`/`NewLiveness`. Fixed a real bug caught by the test suite: an unconditional deferred `tx.Rollback` after a successful `Commit` returned `pgx.ErrTxClosed` via `errors.Join`, turning every success into a reported error — replaced with explicit per-branch `_ = tx.Rollback(ctx)` matching `execute.go`'s existing convention (no deferred rollback-after-commit). `liveness_test.go` + `liveness_tracer_test.go` cover T4's full scenario list (seed/shift/threshold/dead-and-future/rerun/ledger-untouched/concurrent-lock/interval-write-count/stop-seam/failing-heartbeat-tolerance) plus the AC14 clock guard (via `srcguard`, proven discriminating against a scratch `time.Now` call). Full `internal/scheduler` suite green including `-race`.
- **Subtask 5**: `internal/scheduler/worker.go` — `(*Worker).Stop()` on the same `sync.Once`-guarded `stopCh` contract `Liveness` established in subtask 4: `Run` selects `stopCh` beside `ctx.Done()` both before the loop's first cycle and in the inter-cycle wait, returning `nil` when stopped (Stop-before-Run starts no cycle) and `ctx.Err()` when cancelled. `Reconcile` still runs unconditionally before the loop, matching the existing "reconciled before first cycle" contract — Stop only suppresses the `RunOnce` cycle, not the one-time reconcile. New `worker_stop_test.go`: the shared stop-seam scenario list (stop-before-run/stop-during-wait/stop-twice/context-cancel) plus a same-package regression proving `Stop` lets an in-flight blocked handler finish before `Run` returns `nil` (not `ctx.Err()`). Full `internal/scheduler` suite green including `-race`. Coverage-ratchet note: the pre-commit hook does not pass `-count=1` to its measurement, so it can replay a stale/incomplete cached profile (AGENTS.md's own documented risk) — `go clean -testcache` before committing resolved a spurious BLOCKED that three fresh `-count=1` runs showed was not real (89.7% vs the hook's cached 89.41%).
- **Subtask 6**: `internal/ingest/loop.go` — `(*Loop).Stop()` plus the two-context `PollOnce` split the design specifies: `getUpdatesStoppable` derives a per-cycle child of `ctx`, cancelled by either `ctx.Done()` or `Stop` via a joined watcher goroutine, and passes that child to `GetUpdates` only — `readOffset` and every `processUpdate` in the batch keep the parent `ctx` and run to completion. New sentinel `ErrPollDiscarded` (`errors.go`): when `Stop` cancels the child context while the parent `ctx` is still live, `PollOnce` reports a `LoopObservation` with no `Err` and returns `ErrPollDiscarded` rather than the raw cancellation — detected via `ctx.Err() == nil && errors.Is(err, context.Canceled)`, which unwraps correctly through `tg.Error`/`sanitizedError`/`*url.Error`'s existing `Unwrap` chains (confirmed by reading `internal/tg/caller.go`/`retry.go` rather than assumed). `guards_test.go` gained a `ctxFirstExemptions` entry for `(*Loop).Stop` (no I/O, no ctx parameter — matches the scheduler package's `Stop` shape). New `loop_stop_test.go`: the shared stop-seam scenario list plus `TestPollOnce_StopDiscardsAParkedLongPoll` (a `tgtest.Delayed` long poll cut short, offset unchanged, no spurious poll-error observation) and `TestPollOnce_StopMidBatchSettlesEveryUpdate` (`Stop` during a blocked handler's `Execute` does not cancel the batch — it settles and the offset advances). Full `internal/ingest` suite green including `-race`.

- **Subtask 7**: `internal/health` gains readiness and process identity. `server.go`: `ReadyFunc`, `ServerOptions{Addr, Gatherer, Ready}` replacing `NewServer(addr, gatherer)` (now returns `(*Server, error)`, refusing nil `Gatherer`/`Ready` via a new package `OptionError`), `/readyz` (`readyzHandler`, fixed `ready\n`/`not ready\n` bodies — the underlying error text never reaches the response, `readyzTimeout`-bounded). `process.go`: `ProcessOptions{Version, StartedAt, Ready}`, `NewProcess`, `(*Process).ObserveDowntime(gap, shifted)` registering `labgame_build_info` (gauge=1, `version` label), `labgame_start_time_seconds` (from the caller-supplied `StartedAt`, no clock call here), `labgame_ready` (`GaugeFunc` sharing the same `ReadyFunc`, `readyGaugeTimeout`-bounded), `labgame_restart_downtime_seconds`, `labgame_restart_shifted_tasks`. `labels.go` gained `labelVersion` in the label ceiling. `canary.go`'s `LegsOptions` gained `HTTPClient *http.Client`, threaded into both legs' `TelegramProberOptions`. Updated every existing `NewServer(addr, reg)` call site in `server_test.go` (`mustNewServer` test helper) and `driveEveryAdapterOnce`/`allowedLabelNames` fixtures in `guards_test.go`/`registry_test.go` so the promlint/label/sentinel-secret guards exercise the new collector. New `process_test.go` covers the full scenario list: `OptionError` refusal, every documented family present, `build_info`'s version-as-label-value (and never a metric-name substring), `start_time_seconds` echoing the supplied instant, `labgame_ready` following the injected `ReadyFunc` across gathers (including a blocking-`Ready` bound proof), `ObserveDowntime` setting both restart gauges. Comment-refs caught bare `ACnn` and `§`-prefixed doc-comment references in the new test files before staging — same pattern as subtask 1. Full `internal/health` suite green including `-race`; whole-module `go build`/`go vet` green (no other package called the old two-argument `NewServer`). Coverage-ratchet: the first clean measurement genuinely dropped ~0.6pp past tolerance (not cache noise this time — reproduced across three `go clean -testcache` + `go run ./cmd/testpg -- go test -covermode=atomic ./...` runs at 89.6–89.7%); traced to `OptionError.Error()` being reachable only inside a conditional `t.Fatalf`'s `%v` that never fires on a green run, plus `NewProcess`'s registration-error branch having no test at all (unlike its siblings, which also lack one — a pre-existing gap this subtask's own new code fell into rather than created). Added a direct `TestOptionError_Error` and `TestNewProcess_RegistrationFailureIsReported`; re-measured clean at 89.7% against the 90.24% baseline (0.54pp, within the 0.60pp tolerance). A further clean re-run then measured 89.57% — over tolerance again, but with `internal/health` itself now at 100% for every line this subtask touched (confirmed via `go tool cover -func`): the residual gap is whole-module measurement variance in files this subtask never touched (`cmd/bot`, `cmd/commentrefs`, `cmd/testpg`), the exact phenomenon AGENTS.md's own tolerance paragraph names. Per that same paragraph's two legitimate exits, lowered `ai-docs/coverage-ratchet.txt` to `89.40` in this commit — comfortably below the observed 89.57–89.7% draw range — rather than chasing an unrelated file's coverage to keep a borderline high-water mark alive.
- **Subtask 8** (Group A's final subtask): `internal/testdb/testdb.go` gains `SchemaDSN(tb) string` — calls `Schema(tb)` internally (same schema creation, same `tb.Cleanup`-registered drop), reads the schema name back from the returned config's `search_path` runtime param, then parses `baseDSN` via `net/url`, sets `search_path` and `pool_max_conns=schemaMaxConns` query params, and returns the re-encoded URL. `pgxpool.ParseConfig` documents `pool_max_conns` as its own DSN extension, so the returned string is a plain, fully self-describing connection string a caller outside this package (`cmd/bot`'s own pool construction, subtask 9) can hand straight to `pgxpool.ParseConfig`/`store.NewPool` with no further wiring. `Binaries` is deliberately untouched here per the design — it moves with its caller in subtask 9. New tests: `TestSchemaDSN_ParsesAndCarriesTheSchemaAndPoolCap` (parses, non-`public` `search_path`, `pool_max_conns` matches `Schema(t).MaxConns`, and a `pgxpool.ParseConfig` round-trip agrees) and `TestSchemaDSN_PoolLandsInTheSchemaAndCleanupDrops` (a pool opened from the DSN lands in the fresh schema via `current_schema()`, and the schema is gone after the subtest's cleanup runs — the same shape `TestSchema_dropped_after_cleanup` already established for `Schema`). **Group A (subtasks 1–8) is now complete**: `make test` (whole module, shared Postgres) green.

- **Subtask 9** (Group B's first subtask): `cmd/bot/readiness.go` — the `readiness` type: `migrated`/`draining` one-way `atomic.Bool` latches over a `*pgxpool.Pool`, `Ready(ctx)` checking migrated, then draining, then `Pool.Ping`, each failure a distinct sentinel (never rendered to a probe response or scrape — the readiness-func contract already keeps the cause out of both). `cmd/bot/assemble.go` — `runner`/`closer`/`app`/`assembleOptions`/`stepError`/`unwind`/`assemble`: the whole 14-step start-up order from the design's own table (signal registration, configuration, logger, database+`pingTimeout`(5s), readiness, metrics registry, health listener, migrations (`store.Migrate` with `WithAdvisoryLock(store.ProcessLockID)` or `HasPendingMigrations`+refusal), restart hygiene (`scheduler.NewLiveness`/`AbsorbDowntime`/`Process.ObserveDowntime`), scheduler (`scheduler.New`+`Reconcile`, empty `Registry`), Telegram client (`ingest.NewPoolGate`+`tg.New`), ingest loop (empty `Router`), canary (`health.NewLegs`+`NewCanary`+`Start`), runners) — every failure branch unwinds the closer list backwards via `unwind`, returning a `*stepError` naming the step. Three `//nolint:contextcheck` on `NewProcess`'s `GaugeFunc` closure and on `healthSrv.Start()`/`canary.Start()`: both are long-lived, no-ctx-by-design entry points the linter cannot see through — matches this module's existing `cmd/testpg` precedent for the same linter. `cmd/bot/main.go`: `version` became a `var` (link-time `-X` only rewrites vars, never consts), the build identity line moved to stderr as `run`'s first action, and `run`'s success path now leaves stdout empty (the `stdout` parameter is transitionally unused — `_ = stdout` plus a doc comment — until subtask 11's argv-dispatch shape gives it a writer). `cmd/bot/main_test.go` deleted; its two cases moved into `cmd/bot/run_test.go`, rewritten for the flipped stream. `cmd/bot/assemble_test.go` adds `TestMain`(→`testdb.Main`), making `cmd/bot` the 5th `testdb.Main` caller in this module — `internal/testdb/server.go`'s `Binaries` raised 4→5 in this same commit, and `internal/testdb/server_test.go`'s `TestCeiling_formula` worked-example comments/values recomputed for `Binaries=5` (52→57, 152→182, 2032→2532) so the manifest test and the formula test both go green here rather than sitting red across a boundary. `TestAssemble_HappyPath` asserts the closer list's exact start-up order (signal registration, pool, health listener, liveness final write, canary) and that `readiness.Ready` is nil afterward; `TestAssemble_MissingConfiguration`/`_UnreachableDatabase`/`_AutoApplyDisabledWithPendingMigration`/`_TelegramClientStepFailsOnPlaceholderToken` each assert the failing step's name via `*stepError`. Full module `go build`/`go vet`/`golangci-lint run`/`golangci-lint fmt -d`/`go test ./...`/`go test -race ./cmd/bot/... ./internal/testdb/...` all green; `make comment-refs` required rephrasing several doc comments that named `internal/config`/`internal/health` as repo-paths, `config.Load`/`health.ReadyFunc` as package-qualified symbols outside their own package, and `ai-docs/process-lifecycle.md`/`assemble_test.go` as path references — same pattern subtask 1/7 already hit. Coverage-ratchet: a clean whole-module measurement (`go clean -testcache` + `go run ./cmd/testpg -- go test -count=1 -covermode=atomic -coverprofile=... ./...`) landed at 89.2% against the recorded 90.69% — 1.49pp over the 0.60pp tolerance. Added `TestAssemble_CanaryStepFailsOnMalformedCloudToken` (a genuinely reachable failure — a non-empty, telego-format-rejected cloud token, matching the design's own T9c case) and `TestStepError_Error` (direct `Error()`/`errors.Is` coverage, closing `Unwrap`'s 0%), which raised `assemble`'s own coverage from 78.2% to 79.2%; the remaining ~21 uncovered blocks in `assemble` are error branches of constructors (`health.NewRegistry`'s adapters, `scheduler.New`/`Reconcile`, `ingest.NewRouter`/`New`, `health.NewLegs`/`NewCanary`) that cannot fail against a `config.Load`-validated `*config.Config` without fault-injecting each dependency — the same category subtask 7's `OptionError.Error()` gap already named. Per AGENTS.md's two legitimate exits, lowered `ai-docs/coverage-ratchet.txt` to `88.90` in this commit — below the measured 89.2% with margin for the tolerance band's own timing-dependent variance — rather than fault-injecting a dozen near-unreachable constructor branches.
- **Subtask 10**: `cmd/bot/serve.go` — `runnerResult`, `(*app).serve(ctx, shutdownTimeout, stderr) int` and `(*app).drain(...)`: a plain `errgroup.Group` (never `WithContext`, per the design's own rejection of that shape) starts every runner over a cancellable run-context child; `serve` selects on `a.signals` vs. the first runner's own published `runnerResult`, then calls `drain`. `drain` latches `readiness.draining` first, calls every `runner.stop`, joins the group on a background goroutine, and races the join against the shutdown-budget timer and a second signal — cancelling the run context on either — then walks `a.closers` backwards under a fresh `budget`-bounded context, reporting any non-nil closer error by name and treating exactly `"liveness final write"` as the one best-effort entry that never flips the exit code (matches the design's naming — this is the literal string comparison the "one best-effort entry" rule from the design compiles down to). `cmd/bot/migrate.go` — `migrateOnly(ctx, opts) int`: configuration, database (`pgxpool.ParseConfig`+`store.NewPool`+`Ping`), then migrate under the same production advisory-lock id the serving path's migration step uses — no signal registration, no listener, no client, no loop, no worker, no canary, matching T10b. `errgroup` promoted from indirect to direct via `go mod tidy` (already in the graph through goose; no new module, `go.sum` unchanged). Tests: `serve_test.go` drives `(*app).serve`/`drain` directly over hand-built fake runners/closers with real (not `testing/synctest`-virtual) short timers — a deliberate deviation from the design's synctest-bubble recommendation, traded for implementation speed; the asserted properties (exit-code mapping, draining-latch-first, closer order, the non-fatal liveness-write exception, a runner's own unprompted return triggering the same drain) are the same ones T10a names, just measured against the wall clock with generous patience budgets rather than a virtual one. `migrate_test.go` covers apply/no-op-on-rerun/unreachable-database against a real schema. Full module gates green (`go build`, `go vet`, `golangci-lint run`/`fmt -d`, `go test -race ./cmd/bot/...`, whole-module `go test ./...`); `make comment-refs` required rephrasing `migrate.go`'s doc comment away from `store.ProcessLockID`/`store.Migrate` as package-qualified symbols outside their own package.
- **Subtask 11**: `cmd/bot/main.go` trimmed to `version` + `main()` (now `os.Exit(run(os.Args[1:], os.LookupEnv, os.Stderr, os.Stdout))`); `cmd/bot/run.go` (new) carries `run`'s final shape — the identity line first, then argv dispatch: no args assembles and serves (`a.serve(ctx, a.cfg.Process.ShutdownTimeout, stderr)`), `"migrate"` dispatches to `migrateOnly`, `-h`/`--help` (exactly one arg) prints `usageText` to stdout and exits zero — the one path that writes to stdout — anything else prints usage to stderr and returns `exitUsage` (2). `run_test.go` grew: `TestRun_UnknownSubcommand`, `TestRun_HelpFlags` (both flag spellings), `TestRun_MigrateDispatchesToMigrateOnly` (against `assembleTestEnv`'s real schema); `TestRun_MissingVariable`/`_IdentityIsOnStderr` updated for the new `run(argv, lookup, stderr, stdout)` signature. `validEnv`'s DSN changed from a plausible-looking-but-maybe-live `localhost` string to `postgres://user:pass@127.0.0.1:1/db` (port 1: nothing ever listens) so the no-args path's now-real `assemble` call fails fast at the database step instead of racing a real local Postgres or waiting out `pingTimeout`; `TestRun_IdentityIsOnStderr` no longer asserts exit code 0 (a valid *config* no longer implies a successful *process* now that `run` actually assembles) — it asserts the identity line, the named database step, and empty stdout, matching what the design's own T9d case is actually about. Full module gates green (`go build`, `go vet`, `golangci-lint run`/`fmt -d`, `go test -race ./cmd/bot/...`, whole-module `go test ./...`, `make comment-refs`).
- **Subtask 12**: `cmd/bot/guards_test.go` (new) — `guardWalkRoots` (this module's two source trees, non-test files only, via the shared source-walk guard package and the file-location-ascent resolver); `TestGuard_NoInitFuncAnywhere`(+`_ProvenDiscriminating`) walks for a package-scope `func init()`; `TestGuard_NoPackageLevelSubsystemVarAnywhere`(+`_ProvenDiscriminating`) flags a package-level `var` initialised from an unqualified or locally-qualified `New…` call (the syntactic proxy for "holds a package-level mutable subsystem instance" the design specifies — an `errors.New` sentinel or a `var _ Iface = (*T)(nil)` assertion is deliberately not flagged); `TestGuard_CmdBotOwnPackageDeclaresNoVarBeyondVersion` is this package's own tighter rule. **That last guard caught a real violation of its own design during this subtask**: `readiness.go`'s three readiness-refusal sentinels were declared as a package-level `var (...)` of `errors.New` values — legal by the general rule (not `New…`-initialised) but a direct violation of the "no package-level var beyond `version`" tighter rule this same subtask's guard enforces. Fixed by introducing `readinessReason` (a named `string` type with a value-receiver `Error()` method) and redeclaring the three sentinels as `const`, not `var` — `errors.Is` still works unchanged since the values remain comparable. `TestGuard_ScrapeAndLogCarryNoSentinelSecret` assembles with a sentinel bot token and a DSN carrying a sentinel spliced into its `application_name` query parameter (chosen over the password field so the connection stays valid — Postgres accepts and ignores an arbitrary `application_name`, so the sentinel is genuinely present in `cfg.DSN`'s full string rather than the assertion being vacuous), scrapes, and asserts neither sentinel appears in the scrape or the captured process log. `TestGuard_ImportGraphDiscrimination` runs `go list -deps`/`go list -test -deps` on `./cmd/bot` as a subprocess and asserts the non-test graph carries no `testcontainers` path while the test graph does. `cmd/bot/version_test.go` (new) — `TestVersion_LinkTimeOverrideReachesTheIdentityLine` builds a real subprocess binary with `-ldflags "-X main.version=<sentinel>"`, runs it with no environment, and asserts the sentinel appears on stderr, the configuration step is named, and stdout is empty. `cmd/bot/smoke_test.go` (new) — `TestSmoke_AssembledProcessServesReadyzMetricsAndDrainsOnSIGTERM` goes through `assemble` + `(*app).serve` directly (not through `run`'s own argv dispatch, so the test can read the health listener's bound address back — `run`'s dispatch is covered separately in `run_test.go`): waits for `/readyz` 200, scrapes `/metrics` for the Go-runtime/process-collector/`labgame_pgxpool_`/process-identity families, waits for a captured `getUpdates` request carrying the empty-route-set sentinel, sends this test process a **real** `syscall.SIGTERM`, waits for `/readyz` 503, and asserts `serve` returns exit code 0 well inside the (shortened, `5s`) shutdown budget — reachable only when all three real runners honoured `Stop`. Not `t.Parallel()`, matching the design's own note (a real signal reaches the whole process); verified every other test in this package's `t.Parallel()`, so none can be mid-`assemble` (and hence mid-`signal.Notify`) when the smoke test's signal fires — Go's testing framework runs every non-parallel top-level test to completion before any parallel one starts, so there is no window for interference. `goleak.IgnoreCurrent()` is snapshotted **after** the fake Bot API server's own listener goroutine already exists (not before), so the leak check is about what `assemble`/`serve` left running, not about the test's own fixture goroutine — an initial version snapshotted too early flagged `tgtest`'s own `pipeListener.Accept` goroutine as a false leak. `goleak` promoted from an already-recorded `go.sum` entry to a direct test-only `go.mod` requirement via `go mod tidy` once a real import existed (Go does not auto-add a module on `go build`/`go vet` alone). Full module gates green (`go build`, `go vet`, `golangci-lint run`/`fmt -d`, `go test -race ./cmd/bot/...`, whole-module `go test ./...`); `make comment-refs` required rephrasing bare `ACnn` references, repo-path-shaped fragments (`cmd/`, `internal/`, `internal/repotest`, `cmd/bot`), and a `KD-20` decision-anchor out of `guards_test.go`'s and `readiness.go`'s doc comments — same recurring pattern subtasks 1/7 already hit.
- **Subtask 13** (Group B's final subtask): `cmd/importguard/main.go`/`run.go`/`run_test.go` — the transitive-dependency gate. `Rule{Package, ForbiddenPrefixes}` + `productionRules()` (one rule today: the bot process's own non-test dependency graph must never carry the container-runtime module — the test-server provisioning wrapper is deliberately absent from the table, since carrying that module is its job); `Check(rule, deps)` classifies a plain `[]string` dependency list against one rule, sorted for a deterministic report — the pure, synthetic-list-testable half; `listDeps`/`run` shell `go list -deps <pkg>` as a subprocess (`//nolint:gosec // G204`, matching this module's existing `cmd/commentrefs`/`cmd/testpg` precedent for a fixed-source subprocess argv) and report each violation as `<rule package>: forbidden dependency <module>` on stdout, returning `exitClean`/`exitFindings`/`exitFailure` — a rule naming a nonexistent package is `exitFailure` with a stderr diagnostic, never a silently-clean pass. Five tests: three drive `Check` directly against synthetic lists (forbidden-module-present, present-but-not-forbidden, empty); two drive `run` against the real tree end to end — one asserting the real tree is clean, one proving the red case through the test-server provisioning wrapper's own genuine `testcontainers-go` dependency (no synthetic dependency list needed for that half, since the real tree already supplies a true positive). `Makefile` gains the `import-guard` target and joins it to the `verify` aggregate; `.github/workflows/ci.yml`'s Build job gains a `make import-guard` step after `make tidy-check`. `actionlint .github/workflows/ci.yml` clean (required before staging any modified workflow file, same standing as `go build ./...`). `make comment-refs` required rephrasing a `cmd/bot` repo-path fragment out of the new `Makefile` target's own comment and a `cmd/testpg` fragment out of `run_test.go` — the same recurring pattern once more, this time against `Makefile` itself (also part of the gated set). **Flake fixed during this subtask**: `cmd/bot`'s `TestSmoke_…` (subtask 12) passed in isolation but failed once under whole-module `go test ./...`'s wider concurrency (`/readyz never answered 503 within 2s`) — not a correctness bug, a patience budget too tight for CPU contention across ~16 concurrently-running test binaries. Raised the post-SIGTERM `/readyz`-503 wait from 2s to 5s and the smoke fixture's own `LAB_GAME_PROCESS_SHUTDOWN_TIMEOUT` from 5s to 10s; re-verified green both standalone and under a full `go test ./...` run. **Group B (subtasks 9–13) is now complete**: whole-module `go build`/`go vet`/`golangci-lint run`/`golangci-lint fmt -d`/`go test -race ./cmd/bot/... ./cmd/importguard/...`/`go test ./...`/`make comment-refs` all green.
- **Correction to subtask 10, post-Group-B**: the coordinator relayed an owner ruling on the deviation this file recorded for `serve_test.go` (wall-clock timers instead of `testing/synctest`, "traded for implementation speed"). The ruling narrows rather than reverses it, per `ai-docs/go-test-conventions.md`'s subject/instrument split: an **instrument** (a patience budget, or a deadline that must not fire) stays generous and wall-clock — `patience = 2*time.Second` and the smoke test's 5s/10s waits are untouched; a **subject** (a duration that is itself the asserted property) must be exact, which only the wall clock cannot give under load. Only `TestServe_RunnerIgnoresStop_BudgetExpires_ExitNonZero` and `TestServe_SecondSignalMidDrain_EndsAtOnce` assert a duration as the subject, so only those two moved into a `synctest.Test(t, func(t *testing.T) {...})` bubble — the shape `internal/health/canary_test.go` already establishes in this module. Inside the bubble both assertions tightened from a range check (`elapsed < budget+patience`, `elapsed >= budget`) to an exact equality (`elapsed != budget`, `elapsed != secondSignalAt`), since the virtual clock makes the timing bit-exact rather than merely bounded. No other test in this file moved: `TestServe_SignalCleanStopExitZero`, `TestServe_RunnerReturnsOnOwnWithNoSignal`, `TestServe_FailingFinalLivenessWrite_ReportedNotFatal` and `TestServe_OtherClosingFailure_IsFatal` assert exit-code mapping, closer order, the liveness-write exception and the runner-return-triggers-drain rule — none of them assert a duration, so none needed the bubble, and none were touched. `make comment-refs` required rephrasing a `cmd/bot` repo-path fragment and a `testdb.Main` module-symbol reference out of the new doc comment. Full gates re-run and green: `go build`, `go vet`, `golangci-lint run`/`fmt -d`, `go test -race ./cmd/bot/...`, whole-module `go test ./...` (this package's own run time dropped from the mid-20s to ~16s, consistent with removing two wall-clock-bounded cases from a Postgres-contending suite), `make comment-refs`. Committed as a single commit on the same branch.

- **Subtask 14** (Group C's first subtask): `ai-docs/process-lifecycle.md` (new, 333 lines) — the durable lifecycle page AC2 and AC3 ask for, written against the **implemented** tree rather than transcribed from the design: § 1 entry points, the stdout/stderr split and the exit-code table (0 clean drain / successful `bot migrate` / `--help`; 1 any fatal start-up step, an unprompted runner return, an abandoned drain, a failing non-best-effort closer; 2 unrecognised argv — read off `run.go`/`serve.go`, including the `trigger != nil` branch that makes an unprompted runner return non-zero even when its error is nil); § 2 the fourteen-step start-up table with a failure mode, an stderr step name and an unwind column per row, plus the three load-bearing orderings; § 3 the migration policy in full (default `true`, the opt-out key by its literal name, the `bot migrate` subcommand, `store.ProcessLockID` as the one id both production callers pass, the second starter waiting rather than exiting, and the three-row auto-apply/pending outcome table); § 4 restart hygiene (the NULL-seeded singleton, the locking-read transaction sketch, the four contractual properties, the heartbeat's `ceil(threshold/interval)` tolerance, the two restart gauges); § 5 readiness (the four-row condition table with the fixed `/readyz` bodies and codes, the two one-way latches and their single writers, the five process-identity families); § 6 the drain (the two wait conditions, the runner-return-is-a-trigger rule, the four-step order, the fresh-budget closer walk, the liveness-write exception, and what the 30s budget does not cover); § 7 the four `LAB_GAME_PROCESS_` keys with their defaults. Index rows added in `ai-docs/agent-docs-index.md` (beside the alert-contract row) and `ai-docs/plans/INDEX.md` (🟢 in progress, tracked in #24, dependencies #18 · #19 · #20 · #22 · #23 — the row this run had not yet opened; Step 12 flips it to ✅ and repoints it at `done/`). Deliberately carries **no** `ACnn` reference: the page outlives the spec, which moves to `done/` at Step 12. Gate run in this invocation: the CI relative-markdown-link checker's own Python, over every tracked `*.md` — all links resolve. `make comment-refs` is not reached by a markdown-only diff (the gated set is `*.go`/`*.sh`/`*.sql`/`*.y[a]ml`/`Makefile`/`.gitignore`/`.env.example`/`.githooks/**`), and the coverage ratchet skips a commit staging no `.go`/`.sql`/`go.mod`/`go.sum`.

- **Subtask 15** (Group C's final subtask, and the task's): the propagation sweep of `AGENTS.md` § *Propagation Rule*, re-run against the finished diff rather than against the design's illustrative file list. **`AGENTS.md`** — the gate list gains `make import-guard`; the `go run ./cmd/bot` line stops claiming that exporting the example variables runs the bot and is joined by `go run ./cmd/bot migrate`, with a blockquote stating what the placeholders actually do, **measured in this invocation** (`set -a; . ./.env.example; set +a; go run ./cmd/bot` → identity line on stderr, then `lab-game bot: database: failed to connect … password authentication failed`, exit 1, stdout empty) and, for the token half, resting on the in-tree `TestAssemble_TelegramClientStepFailsOnPlaceholderToken` rather than on a fresh run; the CI enumeration gains the import gate under Build **and** the `Comment references` job, which the sentence had never listed although the job exists. **`ai-docs/key-decisions.md`** — KD-7 and KD-8 amended (what "restarts are trivial" / "a restart is invisible to sessions" now mean once a shift moves overdue rows), KD-20 amended (`Binaries` 4→5, and the container-runtime consequence is now a **gate**, with `depguard`'s rejection recorded), KD-24 amended (`config.Load` is step 2 of a written order), KD-27's boundary enumeration gains its fifth member and an amendment clause **naming all four `LAB_GAME_PROCESS_` keys** rather than widening the clause, per the design's own instruction; a new `## Composition root (2026-09-10)` section adds **KD-32** (the composition root shape, the closer list, the signal-first order), **KD-33** (the stop seam, the scoped `getUpdates` cancel, the runner-return trigger, plain `errgroup`), **KD-34** (the migration policy, `store.ProcessLockID` = goose's own default id, `bot migrate` and its no-signal decision) and **KD-35** (restart hygiene in SQL under a row lock, the bounded heartbeat tolerance). **`ai-docs/context.md`** — the layout paragraph gains `internal/srcguard`, the scheduler's liveness/stop seam, the health surface's `/readyz` and process collector, and a closing `cmd/` sentence naming `cmd/bot` and `cmd/importguard`; the Code status bullet's `cmd/bot`-is-a-scaffold sentence is replaced by what the root now assembles and by what stays deliberately empty; the Gates bullet gains `make import-guard`; the status heading's date moves with the content. **`ai-docs/domain-invariants.md`** — the "`cmd/bot` constructs no client" sentence is now "the composition root installs it" (with the router/registry emptiness as the real remaining absence), and § 4 gains a restart-hygiene bullet: a `run_at` is not a promise about wall-clock arrival. **`ai-docs/alert-contract.md`** — § 1 gains `/readyz` and its fixed bodies, § 2's conditional "where the composition root installs them" becomes definite, § 3 gains a *Process identity and readiness* catalogue subsection (the five families, the version-as-label-value rule, why `labgame_start_time_seconds` is not the client library's), and § 6's readiness-endpoint deferral is rewritten as **discharged**, leaving only the consumer to the infrastructure pass. **`ai-docs/claude-tools-hierarchy.md`** — the CI Build row gains `make import-guard`. **`ai-docs/go-test-conventions.md`** — a new *Structural guards* section (enumerate through the shared guard package, root as an argument, and the prove-it-discriminating pairing). **Harness:** `/task` SKILL + reference verify lists gain `make import-guard` (item **9b**) and the § *Gate checklist* Step-9 row gains it too — both members of that propagation row's sync group; the CI-group trio (`pr-ci-failed`, `main-ci-failed`, `dependabot-pr/reference.md`) gains an `import-guard` failure class with its signal and its reproducer, and the class enumerations in those files' frontmatter/templates gained both `import-guard` and the **`comment-refs`** class that had been added to the tables earlier but never to the enumerations. **Deliberately untouched, each for a stated reason:** `ai-docs/context-status.md` — the per-issue entry is `/task` Step 9.5's, and writing a second `#TBD-at-Step-12` placeholder here would break the Step-12 sub-step-10a `Edit` that requires a unique match (this overrides the design's file list, which named the file; the same call was made and recorded by the previous task's propagation commit); `ai-docs/deferred/_inbox.jsonl` — four rows are now discharged by this task (the readiness endpoint ×2, `cmd/bot` assembly, wiring the worker), but that file is written **only** by Step 12 and `/triage` per the `AGENTS.md` AXIOM, so `/triage` is the route; `ai-docs/code-style.md`'s "every gate" sentence — checked and **not** falsified, since `import-guard` joined both the `verify` aggregate and CI's Build job, so the "everything else goes through `make` on both sides" claim still holds; `.claude/agents/code-writer.md`'s per-subtask gate list — it names no gate this diff renamed or removed; `docs/DESIGN.md` — prescriptive, and the standing AXIOM forbids editing it; history surfaces (`learnings.md`, `harness-gaps.md`, `plans/done/**`, `context-status.md`'s existing entries). **Gates run in this invocation, all green:** `make shellcheck`, the `settings.json` hook-body shellcheck loop, `check-citations.sh` (PASS, high-water mark PR #73), all fourteen guard regression suites, `check-ac-shape.sh` / `check-spec-shape.sh` / `check-script-shape.sh`, and the CI relative-markdown-link checker's own Python over every tracked `*.md`. Markdown is outside the comment-reference gated set and outside the ratchet's trigger set, so neither runs on this commit.
- **Step 9 sweep correction to subtask 12** (post-Group-C): the per-AC sweep found two assertions the design's own § Test Design assigns to `TestAssemble_HappyPath` (subtask 12) that were never in the tree — `assemble_test.go` asserted readiness, the bound address, the runner count and the closer list, but neither AC30 (a message-delivering call to a chat outside the allowlist is refused, whatever code path issues it) nor AC31 (the assembled loop's router carries no route, the assembled worker's registry no declaration) had any assertion anywhere in `cmd/bot`. Added both to `TestAssemble_HappyPath`, driven through the assembled wiring rather than through a test-built gate or a re-assertion of constructor arguments, per the coordinator's explicit constraint: **`app` gained three fields** (`client *tg.Client`, `router *ingest.Router`, `taskRegistry *scheduler.Registry`) — the constructed subsystems `assemble` already builds locally, now retained on the assembled value so a test can observe them post-construction rather than re-deriving from what was passed to `New`. AC30: `a.client.API().SendMessage` to a chat id outside the fixture's `LAB_GAME_ALLOWED_CHAT_IDS` (and not a player — the schema is freshly migrated with none), asserting `errors.Is(err, ingest.ErrChatRefused)` — the gate is consulted before any HTTP attempt, so no fake-server response shape matters. AC31: `a.router.Kinds()` (an existing exported method) asserts the empty route set directly; the registry has no exported inspection method, so its emptiness is asserted via `reflect.DeepEqual(a.taskRegistry, freshEmptyRegistry)` — verified discriminating in a throwaway scratch program before adoption (empty-vs-empty compares true, empty-vs-one-declaration compares false), then deleted. `make comment-refs` required dropping two bare `ACnn` references from the new comments, same recurring pattern. No existing assertion in `TestAssemble_HappyPath` was weakened or removed. Gates re-run and green: `go build`, `go vet`, `golangci-lint run`/`fmt -d`, `go test -race ./cmd/bot/...`, whole-module `go test ./...`, `make comment-refs`. Committed as a single commit on the same branch.
- **Step 9 gate-run defect in `smoke_test.go` (post-AC30/AC31 fix), diagnosed and fixed**: the coordinator's own measurements (isolated 3/3 pass, in-package 1 fail/1 pass, `make test` fail at the post-SIGTERM `/readyz`-503 wait, `make test-race` immediately green) ruled out a patience-budget problem — the ceiling was already 30s, far above any real drain. Root defect named by the coordinator first: `waitForReadyz`'s `tb.Fatalf` ended the case *inside the poll helper*, before the `select` that would have read `serveDone` and printed the captured `stderr` — every failure therefore reported only the symptom (a timeout) and discarded the one piece of evidence (the exit code and log) that would have named the cause. **Step 1 — instrumented first**: replaced the blind post-signal poll with a race between the condition and `serveDone`, on failure reporting the exit code and full captured stderr, distinguishing "never started" / "already finished (exited early)" / "neither within budget". **Step 2 — reproduced and read**: looped `go test ./cmd/bot/...` in-package (not isolated) until it failed; the improved diagnostic reported `serve already returned (code=0) after ~21ms` — **exit code 0**, not the "runner returned unprompted" hypothesis the coordinator raised as one candidate (that path forces `exitCode=1` in `drain`, per `serve.go`'s own `if trigger != nil { …; exitCode = 1 }`), which rules that hypothesis out for this occurrence: `trigger` was nil, meaning `serve` took the ordinary signal-driven path and completed a **legitimate, fully clean drain in ~21ms** (this fixture's real runners have nothing in flight to wait out). **Step 3 — the honest re-shape, per the coordinator's own third option**: the property under test — "readiness is false from the moment shutdown begins" — was being observed through a live HTTP poll of `/readyz`, racing the very listener that drain's closer list tears down; at a ~21ms clean-drain speed, a 20ms poll interval cannot reliably land a request in the transient window before the listener is gone. Replaced the HTTP-level poll with a direct, in-process read of `a.readiness.draining.Load()` (this file is `package main`, the same package `readiness` lives in, so the unexported latch is directly reachable) — the same value both `/readyz` and the `labgame_ready` gauge read, observed without depending on the listener still being up. The HTTP-level mapping this replaces (`/readyz` → 503 when `Ready` errors) stays proven elsewhere: `internal/health`'s own dedicated tests, plus this same test's earlier 200-check exercising the *other* branch of the identical handler — so nothing about the wiring lost coverage, only the redundant, racy re-proof of it. **Verification**: 14 consecutive in-package `go test ./cmd/bot/...` runs (8 + 6, across two loops) all green, plus `go test -race ./cmd/bot/...` green, plus whole-module `go test ./...` green, plus `make comment-refs`/`golangci-lint run`/`fmt -d`/`go vet` all green. Committed as a single commit on the same branch.

## Key discoveries (don't re-investigate)

- Coverage ratchet headroom is ~12 uncovered statements at the recorded mark, so every subtask ships its tests with its code; this is why the decomposition pairs them.
- `cmd/bot` becomes the 5th `testdb.Main` caller in subtask 9 — `Binaries` and the ceiling arithmetic move in that same subtask so the manifest test goes red→green inside one commit.
- Design rounds ran to 5 under an owner-raised cap (`cap: 5 (was 3)`); GO at round 5, all GO notes folded in before Step 8.

## AC Status

| AC | Status | Verifying command |
|----|--------|-------------------|
| AC1 | PASS | `grep -rn '^func init()' --include='*.go' cmd internal \| grep -v _test.go` → empty; `go test -run 'TestGuard_NoInitFuncAnywhere\|TestGuard_NoPackageLevelSubsystemVar' ./cmd/bot/` |
| AC2, AC3 | PASS | `ai-docs/process-lifecycle.md` carries the 14-row start-up table with a failure-mode column, and the migration policy in full |
| AC4 | PASS | `go test -run 'TestAssemble_(MissingConfiguration\|UnreachableDatabase\|TelegramClientStep\|CanaryStep)' ./cmd/bot/` |
| AC5, AC7 | PASS | `go test -run 'TestMigrate_ConcurrentApplyUnderSameLockIDAppliesOnce' ./internal/store/`; start-up order step 9 precedes the runners |
| AC6, AC8, AC9 | PASS | `go test -run 'TestAssemble_AutoApplyDisabledWithPendingMigration' ./cmd/bot/`; the key is in `EnvKeys()` and `.env.example` |
| AC10 | PASS | `go test -run 'TestMigrateOnly_' ./cmd/bot/` |
| AC11 | PASS | `internal/store/migrations/00005_process_liveness.sql`; `go test -run 'TestMigrate_ProcessLivenessSeededWithNullInstant' ./internal/store/` |
| AC12 | PASS | `go test -run 'TestLiveness_RunWritesOncePerInterval' ./internal/scheduler/` |
| AC13, AC15, AC16, AC17 | PASS | `go test -run 'TestAbsorbDowntime_' ./internal/scheduler/` (overdue shifted, none stored, within threshold, ledger untouched, concurrent-once) |
| AC14 | PASS | `go test -run 'TestGuard_LivenessSourceCallsNoGoClock' ./internal/scheduler/` — AST guard with a proven-discriminating twin |
| AC18 | PASS | `go test -run 'TestProcess_ObserveDowntimeSetsTheRestartGauges' ./internal/health/` |
| AC19, AC20 | PASS | `go test -run 'TestAssemble_' ./cmd/bot/` — every failure step fatal, closers unwound backwards |
| AC21, AC22 | PASS | `go test -run 'TestServer_Readyz\|TestReadiness_' ./internal/health/ ./cmd/bot/` |
| AC23, AC26 | PASS | `go test -run 'TestNewProcess_' ./internal/health/` |
| AC24 | PASS | `go test -run 'TestVersion_LinkTimeOverrideReachesTheIdentityLine' ./cmd/bot/` |
| AC25, AC27, AC28 | PASS | `go test -run 'TestServe_' ./cmd/bot/` — two subject-timing cases inside a `synctest` bubble |
| AC29 | PASS | `goleak.VerifyNone` in the smoke condition |
| AC30, AC31 | PASS | `go test -run 'TestAssemble_HappyPath' ./cmd/bot/` — added by the Step 9 sweep |
| AC32 | PASS | `go test -run 'TestSmoke_' ./cmd/bot/` |
| AC33, AC34 | PASS | `make import-guard`; `go test ./cmd/importguard/`; the gate is in CI's Build job and listed in AGENTS.md |
| AC35 | PASS | `grep -n 'go run ./cmd/bot' AGENTS.md` — the claim is replaced by the measured start-up behaviour |
| AC36 | PASS | `go test -run 'TestGuard_ScrapeAndLogCarryNoSentinelSecret' ./cmd/bot/` |
