# Progress: posting-signature contract tests — ACTIVE
_Updated: 2026-09-15 12:21 UTC_

> Read THIS FIRST → ready to continue. No need to re-read the codebase.

**Branch:** feat/2026-09-15-posting-signature-contract-tests
**base_commit:** 47b42e978cafc9f0a991ce3267f5b6612de44ec4
**Last build:** PASS

**Issue:** #26
**Spec:** ai-docs/plans/2026-09-15-posting-signature-contract-tests.spec.md

**current_step:** Step 8 — Group A2 complete (subtask 8 done); Group B next
**last_passed_gate:** go build ./...; go test -count=1 ./internal/testdb/ ./cmd/testpg/; golangci-lint run (scoped and whole-module); golangci-lint fmt -d; make comment-refs
**entry_args:** 26

## Next action

**Do this immediately:** Group B (general-purpose, `inherit`) — subtask 7, Documentation (KD-42, domain-invariants, context.md layout line, KD-20 update).

## Subtasks

Group A — code, `sonnet`/`medium` via `code-writer`:
- [x] 1. The shared pool fixture — `internal/storetest` (D13)
- [x] 2. Move the scheduler, ingest and bot suites onto `storetest.Pool` (D14)
- [x] 3. Package scaffold, vocabulary and registry — `internal/contract` (D1–D5, D12)
- [x] 4. The pure matcher (D4, D7, D11)
- [x] 5. The contract check against real transactions (D6, D11)
- [x] 6. The declared set, then close the group with the whole-module gates (D8, D10)

Group A2 — code, `sonnet`/`medium` via `code-writer` (complete):
- [x] 8. Raise `ceilingMax` for the two-client targets (D15, D16)

Group B — instructions/harness, `inherit` via `general-purpose`:
- [ ] 7. Documentation (KD-42, domain-invariants, context.md layout line)

## Decisions log

Append-only, one line per non-trivial decision. Each line is prefixed with the step or phase that made it. Never edit or remove prior entries.

- **Step 7**: design-review round 1 GO; its notes 1–4 folded (e6bced8); note 5 put to the owner, whose answers 3.1 and 3.2 narrowed the scope — spec amended in interview round 4 (7883ed1), issue #26 body updated.
- **Step 7**: design reconciled with the amended spec (b4b1240); design-review round 2 GO; its notes and recommendations folded (4fc6316).
- **Step 7**: GO note 3 (pool-helper duplication) raised an owner-scope question; owner answers 5.1 «Да, перевести» and 6.1 «Да, со всеми вызовами» — D14 and subtask 2 (3e5762b, 47b42e9). Pre-amendment round-1 GO notes are no longer authoritative (Spec Amendment recipe step 7).
- **Step 8, subtask 1** (9c23758): built `internal/storetest` per D13 exactly as designed (`Pool`, its own `TestMain` via `leaktest.Main(m, testdb.Main)`, and `TestPool_migratedAndEmpty`). `Pool`'s root context is `context.Background()` with a `//nolint:forbidigo` in the same shape as `testdb.Schema`'s own root, since `storetest.go` is a non-test file forbidigo's `_test.go` exclusion does not cover. Adding `storetest` as a new `testdb.Main` caller made `internal/testdb`'s own `TestBinaries_matchesTree` fail; `Binaries` moved 5→6 and `TestCeiling_formula`'s fixture (comments and `want` values) were recomputed for the new constant — both in the same commit, in `internal/testdb`, outside subtask 1's own file list, because the manifest test ties them together.
- **Step 8, subtask 2**: deleted `newScheduler` (`internal/scheduler/scheduler_test.go`), `newIngestPool` (`internal/ingest/main_test.go`) and `newBotPool` (`cmd/bot/readiness_test.go`), each with its doc comment, and replaced every call (the 18 files the design's subtask-2 row lists) with `storetest.Pool(t)`. `golangci-lint fmt` (scoped to the three package trees only) added the `storetest` import and dropped the now-unused `context`/`log/slog`/`store`/`testdb`/`pgxpool` imports each definition file no longer needed; `git status --short` after confirms only the 18 listed files plus the deleted-helper files moved, nothing outside the three trees. `rg -n 'func new(Scheduler|IngestPool|BotPool)\('` returns no output — no copy remains. `go build`, `go vet`, `golangci-lint fmt -d`, `golangci-lint run`, `go test` and `go test -race` over `internal/scheduler`, `internal/ingest` and `cmd/bot` all green; `make comment-refs` green.
- **Step 8, subtask 3**: built `internal/contract`'s scaffold, vocabulary and registry per D1–D5, D12. `DocumentType` is an unexported `(basis, code string)` pair with `ManualCorrection`/`Event`/`DeferredTask`/`RecurrentTask` constructors, an unexported `playerOperation()` (no exported constructor, per D2), a `known()` predicate folding both the zero-`DocumentType` refusal and the empty-event/task-code refusal into one rule (a type-carrying basis needs a non-empty code, a table-only basis needs an empty one), and `String()`. `Sign` is `Positive`/`Negative` with the zero value unset (refused). `Cardinality` is `Exactly`/`AtLeast` with `n>=1` required. `Leg` is one struct with a `legClass` discriminator (not an interface — `PostingLeg` and `MovementLeg` both return `Leg`, matching D1's "the Leg constructors" phrasing), an unexported `key()` returning a `postingKey`/`movementKey` used for both the registry's duplicate-leg-key refusal and (subtask 4) the matcher. `Registry`/`NewRegistry`/`Declaration` hold every refusal D3–D5, D12 list; every refusal wraps `ErrInvalidDeclaration` and names the offending `DocumentType`, precedent `scheduler.NewRegistry` (D12). No `Check`/`Mark`/`Queryer` yet — those are subtask 5's D6/D11 file (`check.go`), matching the decomposition's file split. **A `Registry.signatureFor` accessor and `Cardinality.satisfiedBy` were drafted then removed**: with no caller until subtask 5/4 respectively, `golangci-lint run`'s `unused` linter (part of `default: standard`) would have failed subtask 3's own gate on its own commit; they will be (re)added with the subtask that first calls them. `String()` methods on `postingKey`/`movementKey`/`Sign`/`Cardinality` stay even though nothing calls them yet — `unused` exempts `fmt.Stringer` implementations. `internal/contract` is also a new `testdb.Main` caller (its `main_test.go`), so `Binaries` moved 6→7 (same manifest-test mechanism as subtask 1) and `TestCeiling_formula`'s fixture was recomputed again. That third bump pushed `2 * Binaries(7) * parallel * (schemaMaxConns+1) + ceilingSlack` past `ceilingMax(1000)` on this machine's `runtime.GOMAXPROCS(0)` (16) for the six `cmd/testpg` tests that call `run([]string{"--up", "--clients", "2"}, …)` with no explicit `--parallel` — a pre-existing gap the suite's own convention (`--parallel 1` pinned on every other ceiling-sensitive case, e.g. `run_test.go` line 572) was already guarding against everywhere else. Fixed by adding `"--parallel", "1"` to those six call sites (`TestRun_upOnAnUndersizedExistingServer_failsNamingTheCapacity`, `TestRun_upOnAServerSizedForFewerClients_failsNamingTheMount`, `TestRun_upReattachesWithoutVouching_refusesAndDoesNotRecordAskedCount`, `TestRun_upContainerExistsCheckFails_treatedAsAlreadyExisted_doesNotRecordAskedCount`, `TestRun_upRefusedOnMount_doesNotRaiseRecordedClients`, `TestRun_upStaleLocatorNamesAnotherServer_notRefused_recordsThisCount`), matching the sibling tests' own style — no assertion changed. `go build`, `go vet`, `golangci-lint fmt -d`, `golangci-lint run` (whole module), `go test ./internal/contract/...`, `go test ./internal/testdb/...`, `go test ./cmd/testpg/...`, `make comment-refs`, `make import-guard` all green.
- **Step 8, subtask 4** (D4, D7, D11): built the pure matcher. `Registry`/`Leg` stay unexported-data structs from subtask 3; the matcher operates on two new unexported shapes `postingRow`/`movementRow`/`document` (only `check.go`, same package, subtask 5, will construct them from real reads — no exported surface needed, since `conform_internal_test.go` is package `contract`). `mismatch` carries a `mismatchClass` (`cardinality`/`unexpected`/`unbalanced`) and a `String()` used both for a future `ErrNonconforming` render (subtask 5) and for test comparison; `conform` sorts its result by class+rendered-key (D11) but the test file re-sorts both sides itself (`renderSorted`/`assertMismatches`) so the assertions do not depend on that internal ordering choice. `Cardinality.satisfiedBy` (drafted then removed in subtask 3 for being unused) is reintroduced here, now that `conform` is its first caller. Tests cover every Test Design case (`TestConform_postingDimensions` incl. the kind-only dimension per D11's note that only a synthetic fixture can represent it, `TestConform_movementDimensions`, `TestConform_undeclaredRows`, `TestConform_anyBalanced`, `TestConform_property` via `pgregory.net/rapid`). **All three design-named mutants were hand-verified to go RED**, each applied to a `cp`-backed copy (`tmp/conform.go.bak`, never `.go`-suffixed) and reverted, confirmed byte-identical after (`diff`): kind-ignored-in-matching (had to be applied inside `conform`'s own matching maps, not `Leg.key()` alone — a first attempt mutating only `Leg.key()` left actual rows' hand-built keys in the test fixtures unaffected and the mutant stayed green, which is itself a finding about where the "kind ignored" defect must live to be observable); the unexpected-row pass skipped; the cardinality comparison skipped. **First `make comment-refs` run flagged decision-anchor and section-mark references** (`D4`/`D7`/`D11`/`§11` inside `.go` comments) the initial draft left in per the doc-convention ban — reworded to describe the rule itself ("a kind whose postings do not sum to zero", "the design corpus's real shop_sale signature") rather than cite the anchor; re-ran clean. `go build`, `go vet`, `golangci-lint fmt -d`, `golangci-lint run` (whole module), `go test ./internal/contract/...`, `make comment-refs`, `make import-guard` all green.
- **Step 8, subtask 5**: built `check.go` (`Queryer`, `Mark`, `NewMark`, `(*Registry).Check`) and its three test files. `Queryer` declares `Query`/`QueryRow`, matching `pgx.Tx` and `*pgxpool.Pool`. `NewMark` scans `max(id)` into a `*int64` (never a bare `int64` — pgx refuses a NULL scan there) and returns the zero `Mark` on an empty journal. `Check` pre-checks `want` against the registry before any read (`ErrNoSignature`, zero reads — asserted directly via the fake's own call counter); then runs three fixed reads (entries via one query classifying each entry's own `DocumentType` from `event.type`/`deferred_task.task_type`/`recurrent_task.task_type`/the two boolean basis-presence columns, falling to the zero `DocumentType` — always unsigned, since `NewRegistry` can never hold a row for it — when none of the five known columns is set, which is exactly the shape a probe table added by raw SQL past the mark produces, so `unknown_basis` needed no separate branch; postings; movements, each grouped by `journal_entry_id`) before judging any entry, then joins one wrapped error per failure class present (`ErrNoDocument`, `ErrNoSignature`, `ErrNonconforming`) via `errors.Join`. Fixtures (`helpers_test.go`): `storetest.Pool`, `newPlayer` via `store.CreateOwner`, `accountID`/`holderID` (SQL lookups by owner+scope+account code — the store package exposes no id-lookup helper of its own for non-World accounts), `fund` and `grantSlots` matching the `internal/store` test suite's own shape, and `testRegistry` with the Test Design's made-up shop_sale/shop_purchase/probe_grant shapes (the probe_grant signature includes the four slots capacity legs `store.Move` derives, not just the movement leg, matching the design's measured `from.free +1, from.used −1, to.free −1, to.used +1`). `check_internal_test.go` (package `contract`) drives `NewMark`/`Check` through a hand-rolled fake implementing the whole of `pgx.Rows`/`pgx.Row`, covering every read's three failure stages. `check_test.go` (package `contract_test`) drives them through real `store.Post`/`store.Move`/raw-SQL writes on a real schema, covering every Test Design case including the `unknown_basis` raw-DDL probe (a new table + column + replaced `journal_entry_exactly_one_basis` CHECK, all inside the test's own rolled-back transaction) and the released-savepoint case. **Two authoring bugs caught by the suite itself, not the design**: `TestCheck_manualCorrectionAdmitsAnyBalancedSet/money_pair` and `TestCheck_judgesByOwnType` each first asserted the wrong sentinel for a window that, on inspection, plainly could not produce the one asserted (a manual-correction-only window checked against `want=shop_sale` can only ever be `ErrNoDocument`, never `ErrNonconforming`) — fixed by re-deriving the correct expectation from the window's actual contents, not by loosening the assertion. **Both check.go-level design-named mutants hand-verified to go RED** (same `cp`-backup-and-diff discipline as subtask 4): a read's `rows.Err()` check dropped after `readEntries`'s loop (mutating only the *first* read, `readPostings`/`readMovements` untouched, still enough to redden `TestReads_returnEveryError`); an early return with `ErrNoDocument` inserted right after classifying entries, before `readPostings`/`readMovements` run at all — this is the "postings/movements cases turn into ErrNoDocument" mutant, reproduced by re-deriving `foundWant` a second time ahead of the later reads and returning on it, exactly the shape a well-meaning "skip the rest of the work when nothing can match" optimization would introduce. `go build`, `go vet`, `golangci-lint fmt -d`, `golangci-lint run` (whole module), `go test ./internal/contract/...` and `go test -race ./internal/contract/...`, `make comment-refs`, `make import-guard` all green.
- **Step 8, subtask 6 (Group A close)**: shipped `declared.go` — `declarations` is the one-element slice literal `{ManualCorrection(): AnyBalanced()}` (D10: no production code writes under any type yet, so nothing else belongs), and `Declared()` wraps `NewRegistry(declarations...)`. `declared_test.go` covers `TestDeclared_builds`, `TestDeclared_manualCorrectionAdmitsAnyBalancedSet` (a manual-correction money pair plus a `store.Move` mint under a second manual correction, both through the *shipped* set) and `TestDeclared_refusesPlayerOperationWrite` (a player-operation write beside a manual correction, checked through `Declared()`, returns `ErrNoSignature` naming `player_operation` — chosen per the design because no row can ever be added for that basis without first changing `contract` itself, D2). Group-closing gates: `go build ./...`, `make test` (whole module, real Postgres, every package green), `golangci-lint run`, `golangci-lint fmt -d`, `make comment-refs`, `make import-guard` all green; `go list -deps ./cmd/bot` has zero lines matching `internal/contract` or `internal/storetest` (confirmed by grepping the full dep list to a file and counting matches, not by piping through a truncating exit-status read). **`git diff --name-only main...`'s literal file-set check does NOT hold as the design's subtask 6 row states it** — three files outside `internal/contract/` and `internal/storetest/` are neither `_test.go` files subtask 2 lists nor untouched: `internal/testdb/server.go` (non-test, `Binaries` constant), `internal/testdb/server_test.go`, and `cmd/testpg/run_test.go`. All three are the documented, forced ripple from `internal/testdb`'s own `TestBinaries_matchesTree` manifest test (subtask 1: `storetest` becomes a new `testdb.Main` caller, Binaries 5→6; subtask 3: `contract` becomes a second new caller, Binaries 6→7, which on this machine's core count also pushed six pre-existing `cmd/testpg` tests' fixed `--clients 2` past `ceilingMax`, fixed by pinning `--parallel 1` to match that file's own sibling-test convention) — each recorded in its own subtask's decisions-log entry above, at the commit that made it, not retrofitted here. This is a genuine gap in the design's own verification text, not a scope violation: no design decision (D1-D14) is contradicted, no acceptance criterion is affected, and every touched line outside the two new packages is either a manifest-test-mandated integer bump or its arithmetic-comment/fixture follow-through. Reported here, at the checkpoint that names the check, rather than silently passed over.
- **Step 8, Group A return (orchestrator)**: branch, base_commit and clean tree re-validated; `go test -count=1 ./internal/testdb/ ./cmd/testpg/ ./internal/storetest/` green; no `func new(Scheduler|IngestPool|BotPool)(` left (control matched). Deviation from the design's file set: `testdb.Binaries` 5→7 (`internal/testdb/server.go`, `server_test.go`) and six `cmd/testpg/run_test.go` tests pinned `--parallel 1`. Reproduced: `testpg --clients 2 --parallel 16 -- true` exits 1, computed ceiling 1152 > ceilingMax 1000, so `make test-contention` and `make test-db-up CLIENTS=2` refuse on this 16-core host; KD-20 still says Binaries 5. Owner chose "Design Amendment (Recommended)" → design-writer + design-review round 3. Branch pushed to origin.
- **Step 8, Design Amendment close**: design amended (2398244: D15 keeps Group A's Binaries bump and testpg pins; D16 raises ceilingMax 1000→2000; subtask 8 and Group A2 added; subtask 7 gains the KD-20 update); design-review round 3 GO; its notes and recommendations folded without another round (@ ee4588c), GO notes G8–G12. Resuming Step 8 at Group A2.
- **Step 8, subtask 8 (Group A2 close)** (b026a13): added `TestCeiling_formula`'s two-client, host-core-count case (`clients: 2, parallel: 16`, `want: 1152`) first and ran it alone (`go test -count=1 -run TestCeiling_formula ./internal/testdb/ -v`) against the unchanged constant — confirmed RED (`--- FAIL: TestCeiling_formula`), because `2*Binaries(7)*16*(schemaMaxConns(4)+1)+ceilingSlack(32) = 1152 > ceilingMax(1000)`. Then raised `ceilingMax` 1000→2000 and rewrote its doc comment to carry D16's recipe in prose (at-capacity measurement, no pids limit on the wrapper's containers, host memory at full use as the bound, re-measure-to-raise-again) with no path/section/issue reference; re-ran the same case alone — GREEN. Updated the refusal case's (`clients: 10, parallel: 10`) arithmetic comment from `ceilingMax(1000)` to `ceilingMax(2000)`; its `wantErr: true` stayed unchanged since `3532 > 2000` still holds. **Gates**: `go build ./...` PASS; `go test -count=1 ./internal/testdb/ ./cmd/testpg/` PASS (both `ok`); `golangci-lint run` scoped to `./internal/testdb/... ./cmd/testpg/...` PASS (0 issues) and re-run whole-module PASS (0 issues); `golangci-lint fmt -d` PASS (no diff, target files absent from output); `make comment-refs` PASS. `git diff --name-only` against the commit shows only `internal/testdb/server.go` and `internal/testdb/server_test.go`, matching the decomposition row's file list exactly. **Reproducer**: `env -u LAB_GAME_TEST_DSN go run ./cmd/testpg --clients 2 --parallel 16 -- true` exited 0, `testpg: started a server, ceiling 1152, mount rw,size=1024m (clients=2, parallel=16)` (before the fix this same command exited 1 with the refusal, per the Group A return entry above). **`make test-db-up CLIENTS=2`**: checked both skip conditions first — `tmp/testpg-dsn` absent and `podman container exists lab-game-test-postgres` exited 1 (absent) — neither present, so the target was run (not skipped); exited 0, `testpg: shared server "lab-game-test-postgres" up, capacity 1152 admits the 1152 needed (clients=2, parallel=16)`. **`make test-contention`**: run with output captured to `tmp/test-contention.log` and `tmp/testcontention8.log` (no tail/head piping); exited 0; grepped the captured log for `exceeds ceilingMax` and `INSTRUMENT FAILURE` — neither present; final lines read `test-contention: clients=2 parallel=16` and `test-contention: instrument sound — logs clean, server answering`. Ran `make test-db-down` afterward per the exit-0 branch of the design's stated recipe; exited 0, `testpg: shared server down`. Group A2 is now fully complete — subtask 8 was its only member.

## GO notes

| # | round | note | kind | route | resolution |
|---|-------|------|------|-------|------------|
| G1 | 2 | D6 does not define the mark when no journal entry exists yet. | design-internal | folded | design § D6 zero mark, `TestNewMark_emptyJournal` @ 4fc6316 |
| G2 | 2 | § Risks says "A cancelled-context test drives each read's query-error branch" | design-internal | folded | design § D11 reads-before-judging, `TestReads_returnEveryError`, § Risks @ 4fc6316 |
| G3 | 2 | The migrated schema-pool helper (`testdb.Schema` → `store.NewPool` → `store.Migrate`) is already copied per package in 4 places | design-internal | folded | design § D13 @ 4fc6316; owner-scope extension § D14 + subtask 2 on answers 5.1 and 6.1 @ 47b42e9 |
| G4 | 2 | Two out-of-remit sentences. | design-internal | folded | both sentences removed @ 4fc6316 |
| G5 | 2 | D8's rules for the literal (catalog codes, named sign and cardinality, no helper) do not require one leg per line. | design-internal | folded | design § D8 "each leg on its own line" @ 4fc6316 |
| G6 | 2 | In the Test Design fixture list, label the test registry's rows as made-up shapes that borrow existing type codes, not the §11 signatures. | design-internal | folded | § Test Design fixture label @ 4fc6316 |
| G7 | 2 | `internal/store/errors.go:10`, cited in D1, points at the comment above `ErrNoBasis`; the declaration is on line 11. | design-internal | folded | D1 citation re-pinned to line 11 @ 4fc6316 |
| G8 | 3 | **Subtask 8's safety check for `make test-db-up CLIENTS=2` looks only for the locator file.** | design-internal | folded | subtask 8 run condition + no down after a failed up @ ee4588c |
| G9 | 3 | **Subtask 8's "Depends on" column reads "—".** | design-internal | folded | subtask 8 depends on 1, 3 @ ee4588c |
| G10 | 3 | **KD-20 states the old count twice, and subtask 7(a) names only one.** | design-internal | folded | subtask 7(a) names both phrases @ ee4588c |
| G11 | 3 | D16's claims hold under independent re-measurement: the refusal, the 2000-connection server at full use, and the missing pids limit on the wrapper's containers. | design-internal | folded | no change required; D16 stands @ ee4588c |
| G12 | 3 | **Round-trip required:** before Step 8 resumes, update the design doc to incorporate each note/recommendation above. | design-internal | folded | G8–G10 folded @ ee4588c |

## Key discoveries (don't re-investigate)

- No production code writes under any basis document today: the framework is exercised by its own test fixtures only.
- `internal/store`'s own pool helper cannot move to `storetest` (import cycle, measured in D13).

## AC Status

| AC | Status |
|----|--------|
| AC1 | PASS — `TestCheck_passesExactSignature` |
| AC2 | PASS — `TestConform_postingDimensions`, `TestCheck_failsOnPostingDifference` |
| AC3 | PASS — `TestConform_movementDimensions`, `TestCheck_failsOnMovementDifference` |
| AC4 | PASS — `TestConform_undeclaredRows`, `TestCheck_failsOnUndeclaredWrite` |
| AC5 | PASS — `TestCheck_judgesByOwnType` |
| AC6 | PASS — `TestCheck_unsignedTypeFails`, `TestNewRegistry_refusesPlayerOperation` |
| AC7 | PASS — `TestConform_anyBalanced`, `TestNewRegistry_refuses` (AnyBalanced-on-non-ManualCorrection cases), `TestCheck_manualCorrectionAdmitsAnyBalancedSet` |
| AC8 | PASS — `TestConform_anyBalanced`, `TestCheck_manualCorrectionAdmitsAnyBalancedSet` |
| AC9 | NOT_TESTED — deferred to Step 9's scratch-edit measurement (design § AC9), after subtask 6 ships `declared.go` |

## Review register

| id | raised | severity | status | verifying command |
|----|--------|----------|--------|-------------------|

## Files touched

- `internal/storetest/storetest.go`, `internal/storetest/storetest_test.go`, `internal/storetest/main_test.go` (new — subtask 1)
- `internal/testdb/server.go`, `internal/testdb/server_test.go` (Binaries 5→6, `TestCeiling_formula` fixture — subtask 1, forced by `TestBinaries_matchesTree`)
- `internal/scheduler/scheduler_test.go` (helper deleted), `internal/scheduler/{observe,liveness,worker_stop,schedule,reclaim,reconcile,failure,panic,worker,deadline}_test.go` (call sites moved — subtask 2)
- `internal/ingest/main_test.go` (helper deleted), `internal/ingest/{retry,gate,loop_stop,dead,offset,loop}_test.go` (call sites moved — subtask 2)
- `cmd/bot/readiness_test.go` (helper deleted, call sites moved — subtask 2)
- `internal/contract/contract.go`, `internal/contract/registry.go`, `internal/contract/errors.go`, `internal/contract/main_test.go`, `internal/contract/registry_test.go`, `internal/contract/registry_internal_test.go` (new — subtask 3)
- `internal/testdb/server.go`, `internal/testdb/server_test.go` (Binaries 6→7, `TestCeiling_formula` fixture — subtask 3, forced by `TestBinaries_matchesTree`)
- `cmd/testpg/run_test.go` (pinned `--parallel 1` on 6 pre-existing tests whose fixed `--clients 2` crossed `ceilingMax` at Binaries=7 on this machine's core count — subtask 3, forced by the Binaries bump)
- `internal/contract/conform.go`, `internal/contract/conform_internal_test.go` (new — subtask 4)
- `internal/contract/check.go`, `internal/contract/check_test.go`, `internal/contract/check_internal_test.go`, `internal/contract/helpers_test.go` (new — subtask 5)
- `internal/contract/declared.go`, `internal/contract/declared_test.go` (new — subtask 6)
- `internal/testdb/server.go`, `internal/testdb/server_test.go` (`ceilingMax` 1000→2000 and its comment, two-client `TestCeiling_formula` case, refusal case's comment value — subtask 8)
