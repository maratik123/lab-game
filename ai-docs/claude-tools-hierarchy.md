# Tool / Subagent / Skill / Hook inventory

The live registry of the harness surface in this repository. **`AGENTS.md` § *Propagation Rule* requires updating this page in the same PR as any change to a Tool, Subagent, Skill or Hook contract.** A row that no longer matches reality is a defect, not documentation debt.

The harness is being ported from `graphite-gp` in phases; this page lists what exists **now**, not what is planned.

Rules files, hooks and permissions landed in phase 1; the subagents and spec-driven skills in phase 2; the learning loop in phase 3; CI and the PR skills in phase 4.

## Hooks — `.claude/settings.json`

| Event | Matcher | Hook | Contract |
|---|---|---|---|
| `SessionStart` | — | ast-index sync | Updates the index, or rebuilds it when absent. Never fails the session (`\|\| true`). |
| `SessionStart` | — | rules reminder | Injects the instruction to read `CLAUDE.md` (→ `AGENTS.md`) and summarise the rules. |
| `PreToolUse` | `Bash` | main-branch commit guard | **Blocks (exit 2)** any `git commit` while `git branch --show-current` is `main`, printing the recovery procedure. The only enforcement of AXIOM 1 — `origin` has none. |
| `PreToolUse` | `Bash` | ast-index refresh | Updates the index before a commit. Advisory, never blocks. |
| `PreToolUse` | `Bash` | piped-gate guard | **Blocks (exit 2)** `go build/test/vet` / `golangci-lint run` / `golangci-lint fmt` / `gofmt` / `make` (matched as a class — every target of the `Makefile` is a gate) piped into `tail`/`head` without `set -o pipefail`. Matches command *text*, so a quoted example also matches — fails closed by design, and `make -n verify` piped into `head` is a known, accepted false positive. Regression suite: `ai-docs/scripts/test-piped-gate-guard.sh`. |
| `PreToolUse` | `Task\|Agent` | reviewer-spawn-contract guard | **Blocks (exit 2)** a `self-review` / `design-review` spawn whose prompt carries a line outside the closed list those agent files declare, printing the offending lines and the permitted shapes. Keyed on `tool_input.subagent_type`, so every other spawn passes untouched. Two gaps, and the first is structural: the matcher names the SPAWN tools, so a follow-up round delivered to a warm reviewer by `SendMessage` never reaches the guard at all — both agent contracts therefore state that the closed list binds the content, not the carrier. It also fails open on an unparseable payload or a missing `jq`, leaving the reviewer-side `PROMPT-CONTAMINATION` finding as the backstop. Regression suite: `test-spawn-contract-guard.sh`. |
| `PreToolUse` | `Bash` | gate-log path guard | **Blocks (exit 2)** a gate command (`go build/test/vet`, `golangci-lint run/fmt`, `gofmt`, `make`, `actionlint`, `shellcheck`) whose output is redirected to a bare filename, which lands in the repository root; a target with a slash passes. Other tools' writes to the root are not matched. Suite: `test-gate-log-path-guard.sh`. |
| `PreToolUse` | `Bash` | no-verify guard | **Blocks (exit 2)** `git commit` / `git push` with `--no-verify` or a short-flag cluster carrying `-n`, printing the two legitimate exits from a coverage-ratchet block. Suite: `test-no-verify-guard.sh`. |
| `PreToolUse` | `Bash` | review-register commit gate | On `git commit`, runs `ai-docs/scripts/check-review-register.sh` over every staged `*.progress.md` and **blocks (exit 2)** when a round table and the register disagree. Reads the index before the command runs. Suite: `test-review-register.sh`. |
| `PreToolUse` | `Bash` | instruction-edit guard (Bash side) | **Blocks (exit 2)** a command that writes into `AGENTS.md`, `CLAUDE.md`, `.claude/**`, `ai-docs/code-style.md` or `ai-docs/doc-convention.md` (a redirect into one, `sed -i`/`tee`/`rm`/`git mv` naming one, `cp`/`mv` whose last argument is one, a `write_text`/`open(…,"w")` in a command that names one) while an `ai-docs/plans/*.spec.md.state.md` exists and `ai-docs/plans/.task-inflight` does not — Boundary rule 2, between `/interview` round 1 and `/task` Step 8. Reads (`sed -n`, `grep`, `cat … \|`), copies OUT of an instruction file, `git checkout --`/`git restore` of one, and every write to `ai-docs/plans/**` or the two logs pass. Fails closed: a body that cannot parse blocks everything, which its suite would show. |
| `PreToolUse` | `Edit\|Write\|MultiEdit\|NotebookEdit` | instruction-edit guard (Edit side) | Same rule keyed on `tool_input.file_path`, same two-state condition, same message. The path is classified relative to the repository root (the hook's working directory): a path outside the repository — the user's own `~/.claude/` memory, another tree's `AGENTS.md` — is never an instruction file here. |
| `PostToolUse` | `Write\|Edit` | `golangci-lint fmt` | Formats a written `.go` file in place, path-scoped to that file — so it applies every formatter `.golangci.yml` enables (`gofumpt` included) and rewrites no sibling. Keeps the hook from re-introducing what the format gate rejects. |
| `PostToolUse` | `Write\|Edit` | panic-gate | **Warns (exit 2)** on `panic(` / `log.Fatal*` / `log.Panic*` in a non-`_test.go` file; points at `ai-docs/panic-index.md`. Exit 2 is non-blocking on `PostToolUse` (the write already happened) and is the only exit status whose stderr reaches Claude — an exit-0 stderr goes to the debug log only. |
| `PostToolUse` | `Write\|Edit` | secret-gate | **Blocks (exit 2)** when a written file contains a string shaped like a live Telegram bot token (`\d{8,10}:[A-Za-z0-9_-]{35}`). |
| `PostToolUse` | `Bash` | in-flight marker stamp | When the main agent's command creates `ai-docs/plans/.task-inflight` or appends a `claim:` line to it, appends `owner: <session id>` from the hook's own input. A subagent's command never stamps. Never blocks. |
| `Stop` | — | `/task` in-flight gate | **Blocks (exit 2)** ending a turn of the marker's owning session while the marker exists and no hand-back token is in its tail; appends `blocked:` to the marker's ledger; spends a token it permits. A stop by any other session is released untouched. Contract: `.claude/skills/task/reference.md` § In-flight marker. |
| `PostToolUse` | `Bash` | PR-body sync | After a `git push` on a branch with an open PR, reminds to re-read the PR body (AXIOM 2). Advisory, delivered via exit 2 for the same reason as panic-gate. Matches command *text*, so a quoted push command inside a heredoc fires it too — harmless noise, never a block. |

Verification protocol for any hook change: [`hook-verification.md`](hook-verification.md). All **seventeen** bodies pass `shellcheck -s bash`. This count has been stale here twice — it read nine while there were ten (corrected 2026-08-30), and eleven while there were fourteen (corrected 2026-09-08) — so re-derive it rather than trusting the line: `jq -r '.hooks[][].hooks[].command' .claude/settings.json` yields `SessionStart` 2 · `PreToolUse` 9 · `PostToolUse` 5 · `Stop` 1. The blocking ones were exercised against real commands, including innocent ones containing the matched substring. The two bodies changed on **2026-08-30** — the `Write\|Edit` formatter and the piped-gate guard — were re-verified against all three MUSTs on that date; the guard's MUST 2 is the 26-fixture matrix its regression suite now runs in CI, and MUST 3 confirmed in-session that an edited `.claude/settings.json` is live and the load-bearing `tool_input` field populated.

The body added on **2026-09-04** — the reviewer-spawn-contract guard of PR #48 — was verified against all three MUSTs on `29fcdd1`. MUST 1: all eleven bodies through `shellcheck -s bash`. MUST 2: its regression suite runs the live body against the four spawn templates this harness ships and against the contaminated round-2 prompt recorded in that run's review register, both directions asserted. MUST 3, by the passive probe this page's protocol prescribes: the real spawn tool is named `Agent`, so the matcher `Task|Agent` matches; `tool_input` carries exactly `description`, `prompt` and `subagent_type`, which are the two fields the body reads plus one; and `prompt` arrives verbatim, with no wrapper and no leading whitespace of its own. **An edited `.claude/settings.json` is live in both directions** — the probe stopped firing the moment `git restore` removed it, silent across every Bash call that followed — which extends the 2026-08-30 finding above, covering only installation. Consequence worth having: a temporary diagnostic hook does not outlive its revert, so the MUST-3 procedure leaves nothing behind in the session it ran in.

The two advisory bodies changed on **2026-09-01** — panic-gate and PR-body sync — moved from exit 0 to exit 2, because an exit-0 stderr reaches the debug log only (the hooks reference: Claude never sees it). Re-verified that day: MUST 1 — all ten bodies through `shellcheck -s bash`; MUST 2 — panic-gate: a real `Write` of a `.go` file containing `panic(` was silent before the change and its `[panic-gate]` message arrived in the transcript after it (reproduced independently by the self-review round on that commit); PR-body sync: the innocent path — a `git push --dry-run` on a branch with no PR — stays silent, and the OPEN-PR path — a no-op `git push` on this branch once its PR (#13) was open — delivered the `[pr-sync] open PR #13 …` line into the transcript; MUST 3 — `tool_input.file_path` populated on the `Write` path and `tool_input.command` on the `Bash` path (each visible message names the file or the PR the field resolved to).

## Rules — `.claude/rules/`

| File | Loaded when | Contract |
|---|---|---|
| [`ast-index.md`](../.claude/rules/ast-index.md) | Any code-search task | ast-index first; grep only for regex / string literals / comments / non-Go files. Negative results are not evidence. |

## Subagents — `.claude/agents/`

| Agent | Model | Spawned by | Contract |
|---|---|---|---|
| `spec-writer` | inherit | `/interview`, `/task` Steps 1–5 | Drafts the spec one interview round at a time; asks 0–3 questions per round or returns `ready` / `unresolvable`. Never implements. Reads every linked issue / PR itself (`gh`) and has `WebFetch` / `WebSearch` (owner's decision 2026-09-08: sources outside the tree are read, not relayed). Writes only what the task needs to count as solved: every Scope item, Key decision and AC ends with an anchor quoting the owner's words (`check-spec-anchors.sh`), and how the solution is built is the design's. |
| `design-writer` | inherit | `/task` Step 6 | Produces the design document with decomposition and a `## Handoff plan`. Reads the binding-constraint file for anything it specifies, every linked issue / PR, and — no `tools:` line, so it inherits the full set — the web where the design depends on a page. Writes no code. |
| `design-review` | inherit | `/task` Step 7 | Reviews a design against the task (the spec's interview state file), the spec and the checklist, issues GO / ITERATE / STOP; raises `SPEC-REMIT` against a spec row outside the spec's zone. Loops with `design-writer`; a GO is final for its design-internal notes, which are folded in without another round (`/task` Step 7). |
| `code-writer` | sonnet, effort medium (pinned in frontmatter) | `/context-reset` group handoff | Implements a group's subtasks sequentially, gates and commits per subtask. **Never** pushes, opens a PR, runs self-review, or spawns anything. |
| `self-review` | inherit | `/task` Step 10, `/bugfix` Step 6, `/project-review` | Reviews the implementation diff against the task, the spec and the design; APPROVE / REJECT. The push gate. |
| `review-findings` | inherit | `/project-review` | Walks the whole codebase (no diff, no spec) and writes a findings table into the progress file. |

| `self-improve` | inherit | `/improve` | Reads `learnings.md` for repeating patterns, proposes instruction diffs, escalates to hooks at ≥3 occurrences. Re-verifies every factual claim it carries out of an entry. Writes no code. |
| `self-reflect` | inherit | `/reflect` | End-of-work retrospective: a structured good/bad list, each finding routed {learnings \| ticket \| none}. Assembles and yields; the parent performs every write. |
| `learnings-escalation-audit` | opus | `/ai-audit` Phase 1 | Verifies every entry's `Escalated?` and `Superseded by:` still point at something real; fixes drift **only** in those two fields. |
| `triage-runner` | opus | `/triage` | Promotes untracked `_inbox.jsonl` rows to issues, drains the queue, reconciles JSONL ↔ issue divergence. Mutation scope is `ai-docs/deferred/**` + `gh issue` only. |

Model column: `inherit` = the orchestrator's model, via `model: inherit` in the agent's frontmatter; a named alias is a frontmatter pin. No spawn passes an inline `model=` — the frontmatter is the only lever.

Not ported from the source harness: `image-check` (verifies a golden *image* against its drawing code — this project's goldens are text, and `code-writer` reads them itself).

## Skills — `.claude/skills/`

| Skill | Invocation | Contract |
|---|---|---|
| `/task` | explicit | The full workflow: interview → spec → design → design-review → implementation → verify → self-review → PR. Steps are strictly ordered; 6 / 7 / 10 cannot be skipped. Pre-authorises its own commits, push and `gh pr create`. |
| `/interview` | explicit or via `/task` | Drives the spec-drafting rounds through `spec-writer`; never drafts the spec itself. Runs the three spec gates itself before the owner approves — its whole check of the spec's zone is their exit codes. |
| `/context-reset` | at every design-defined group boundary, and on compaction recovery | The handoff protocol: spawns the group's implementor and re-validates state on return. |
| `/project-review` | explicit | Whole-codebase review on the current branch: `review-findings` → fix loop → `self-review` until APPROVE, then commits. |
| `/bugfix` | model-invocable on failure signals | Trace → root cause → failing test → fix. Analysis before code; the test is written before the fix. |
| `/next` | explicit | Recommends one task to work on next, with runner-ups. |
| `/verify-change` | explicit | Runs `go test ./...`, optionally filtered. |
| `/improve` | explicit | Launches `self-improve`; the escalation path from the corrections log into instruction files and hooks. Run at ≥3 unescalated corrections or ≥2 validations. |
| `/reflect` | explicit | Launches `self-reflect`, then applies each finding per the user's per-finding routing consent. |
| `/ai-audit` | explicit | Two phases: (1) `learnings-escalation-audit` fixes field drift; (2) the main session audits the whole instruction surface for dead references, format violations and size-cap breaches. Ships four shell guards — `check-citations.sh`, its regression test, and the two hook regression suites (`test-piped-gate-guard.sh`, `test-spawn-contract-guard.sh`). |
| `/triage` | explicit | Launches `triage-runner`; batched promotion of deferred rows to issues. Default threshold ≥3 unhandled rows. |
| `/pr-commented` | explicit | One round of reviewer-comment response: classify each unresolved thread, bundle fixes into one commit, self-review, push, reply and resolve per category. |
| `/pr-ci-failed` | model-invocable | One round of CI-failure response on the current PR: classify, reproduce locally, fix, self-review, push, re-read the PR body. |
| `/main-ci-failed` | model-invocable | Same, for a red run on `main` — the fix lands on a NEW branch and a new PR; `main` is never modified directly. |
| `/pr-merged` | explicit | After a merge: switch to `main`, pull, delete the fallback progress files, delete the local branch. |
| `/dependabot-pr` | explicit | One round of triage on a Dependabot **gomod** PR. Never auto-merges, never pushes to the bot branch; prints the merge command and pauses. |

Built-in Claude Code commands (`/code-review`, `/simplify`, `/security-review`, `/init`) are **not** part of this harness and are not governed by this page. They overlap `self-review` / `project-review` in purpose but not in contract: the harness surfaces review against *this* project's spec, design and domain invariants, and gate the push; the built-ins review a diff on general principles and gate nothing. Use the harness surfaces inside a `/task` flow; the built-ins are fine ad hoc.

The port is complete: every subagent and skill the source harness carried, minus the ones whose subject this project does not have (`image-check`, the design-system skill).

## Shell guards — `ai-docs/scripts/` and `.claude/skills/<skill>/scripts/`

**Placement rule (2026-09-08).** A script lives under `.claude/skills/<skill>/scripts/` only when that skill is its sole caller and owner (`/ai-audit` Checklist P's citation guard and its suite; `/task` Step 12's task-run writer and its suite; `/pr-merged`'s cleanup). Everything else — the regression suite of a hook in `.claude/settings.json`, a CI gate over specs or registers, a helper two agents share — lives in `ai-docs/scripts/`. Before this rule, eleven of thirteen files under `ai-audit/scripts/` had nothing to do with `/ai-audit`; the directory was the first one that existed. Rows below name the full path.

| Script | Run by | Contract |
|---|---|---|
| `.claude/skills/ai-audit/scripts/check-citations.sh` | `/ai-audit` Phase 2, or standalone | Every `#N` / learnings-date / memory-file citation must resolve **for its reader**: local refs resolve here, inherited ones name `graphite-gp` or `quartzite`. The local ceiling is the newer of the newest issue and the newest pull request — they share one number sequence. Exit 1 on any unresolvable citation, and on an unreadable ceiling. |
| `.claude/skills/ai-audit/scripts/test-check-citations.sh` | before editing the guard | Locks the content-addressed (never line-pinned) exclusion, that the guard restores the tree it edits, and — through a stand-in `gh` — that a ref above the newest pull request but not above the newest issue is local, a ref above both is not, and an unreadable issue ceiling is an instrument error. |
| `ai-docs/scripts/test-piped-gate-guard.sh` | before editing the piped-gate hook, and by CI's Harness-guards job | 26 fixtures through the hook body **extracted from `.claude/settings.json`**, never a retyped regex: 15 must-block, 10 must-allow, plus the known false positive asserted as blocked. Fails when a regex edit breaks either direction. |
| `ai-docs/scripts/test-stop-gate.sh` | before editing the `Stop` hook or the in-flight marker stamp, and by CI's Harness-guards job | Both live bodies **extracted from `.claude/settings.json`**, each case in its own sandbox: the recorded one-token-two-stops replay must block its second stop; a token is spent by the stop it permits, and an unspendable one fails open with a warning; the ledger counts Step 12 reads; the owning session's stop blocks while another session's is released without a ledger line or a spent token; a marker with no owner, or a stop with no session id, gates as before; the stamp records the main-agent session that creates or claims the marker, never a subagent, a hand-back or a read. |
| `ai-docs/scripts/test-spawn-contract-guard.sh` | before editing the reviewer-spawn-contract hook, and by CI's Harness-guards job | 20 fixtures through the hook body **extracted from `.claude/settings.json`**: the four spawn templates the harness ships (must-allow, placeholders realised), the round-2 prompt recorded in the 2026-09-03 register and both pre-unification template shapes (must-block), and the fail-open paths. Asserts both contracts still say "exactly five things", so a sixth permitted item cannot land without touching the guard. |
| `ai-docs/scripts/test-instruction-edit-guard.sh` | before editing the instruction-edit hook, and by CI's Harness-guards job | Fixtures through both hook bodies **extracted from `.claude/settings.json`**, run in a scratch directory set to each of the guard's three states (live / in-flight / no interview): the real 2026-09-08 `write_text` command and its `Edit` equivalent must block; reads, copies out, the `git checkout --` that undid it, and every log/plans write must pass; a path outside the repository — the user's own memory directory, another tree's `AGENTS.md` — must pass while an interview is live. |
| `ai-docs/scripts/check-spec-shape.sh` | `spec-writer` before every `ask` / `ready`, and by CI's Harness-guards job | A `*.spec.md` stores no bare `path:line` (a `<commit>:` pin or a `§ section` / symbol locator instead), no counting command inside a `[source: … · <command>]`, no `### Sizing`. Default mode judges only the lines the branch added — the eight merged specs carry 109 legacy hits nobody touched. Exit 1 on a hit. |
| `ai-docs/scripts/test-spec-shape.sh` | before editing the guard, and by CI | The five `## Source conflicts` lines of the rejected 2026-09-08 round-1 spec and its `### Sizing` must hit; pinned coordinates, URLs, clock times, fenced code and the visible `spec-shape: example` exemption must pass; default mode proven on a scratch repo in both directions. |
| `ai-docs/scripts/check-spec-anchors.sh` | `spec-writer` before every `ask` / `ready`, `/interview` Step 4 before the owner approves, and CI's Harness-guards job | Every `## Scope` item, `## Key decisions` row and `## Acceptance Criteria` row ends with `[task: "…"]` or `[answer <round>.<n>: "…"]`; while the interview state file is on disk, each quote must occur in the task text or in that answer. CI has no state file, so there it checks presence only and says so. Default mode judges only the units the branch added or touched. It proves a source, not a remit — a mechanism under a real quote is `spec-writer.md` Rule 11's and `design-review`'s `SPEC-REMIT`. Exit 1 on a finding. |
| `ai-docs/scripts/test-spec-anchors.sh` | before editing the guard, and by CI | The rows the owner struck from the 2026-09-10 spec, verbatim, must hit — and so must quotes of a label, a linked number or a question; the kept rows, anchored, must pass; plus a superseded body, free-text mode, a missing state file, the retired copy, and default mode on a scratch repo in both directions. |
| `ai-docs/scripts/doc-edit-guard.sh` | `spec-writer` and `design-writer` around every scripted edit of their document | Two modes, stated by its own `--help`: one snapshots the file and counts `## ` headings, `| AC` rows, `| D`/`| KD` rows and code fences; the other restores from that snapshot and exits 2 when any count shrank — the heading-anchored slice that truncated a design (2026-09-02) and a spec (2026-09-08). |
| `ai-docs/scripts/test-doc-edit-guard.sh` | before editing the guard, and by CI | Replays the real truncating edit (`index("## Open questions")` landing on a table-cell mention) and asserts the restore; the same edit anchored on the heading line passes; growth passes; a dropped AC row is caught; `verify` without a snapshot is itself exit 2. |
| `.claude/skills/task/scripts/append-task-run.sh` | `/task` Step 12 sub-step 5a | Single writer of `ai-docs/metrics/task-runs.jsonl`. Degrades rather than halting Step 12. |
| `.claude/skills/task/scripts/test-append-task-run.sh` | before editing the writer | 20 cases; AC6 asserts the case count equals `ai-docs/task-run-schema.md` § *Cases* — add a row there in the same commit as a new case. |
| `.claude/skills/pr-merged/scripts/cleanup-progress.sh` | `/pr-merged` step 3 | Deletes only the fallback progress files of the merged PR (`pr-comments/`, `ci-fixes/`). Flow-owned state files are retired by their own flow before its PR and are left alone. |
| `ai-docs/scripts/check-script-shape.sh` | before editing any script's argument handling, and by CI's Harness-guards job | Four properties over the tracked tree: a help dispatch is exactly one line and byte-identical to the fixed shape; nothing runs before it (walked, not grepped — comments, a `set` line, function definitions and assignments without a command substitution may precede it, nothing else); the flag exits 0 and prints a non-empty block; and a tracked file beginning with `#!` is named with the shell extension or is a symbolic link to one that is. Says nothing about whether a script *must* answer the flag. |
| `ai-docs/scripts/test-script-shape.sh` | before editing that checker, and by CI | One sandbox repository per defect — a divergent dispatch shape, a dispatch something runs before, a non-zero answer, an empty answer, a bare shebang file, a link to an extensionless one — each asserted to be named in the report, plus the all-conforming sandbox as the instrument check. |
| `ai-docs/scripts/check-harness-gaps-forge.sh` | CI's Harness-guards job | The two closing fields of `ai-docs/harness-gaps.md` — `**Forge:**` (the forge that took an entry) and `**Closed by:**` (the pull request that fixed one in full outside any forge) — are a forge's record. Over the whole log (fenced skeleton skipped): `forge-<N>[, forge-<M>]` and `#<N>[, #<M>]` respectively, inside an entry, each at most once per entry. Against the merge base with `main`: a line of either field added or removed on a branch not named `harness/forge-<N>` is refused, and on a forge branch no added `**Forge:**` value names a forge above its own number. The branch name is the whole provenance signal — a local commit is not refused, the PR is; with no merge base the provenance half is skipped and says so; a `**Closed by:**` number is left to the citation guard. Exit 1 on a finding. |
| `ai-docs/scripts/test-harness-gaps-forge.sh` | before editing that guard, and by CI | Shape fixtures on one log for both fields — malformed values, a trailing note, a field twice in one entry, a field before the first entry — and the unfenced skeleton as the instrument check for the fence skip. Provenance on a scratch repository: a non-forge branch that adds, removes or extends either field fails, as do `harness/forge-x` and a detached HEAD; the same edits pass on `harness/forge-15`, and `forge-16` on it fails; main after the merge passes; no merge base passes and says so. |

Every regression suite must pass `shellcheck -s bash` and run green before `git add` (`AGENTS.md` § *Build & Test*); CI's Harness-guards job runs the full list in `.github/workflows/ci.yml`.

## Permissions

`allow` covers the project's own toolchain (`go`, `gofmt`, `golangci-lint`, `make`, `git`, `gh`, `ast-index`, `psql`, `actionlint`, `shellcheck`) plus read-only text tools, and — since 2026-09-08 — `WebFetch` and `WebSearch` unrestricted, so a background delegate (`spec-writer`, `design-writer`) never hits a permission prompt it cannot answer. Owner's reasoning, recorded as the primary fact: the delegates search for best practices and read the documentation of podman, Postgres and the other tools the project runs; neither has an intent to browse at large, so a domain list would only ever be stale. `deny` covers `.idea/**`, `**/secrets*` and `**/.secrets*`, and the environment files that carry a real secret — `**/.env` plus one entry per named variant (`.env.local`, `.env.development`, `.env.test`, `.env.staging`, `.env.production`, `.env.secret`, `.env.secrets`). The bot token and the database DSN must be unreachable to both `Read` and `Edit`, and `Edit(...)` also governs `Write`. **`.env.example` is deliberately outside the deny**, because `.gitignore` negates it and the configuration layer authors it; the rules enumerate the real files rather than using a `**/.env.*` catch-all because the matcher takes gitignore syntax with no in-pattern negation, and `deny` is final — no `allow` entry can carve an exception out of a broad deny. A new environment file whose name is not on that list is therefore **not** denied until it is added.

## CI — `.github/workflows/ci.yml`

| Job | Runs when | Gates |
|---|---|---|
| Format | `go` paths changed | `make fmt-check` — `golangci-lint fmt -d` prints no diff |
| Build | `go` paths changed | `make build`, `make vet`, `make tidy-check` (`go mod tidy` leaves no delta), `make import-guard` (no rule-table package's non-test dependency graph carries a forbidden module — today, the container runtime in the bot's) |
| Test | `go` paths changed | `make test` — the whole suite against the one Postgres the provisioning wrapper reuses or provisions |
| Race | `go` paths changed | `make test-race` — the same suite under `-race`, the only one of the five whose target passes the flag |
| Coverage ratchet | `go` paths changed | `make cover-ratchet` — the ratchet script in `--check` mode, which measures and compares but never records a new mark |
| Test fallback | `go` paths changed | `make test-fallback` — a bare whole-module run with the DSN variable cleared, the only CI route that still reaches the per-binary container path |
| Architecture | `go` paths changed | `make test-arch` — the two generation packages' tests rebuilt and re-run under a second `GOARCH`, asserting both that the run passes and that its word size differs from the host's. The runner is this gate's reference environment: it skips loudly on a machine that cannot execute the second architecture's binaries and refuses loudly here |
| Lint | `go` paths changed | `make lint` at the pinned `golangci-lint` version, then `make file-limits` (hard 1000 / 1500 line limits) |
| Harness guards | `.claude/**`, `ai-docs/**`, `AGENTS.md`, `CLAUDE.md`, `.githooks/**`, `Makefile` changed | `make shellcheck` — the same command a local run uses, so the two cannot disagree about which scripts are checked — plus shellcheck on every hook body; the citation guard; the `context-status.md` PR-locator check; the script-shape checker; the harness-gaps closing-field provenance check; the three spec gates (AC shape, spec shape, row anchors); every guard regression suite; every relative markdown link resolves. **No size check**: instruction-file bytes are `/ai-audit`'s exclusive property per the AXIOM in [`checklist-m.md`](../.claude/skills/ai-audit/checklist-m.md) § *Sub-check 9*, and CI deliberately carries no gate over them |
| Comment references | any path of the gated set changed — `**/*.go`, `**/*.sh`, `**/*.sql`, `**/*.yml`, `**/*.yaml`, `.gitignore`, `.env.example`, `Makefile`, `.githooks/**` | `make comment-refs` — no comment in a gated file points outward. Exit 2 is an instrument failure, never a pass |
| Actionlint | `.github/workflows/**` changed | `actionlint` via reviewdog |

**A filtered-out job is not a passing job.** `dorny/paths-filter` decides what runs; a new artefact class must be added to its filter in the same PR that introduces it, or its gate silently stops running.

**No check is required at the merge button** — GitHub refuses rulesets on a private repository on the free plan (`AGENTS.md` § Permissions). CI reports; discipline enforces.

## Dependabot — `.github/dependabot.yml`

Weekly, two ecosystems: `gomod` (commit prefix `build`) and `github-actions` (prefix `ci`), 5 open PRs each. `/dependabot-pr` triages the `gomod` ones; a `github-actions` PR bails at preconditions as out of scope for v1.
