# План переделки топологии мира и генерации (MVP)

Статус: **черновик плана, не решения.** Файл раскладывает решения, принятые владельцем в сессии 2026-09-13, по правке [DESIGN.md](DESIGN.md) и по issue. Имплементировать из этого файла нельзя: решения становятся обязательными только после переноса в DESIGN.md (issue N1 ниже). Цитаты в таблице решений — дословные формулировки владельца.

---

## 0. Что решено

| # | Решение | Формулировка владельца |
|---|---|---|
| D1 | Чанк — гекс радиуса R (ориентир R=9, значение — конфиг). Чанки образуют гексовую суперрешетку: 6 равных границ по 2R+1 грани, на каждой 1–2 гарантированных портала. Спаннинг-структура, доп. проходы и островки — по прежним правилам. | «Форма чанка: гекс радиуса R, ориентир R=9» (саммари обсуждения, раздел «Решено») |
| D2 | Все дистанции для формул (глубина, PvP-градиент, бюджет монстров, множитель волн) считаются в координатах суперрешетки, гексовой метрикой по чанкам. | «Все дистанции для формул … считаются в координатах суперрешетки» (там же) |
| D3 | Карта чанка хранится, заново не генерируется. | «мне казалось, что нужно хранить карту чанка, чтобы не генерировать заново» |
| D4 | Чанк создается лениво и целиком — при переходе границы или при осмотре клетки в несуществующем чанке. Осмотр — на одну клетку; на стыке трех чанков это создает до двух чанков. Уникальный индекс по (мир, координата чанка). | «если требуемая локация в несуществующему чанке - он создается автоматически (если не создан ранее)»; «осмотр пока на одну клетку, на границе, где сходятся 3 чанка и открыт только 1 это приведет к генерации сразу 2 чанков»; «нужен уникальный индекс, чтобы не было вставок чанков по одинаковым координатам» |
| D5 | Первый активированный чат занимает центральный чанк («середина мира»). Каждый следующий — первый **несозданный** чанк по спирали от центра, отстоящий от всех ворот не менее чем на k чанков (сами чанки ворот не считаются). k — параметр мира: k=0 — концентрические кольца от центра. | «первый чат занимает первый чанк (назовем это "середина мира")»; «располагается по спирали в ближайшей свободной дырке по направлению спирали от центра»; «k - это минимальное расстояние в чанках между чанками с воротами в чаты, не считая самих чанков с воротами» |
| D6 | Активация чата в мире — первый рейд соло-игрока или группы из этого чата. Активация — своя в каждом мире. | «активация - первый рейд соло-игрока или группы из чата»; «у каждого мира отдельная активация» |
| D7 | Телеметрия: одно общее событие создания чанка, отдельное от `raid_started`. Тип чанка и причина создания (активация чата / исследователь) — раздельные поля. | «нужно просто событие создания чанка как общее, а его признак - создано чатом или исследователем»; «да, раздельно тип и причина»; причина: «в дальнейшем после мвп механика генерации мира может поменяться, в том числе и условия генерации» |
| D8 | Боссов и руин в MVP нет (DESIGN §14). Тип чанка — открытое перечисление, чтобы пост-MVP типы (босс-область, руины) не требовали переделки. | Следствие §14 и преамбулы DESIGN.md (код MVP не зашивает допущений против спроектированного пост-MVP) |
| D9 | Выталкивание новых ворот наружу активными исследователями — не чиним заранее, наблюдаем. | «когда встретимся с таким - тогда и будем решать, что надо чинить в следующем сезоне» |
| D10 | Порядок в следующих сезонах — вне скоупа MVP; k как средство разрядить обстановку при ротации — драфт. | «как это будет в дальнейших сезонах - пока вне скоупа мвп (например, есть параметр k, который позволит разрядить обстановку, но это пока идея на уровне драфта)» |
| D11 | Вход-префаб — 5–6 вариантов локации у ворот в центре чанка, в пределах некоторого радиуса, **не весь чанк**; остальное пространство чанка — ткань. Цель — чанк с воротами неотличим снаружи. | «нужны 5-6 вариантов префаба локации у ворот в каком-то радиусе, но не весь чанк (иначе снаружи будет понятно по вервым узорам, что впереди чанк с чужой дверью)» |
| D12 | Мир за спиралью бесконечен. | «да, не вижу смысла в ограничении» |
| D13 | Цена двери считается от ворот своего чата и плавно растет до запретительной по мере приближения к чужой двери; в локации с чужой дверью ставить дверь запрещено. Цель — исключить кемпинг на новичков, когда появится PvP. Двери не в MVP: это решение для §2.3, форма кривой — балансовые числа (§16.5). | «от ворот своего чата, при этом должна плавно увеличиваться до запретительных величин с уменьшением расстояния к чужой двери вплоть до запрета ставить дверь в локации с чужой дверью (чтобы устранить кемпинг на новичков, когда появится pvp)» |
| D14 | Действие «осмотреться» (включая создание чанков) — в #36; #37 — только представление. | «#36 там же основная механика, а #37 - это про представление» |
| D15 | Посещенные клетки построчно не хранятся; хранится только состояние клетки там, где оно есть (установленная дверь и т.п.). | «под состояние клетки, наверно, не очень понимаю, что даст хранение посещенных клеток, кроме разве что клеток с установленными дверями» |
| D16 | Спиральное размещение ворот — отдельный issue (N3). | «отдельный issue» |
| D17 | Тела существующих issue правятся вместе с созданием N1–N3. | «правим вместе с созданием N1-N3» |

### Поправки к исходному саммари

- «6 границ вместо 4» неточно: у текущих чанков-параллелограммов тоже шесть соседей с равноудаленными центрами, но четыре границы длинные, а две диагональные — в одну грань. Точная формулировка: **шесть равных границ вместо четырех длинных и двух одногранных**.
- Формулы «n = (k+1)²−1» и «расстояние между соседними чатами (k+1)·(2R+1) гексов» в DESIGN.md не переносить. Первая описывает регулярную подрешетку, а жадная спираль из D5 дает иную, более плотную расстановку. Вторая неверна для гексов: суперрешетка повернута относительно исходной, и расстояние между центрами чанков в гексах зависит от направления. В DESIGN.md дистанции — только в чанках (D2).

### Следствия решений (записываются в DESIGN.md вместе с ними)

- **Граница чанка не зависит от его типа** (из D11). Порталы на границе уже выводятся только из seed'ов двух чанков, остальные грани границы — стены. Префаб входа не касается границы чанка: иначе исследователь у границы увидит отличие.
- **Личное знание карты — не строка клетки** (из D15). Открытия (§2.4: знание чата = union по участникам, его читает радар) — это записи «кто, какая клетка, когда», то есть состояние, а не материализованная клетка.
- **Хранение карты чанка требует версии генерации на чанке** (из D3 и причины D7): генератор после MVP может меняться, а граница нового чанка обязана совпасть с сохраненной картой соседа.

---

## 1. Правка DESIGN.md (issue N1, отдельный PR, идет первым)

| Раздел | Что меняется |
|---|---|
| §2.2 | Вход не «якорится детерминированно от chat_id + seed мира», а размещается по спирали при активации чата в этом мире (D5, D6). «Алгоритм размещения входов» = параметр k. Абзац про профиль риска («одинокий чат на отшибе») переписывается под инверсию фронтира: глубина растет наружу от заселенной зоны, профиль риска чата задается позицией в спирали, k и исследованием. «В разных мирах разные соседи» остается, с новой причиной — порядок активации в каждом мире свой. Мир бесконечен (D12). |
| §2.2.2 | Чанк — гекс радиуса R, суперрешетка, 6 равных границ (D1). Ленивость — на уровне чанка (D4). Детерминизм переформулируется: ткань чанка — функция seed мира и координаты чанка на момент создания; тип чанка определяется историей (активация / исследование) и хранится; карта хранится вместе с версией генерации (D3); граница чанка от типа не зависит, грани общей границы с уже существующим соседом берутся из его сохраненной карты. Воспроизводимость мира = seed + таблица чанков. Отказ от пост-починки связности сохраняется. |
| §2.2.3 | Вход-префаб — 5–6 вариантов локации у ворот в центре чанка, не касающихся границы; остальное — ткань (D11). Якорение босс-области при ленивой спирали — в §16. |
| §2.2.4 | Схема: таблица чанков (координата, тип, причина, чат ворот, номер в спирали, карта, версия генерации) вместо `nodes`; клетки построчно не хранятся, хранится только их состояние (D15); дистанции — в координатах суперрешетки; глубина — до ближайших ворот. |
| §2.3 | Цена двери — от ворот своего чата, с ростом до запретительной у чужой двери и запретом в локации с чужой дверью (D13). |
| §10 | Чаты в новом сезоне добавляются по мере активации (D10). Размещение руин старых входов в новой спирали — в §16. |
| §13.4 | Словарь событий: событие создания чанка (D7). |
| §16.2 | Закрыть «алгоритмы размещения входов по мирам» (спираль + k). Добавить открытые пункты: размещение босс-области при ленивой спирали; руины (заглохший чат посреди сезона, старые входы при ротации). |
| §16.5 | Кривая цены двери получает член штрафа за близость к чужой двери (D13). |
| [IDEAS.md](IDEAS.md) | k при ротации сезона как средство разрядить обстановку (D10). |

По Propagation Rule в том же PR сверяются живые документы, повторяющие старую модель (описание мира в ai-docs, где сказано «клетка материализуется при первом посещении» и про порталы между прямоугольными чанками).

---

## 2. Новые issue

| # | Issue | Скоуп | Зависит от |
|---|---|---|---|
| N1 | Design amendment: hex chunks, lazy chunk creation, spiral gate placement | Раздел 1. Открытые вопросы раздела 4 закрываются в нем. | — |
| N2 | World generation rework: hexagonal chunks on a super-lattice | Переделка кода закрытого #27. Топология: отображение клетки в (чанк, локальная координата) на гексовой суперрешетке, расстояние по суперрешетке. Генератор: 6 равных границ и их порталы, кандидаты в островки и их емкость, входные параметры с R вместо колонок/строк, согласование границы с сохраненной картой соседа (или API под это). Префабный хук: захват **области внутри чанка** вместо целого чанка, со связностью ткани вокруг нее по построению (D11). Эталоны генерации перегенерируются. Ключ R в балансовом конфиге и его схеме. Пересмотр ключевых решений о генерации (KD-37…KD-40). Без БД. | N1 |
| N3 | Spiral gate placement and nearest-gate depth | Чистая функция: множество созданных чанков + множество ворот + k → чанк следующих ворот; дистанция до ближайших ворот. Свойства, которые проверяют тесты: первые ворота — центральный чанк; порядок спирали; созданный чанк никогда не занимается; зазор между любыми воротами не меньше k+1 чанков. Без БД. | N1, N2 |

---

## 3. Правка существующих issue (вместе с созданием N1–N3, D17)

| Issue | Что меняется |
|---|---|
| #28 World and biome config, prefab layer, chat entrance | Убрать якорение входа от (chat_id, seed мира) и открытый вопрос об алгоритме размещения — теперь это k в конфиге мира. Префаб входа — 5–6 вариантов области у ворот, не касающейся границы чанка (D11). Формат конфига мира принимает R, k, веса алгоритмов и доли (сейчас им негде жить). |
| #29 Maze persistence | Таблица чанков с сохраненной картой и версией генерации вместо построчной материализации клеток; клетки хранятся только как состояние (D15), открытия — записи знания. Ленивое создание под уникальным индексом. Выдача ворот при активации: последовательно в пределах мира (уникальный индекс не ловит нарушение зазора k двумя одновременными активациями) плюс уникальность (мир, чат). Глубина — по суперрешетке. Появляется телеметрия: событие создания чанка (D7). Вопрос «хранить дистанцию или считать» остается — на уровне чанка. Зависимости: + N2, N3. |
| #30 Chat location, onboarding, membership | Убрать «якорение входа в MVP-мире при добавлении бота»: вход появляется при активации (D6). |
| #36 Raid session FSM | Создание сессии: первый рейд чата в мир активирует чат в той же транзакции. `move(грань)` и «осмотреться» создают соседние чанки при необходимости (D4, D14). Зависимости: + N3. |
| #37 Leader screen | Кнопка «осмотреться» — только представление; механика в #36 (D14). |
| #47 MVP roadmap | Раздел C «World»: N1–N3, пометка о переделке #27, новые зависимости. Граф — новым комментарием. |

#34 и #39 по существу не меняются: дистанция в них абстрактна. Отложенный вопрос «нужен ли кэш генерации» из #27 закрывается решением D3; строки отложенного inbox меняет только `/triage`. Двери (D13) не в MVP — issue под них нет.

---

## 4. Открыто до написания текста DESIGN.md

1. **Выбор варианта входа-префаба** (D11): жребием из seed'а чанка ворот или иначе.
2. **«Чужая дверь» в D13**: только ворота чужого чата или также крафтовые двери чужого чата.
3. **«Локация» в D13**: чанк или клетка — в чем именно запрещено ставить дверь.
4. **Следствие D13 при малом k — принять или нет.** При плотной спирали путь внутреннего чата наружу проходит мимо ворот соседей, поэтому двери на этом пути дороги или запрещены; дешевые двери остаются только в направлениях, где соседей нет.

Пост-MVP, в §16 без решения: размещение босс-области при ленивой спирали; руины заглохших чатов посреди сезона и старых входов при ротации.

---

## 5. Порядок

N1 → N2 и правка тел #28, #29, #30, #36, #37, #47 (вместе с созданием N1–N3) → N3 → #28 → #29 → далее по роадмапу #47.
